# Install in /etc/tmpfiles.d/. Sorts before vendor systemd.conf so this
# path's age overrides its default two-week retention.
# Existing systemd-tmpfiles-clean.timer performs daily age cleanup.
d /var/lib/systemd/coredump 0755 root root 7d
