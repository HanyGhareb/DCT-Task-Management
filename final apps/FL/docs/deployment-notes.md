# Freelancers (App 203) — Deployment Notes

> **Keep this file current: every deployment (frontend, DB, ORDS) must be recorded here.**

- **Module code:** FREELANCERS · **ORDS base path:** `/ords/admin/fl/` · **Brand:** `#7C4DBE` (Violet)
- Depends on Admin (App 200) for auth — deploy/verify Admin first.

---

## 1. Frontend (JET SPA) deployment

- Bump `window.APP_VERSION` in `Jet/index.html` (currently `4.5.6`) — cache key for requirejs + i18n; mandatory per deploy.
- **Update `docs/functions_list.md`** if this deploy added/removed/renamed any view, viewModel method, service, or ORDS endpoint (functional inventory — see root `CLAUDE.md` → "Functions List").
- Deploy `final apps/shared/` alongside (`../shared/`). If shared/ changed, bump APP_VERSION in **all 7 apps**.
- `js/services/config.js` → live `apiBase`, never `null`.
- 17 views; FL appears in the module switcher (flipped live in Phase 4).

## 2. Database deployment (PROD schema, via SQLcl)

Run order = `db/install.sql`:

| Script | Contents |
|---|---|
| `01_fl_ddl.sql` → `04_fl_pkg.sql` | FL tables, views, seed, base package |
| `05_fl_alter_audit_cols.sql` | Audit columns |
| `06_fl_unified_adoption.sql` | ⚠ **One-way migration** — drops FL's private doc/attachment/line tables; FL uses unified `DCT_DOCUMENTS`/`DCT_BUDGET_CODING_LINES`/`DCT_REQUEST_STATUS_HISTORY` natively. Never re-create the dropped tables. |
| `07_fl_pkg_workflow.sql` | `DCT_FL_PKG` workflow engine (registration → contract → voucher lifecycle) |
| `08_fl_ords.sql` | ORDS module — run in a **fresh SQLcl session** (synonym ORA-01471 gotcha) |
| `10_fl_reg_documents.sql` | Registration required-documents: `DOCS_REQUIRED_FOR_SUBMIT` setting + `DCT_DOC_REQUIREMENTS` rows (context `REGISTRATION`). Data-only + idempotent (MERGE); own session. |

Platform SQLcl rules apply (see `final apps/Admin/docs/deployment-notes.md` §2).

FL-specific DB notes:

- **Convention:** `dct_documents.reference_id` carries the `freelancer_id` for FL rows.
- FL is lookup-first — status value sets live in `DCT_LOOKUP_VALUES`; no status CHECK constraints on new tables. Handlers call `DCT_LOOKUP_PKG.validate_lookup`.
- Approval routing: FL Manager, FL Admin joins above AED 50,000 (contract submit).

## 3. ORDS deployment

- Module `fl.rest` under ADMIN schema at `/ords/admin/fl/`; ADMIN synonyms; fresh session for the ORDS script.
- `validate_session` first in every protected handler; CGI `'AUTHORIZATION'` key (no `HTTP_` prefix).
- `:body` may only be dereferenced ONCE per handler — assign to a local first.

## 4. Post-deploy smoke test

1. Open FL from the Admin module switcher; violet brand loads from `THEME_BRAND_COLOR`.
2. Registration → Save Draft → Submit for Approval; unified inbox (Admin + FL approvals page) shows the pending step, delegation-aware (`actingFor`).
3. Contract amendment/renewal modals create drafts; voucher Confirm Payment (admin) works.

## 5. Deployment history

- **2026-07-15 — Payment methods + admin-editable generated voucher (FL JET 4.18.0, webtier release 20260714235553; ORDS `db/37`):**
  - **`db/37_fl_payment_methods.sql` — the `FL_PAYMENT_METHOD` lookup is now EFT / Ratabi / Trust.** The legacy `BANK_TRANSFER` / `CHEQUE` / `CASH` values are **de-activated, not deleted** — 9 vouchers already carry them, and deleting the value would leave those rows pointing at nothing. `EFT` is the default and the `DEFAULT_PAYMENT_METHOD` module setting (`allowed_values`, `default_value`) follows it. The voucher form now **binds the select to the lookup** instead of hard-coding three options, and **re-injects a stored-but-retired code as its own option** (KO's `options:` binding nulls a value it cannot find, which would silently blank the field of every historical voucher — the same rule already applied to contract titles).
  - **A generated voucher stays correctable by an FL admin.** The PUT handler previously refused anything that was not a DRAFT and never accepted the money fields at all. Now: **amount, due date and period label are admin-only** (403 for anyone else — the check is on the *fields present in the body*, not on the route, so a non-admin can still edit their own draft's invoice/notes), an **FL_ADMIN/SYS_ADMIN may also edit a voucher that is already SUBMITTED** for approval, and nothing is editable once it is APPROVED / REJECTED / CANCELLED / PAID. Amount must be > 0; due date must parse. **Changing amount / due date / period also re-syncs the `DCT_FL_PAYMENT_SCHEDULE` row the voucher was generated from** (response carries `scheduleSynced`), so the schedule and the voucher can never disagree. Both writes are audit-snapshotted.
  - **Frontend:** voucher detail shows the admin fields (Amount, Due Date, Payment Period, Pay Group, Description) as inputs when the current user may edit them and as read-only text otherwise; the action button reads **Save Changes** rather than "Save Draft" once the voucher is submitted, and **Submit for Approval** only appears on a draft. Saving re-loads, so the summary rail and the fund verdict re-compute against the new amount.
  - Verified on PROD: lookup returns exactly EFT/Ratabi/Trust; non-admin amount change → 403; admin edit → 200 with `scheduleSynced=Y` and the schedule row updated; amount 0 → 400; browser 9/9 (retired value still selectable and selected on the historical voucher).
  - **Post-08 re-run list is now: 15, 23, 24, 28, 29, 30, 31, 33, 34, 35, 36, 37, 38** (38 = the inbox repoint at `DCT_WF_INBOX_V`; an 08 re-run rebuilds the plain `approvals/pending` handler over it).

- **2026-07-14 — Voucher round: schedule status, auto invoice no., period, budget, attachments (FL JET 4.17.0, webtier release 20260715000554; `db/35` + `db/36` + `db/07` re-run):**
  - **`db/35_fl_schedule_status.sql` — the schedule row now mirrors its voucher.** It used to sit on PENDING after a voucher was raised and only flip to VOUCHER_GENERATED once the voucher was *approved*. Lifecycle is now `PENDING → VOUCHER_GENERATED ("Generated") → VOUCHER_APPROVED ("Approved") → PAID`, with SKIPPED unchanged: voucher creation (single **and** bulk) stamps the row; `DCT_FL_PKG` (07) moves it to VOUCHER_APPROVED on approval (`push_to_fusion` + the non-Fusion callback path) and to PAID via `receive_fusion_callback`; **a rejected voucher releases its period back to PENDING** so a fresh voucher can be raised. New lookup value `VOUCHER_APPROVED` + VOUCHER_GENERATED renamed "Generated". **Backfill included** (6 rows whose voucher predated the change were stuck on PENDING with a NULL voucher_id). Verified: generate → Generated, approve → Approved, pay → Paid.
  - **`db/36_fl_voucher_enh.sql` — voucher page.** (1) **Invoice number generated by the system** from module settings `INVOICE_NUMBER_AUTO` (Y/N) + `INVOICE_NUMBER_TEMPLATE` (default `{FREELANCER_NUMBER}-{CONTRACT_NUMBER}-{PERIOD}`; placeholders also include `{FREELANCER_ID} {CONTRACT_ID} {SCHEDULE_ID} {VOUCHER_NUMBER} {YYYY} {MM}`) via `prod.dct_fl_invoice_number()`; a value posted in the body still wins, and switching AUTO off returns the field to the user. (2) **Payment period from/to** derived from the contract's billing method (MONTHLY = the due date's calendar month, WEEKLY = the 7 days ending on the due date) — no new columns. (3) **Full budget details** on `GET /vouchers/:id`: project + name, task, expenditure type, GL combination, and the live budget / actual / commitment / obligation / fund-available figures for that line and budget year, plus the contract's value and paid-to-date. (4) **Attachments** — unified `DCT_DOCUMENTS` with `source_type='VOUCHER'`, `source_id=voucher_id`; new `GET /vouchers/:id/documents`, create/upload/delete reuse the `/documents` endpoints.
  - **Frontend:** voucher detail rebuilt as a two-column work surface (`.vch-*` in FL app.css, platform components throughout) — left: Voucher Details (invoice number read-only while auto-generated), **Budget Allocation** with a figure strip, consumption bar and green/red fund verdict, and **Attachments** (attach / view / download / remove); right: a sticky summary rail with the amount payable, the **payment period From → To**, due/invoice facts and the contract's paid-to-date bar. Payment Schedule shows the new statuses (Generated / Approved) and only offers **Voucher** while a period is Pending.
  - **Gotchas:** `DCT_MODULE_SETTINGS.value_type` CHECK allows only BOOLEAN/NUMBER/TEXT/SELECT/COLOR (not "STRING"); `SELECT v.*` plus a join that re-exposes a column the view already has (`vendor_number`) = **PLS-00402 alias required**; and an ORDS script must not run in the same SQLcl session as a package script that set `CURRENT_SCHEMA=PROD` (ORDS.DEFINE_HANDLER then fails with ORA-01403). API + browser E2E all green.

- **2026-07-14 — FL_ADMIN controls both document checklists (FL JET 4.15.0, webtier release 20260714225940; ORDS `db/34`):** the checklists are data (`DCT_DOC_REQUIREMENTS`: one row per context × document type, with `is_mandatory` = required/optional and `is_active` = shown/hidden), but FL_ADMIN could only flip `is_mandatory`, and only for REGISTRATION. **`db/34_fl_doc_req_admin.sql`** (additive; redefines GET+PUT `doc-requirements`, adds POST `doc-requirements/` and GET `doc-types/`; re-run after any `08`/`15` re-run) exposes **both switches for both contexts** plus add/remove: `GET doc-requirements/?context=&all=Y` (all=Y returns hidden rows so they can be un-hidden), **partial** `PUT doc-requirements/:id {isMandatory?, isActive?, displaySeq?}` (absent key never overwrites — the standard `does_exist` guard), `POST doc-requirements/` (adds a type, or **re-activates** a previously hidden one — `uq_dct_doc_req` is module+context+type, so never a duplicate), `GET doc-types/` (31-type catalogue for the picker). FL_ADMIN/SYS_ADMIN on every write. **Module Settings** now has a **Registration Documents** and a **Contract Documents** region, each row with Shown/Hidden + Required/Optional switches and an "Add a document" picker; hiding a row removes it from the checklist (that IS the delete — the row is kept so history stays intact). API E2E 6/6 + browser 5/5.

- **2026-07-14 — Contract document checklist reads the freelancer's record (FL JET 4.14.0, webtier release 20260714224354; ORDS `db/33`):** required documents belong to the **freelancer**, not to one contract. The submit gate in `DCT_FL_PKG` already accepted an unexpired profile document, but the checklist upload filed its copy against the CONTRACT (`source_type='CONTRACT'`), so nothing ever enriched the freelancer's record and every new contract started from scratch. Now: **uploads go to the freelancer's record** (`source_type='FREELANCER'`, `reference_id = freelancer_id`, `is_required='Y'`, with an expiry prompt so the renewal alerts and the expired-document gate can see them) — one upload satisfies this contract *and* all their future contracts, until it expires. **`db/33_fl_contract_reqs.sql`** (additive DEFINE_HANDLER-only; re-run after any `08`/`28` re-run; source synced into 28) makes `GET contracts/:id/requirements` **prefer the profile copy** and return `fileName` / `expiryDate` / `expiryStatus` / `profileExpired` + `profileExpiredOn`, so each row shows *where* the document came from ("From freelancer record" vs "Uploaded on contract"), its expiry, View/Download buttons (shared `docFile`), and — the one case a profile document does NOT satisfy — "On the freelancer record but expired on YYYY-MM-DD — upload a current copy". API E2E verified: upload on the record → satisfies the contract → satisfies a *different* contract of the same freelancer → an expired copy is flagged, not silently accepted. **Gotcha:** `APEX_JSON.write(key, '')` omits the key entirely (Oracle `'' IS NULL`), so the view binds every one of these as `$data.x`.

- **2026-07-14 — Type-ahead LOVs + the FK that blocked every real budget line (FL JET 4.13.2, webtier release 20260714222946; DB `db/32`):**
  - **`ORA-02291: FK_FLCON_EXPTYPE violated` on save (the blocker).** `DCT_FL_CONTRACTS.PROJECT_NUMBER / TASK_NUMBER / EXPENDITURE_TYPE` FK to the natural-key masters `DCT_PROJECTS` / `DCT_TASKS` / `DCT_EXPENDITURE_TYPES` (Phase 2), but those masters still held **only the original demo seed** — 0 of the 531 Fusion budget projects and 0 of the 81 expenditure types were in them, so *any* contract coded to a real budget line was rejected. **`db/32_budget_masters_sync.sql`** adds `prod.dct_budget_masters_sync` (insert-only, re-runnable) which seeds the three masters from `DCT_BUDGET_UTILIZATION_V` — the same view the Budget Allocation LOVs read, so anything pickable now satisfies the FK — plus nightly `DCT_BUDGET_MASTERS_SYNC_JOB` (02:40) to absorb new projects/tasks from later ATD reloads. First run: **531 projects / 1,373 tasks / 81 expenditure types**; contract POST on a real line verified. **Platform-wide fix — any module coding to project/task/etype was equally broken.**
  - **Searchable LOVs.** New FL component **`<lov-input>`** (`Jet/js/components/lovInput.js`, registered in `main.js`) — an `<input>` + native `<datalist>` type-ahead that resolves the typed text back to the option's **code** (exact text → exact code → single substring hit; anything else leaves the value null and shows "No match", so a half-typed entry can never be saved). Applied to 10 contract-form fields (Freelancer, Title, Terms of Payment, Project, Task, Expenditure Type, GL Entity/Cost Centre/Account/Combination). `onSearch` gives server-side search where the list is capped (projects 300, GL combinations 200). KO gotchas fixed while building it: a late `options` refresh (the debounced search landing after blur) would overwrite the text being typed → `typing` guard; and writing `value(null)` on a non-match bounced through the value subscription and wiped both the text and the "No match" flag → `settling` guard. Browser E2E 13/13.

- **2026-07-14 — Contract page review round: lookups + Budget Allocation (FL JET 4.12.1, webtier release 20260714215734; DB/ORDS `db/31`):** user review of the Phase 2 contract form. **`db/31_fl_contract_lovs.sql`** (additive, fresh SQLcl session — it creates 2 synonyms; re-run after any `08` re-run) does four things: (1) `DCT_FL_CONTRACTS.TITLE` relaxed to **NULL** and driven by a new lookup category **`FL_CONTRACT_TITLE`** (12 starter values, editable in Admin → Lookups); (2) Terms of Payment driven by **`FL_PAYMENT_TERMS`** (ARREARS default / ADVANCE); (3) new ADMIN synonyms `dct_budget_utilization_v` + `dct_gl_coa_v`; (4) seven authenticated GET routes on fl.rest — `lookup-values?cat=`, `budget/projects|tasks|etypes` (dependent LOVs over the project balances view for the contract's budget year), `budget/check` (fund verdict: `sufficient` Y/N + `shortfall`), `gl/segments?seg=entity|costcenter|account` + `gl/combinations` (cascading COA picker). Frontend: the region is renamed **Budget Allocation**, defaults to **Project**, and Project → Task → Expenditure Type are dependent selects showing each line's remaining fund; a **green flag** confirms the line covers the contract total for that budget year, a **red flag** states the shortfall and tells the user to raise a Fund Transfer or request additional fund. The GL alternative picks a real combination through Entity → Cost Centre → Account. Budget year = the contract's **start-date year**. Gotchas: `q` is an ORDS-reserved query param (these routes use `search`); a KO `options:` select **blanks its value when the current value is absent from the list**, so legacy free-text titles/terms and retired budget lines are re-injected as their own option (`keepCurrent` / `keepLegacy`); Total Amount needed `valueUpdate: 'input'` for the fund flag to track typing. Browser E2E 14/14.
  - **Follow-up (FL JET 4.12.2, webtier release 20260714221412) — empty Project LOV:** a 2-digit year typed into an `<input type=date>` is stored as **year 0026** (`0026-01-03`), so `budgetYear` asked the balances view for budget year 26 and the Project dropdown came back empty (badge read "Budget Year 26"). `budgetYear` now normalises a <100 year to 20xx and falls back to the current year outside 2000–2100; `validate()` rejects a start/completion date whose year isn't 19xx/20xx (so a year-26 date can never reach the DB); and an empty project list now says so ("No project budgets are loaded for budget year X — check the contract start date, or use the GL level instead") instead of rendering a silently empty select.

- **2026-07-14 — Document View / Download (FL JET 4.11.1, webtier release 20260714213911; ORDS `db/30`):** an approved freelancer's Documents tab listed the files but offered no way to open them — the ORDS media handler `GET /fl/documents/:id/file` has existed since `db/08`, it was simply never called (the only caller, `registrationEdit.viewDoc`, hard-coded the ADB host, which bypasses the web-tier proxy and sends no Bearer token). Now: **`db/30_fl_doc_download.sql`** (additive, DEFINE_HANDLER-only — re-run after any `08` re-run; source synced into 08) adds `fileSize` + `hasFile` to `GET /fl/documents/`, and a new shared FL service **`Jet/js/services/docFile.js`** (`view` / `download` / `hasFile`) fetches the blob through `api.fetchBlobUrl` (authed, same-origin) for **View** (new tab) and **Download** (named file) buttons on the freelancer detail Documents tab, the Compliance → Documents register and the registration document checklist. Rows with no bytes show "No file"; non-inline types (Word/Excel) download rather than open a blank tab. Gotchas: the new tab must be `window.open`ed **synchronously on the click** (a `window.open` inside the fetch's `.then()` is killed by the pop-up blocker), and headless Chromium has no PDF viewer so it converts a PDF-blob tab into a download — that's a test artifact, not a bug. Browser E2E 8/8.

- **2026-07-14 — Registration email vs staff account collision (-20148; pkg `db/07` + handler patches, DEPLOYED):** force-approving a registration whose email belongs to an INTERNAL i-Finance user died with raw `ORA-00001 (UQ_DCT_USER_EMAIL)` — approval creates the freelancer's external portal login with `username = LOWER(email)`, and the reactivation UPDATE only matched external rows. Fixes: (1) `submit_registration` fails fast with **-20148** ("email belongs to an existing i-Finance staff account — use the freelancer's own email") when the email is held by a non-external `dct_users` row; (2) `create_freelancer_profile` reactivation now matches external users by username OR email, and raises the same friendly -20148 (naming the owning account) instead of ORA-00001 when the email is taken by an internal account; (3) -20148→400 mapped in the staff submit handler (08, patched in place via a one-handler DEFINE_HANDLER — full 08 re-run NOT needed), the public portal submit (15 re-run) and registration force-approve (24 re-run). 15/24 are safe full re-runs (guarded deft/defh). Live-verified: force-approve of the colliding registration returns HTTP 400 with the -20148 message. No frontend change.

- **2026-07-14 — Post-go-live UX hotfix round (FL JET 4.10.0→4.10.1, webtier release 20260714201814; also HR 4.6.12 / CC 4.5.17 / TM 4.8.12):** two user-reported issues from first manual testing of Phase 2:
  - **Notifications page KO binding crash (FL + HR + CC + TM):** `notifications.html` bound the type badge as bare `notifType || notif_type`, but every notifications handler (FL `db/08`, CC, and the shared `/dct/notifications/` used by HR/TM) emits the key **`type`** — with the key absent at every binding-context level KO throws `notifType is not defined` and aborts the whole list foreach (red "Unable to process binding" banner). Fixed to `$data.notifType || $data.type` (+ `$data.createdAt || $data.created_at`) in all four apps — the standing "APEX_JSON omits NULL keys ⇒ always bind optional fields via `$data.`" rule applies to *mis-named* keys too. Frontend only.
  - **Approver Assignments modal Save "did nothing":** Save was silently DISABLED (`enable: !!form.userId()`) whenever the approver email didn't resolve to an active `dct_users` row (e.g. a personal/yahoo address — platform users are `*@ifinance.local`), and the validation message went to the page-level banner **behind** the modal backdrop. Now: Save is always clickable, validation + ORDS errors render in an in-modal `alert-banner--danger`, and the lookup hint spells out that the approver must be an existing i-Finance user (Admin → Users). `approverMap.js`/`.html` only.
  - Playwright verification 7/7 (notifications render 50 rows + badges with zero console errors; bad-email Save shows the in-modal error; valid email resolves, saves, row appears).

- **2026-07-14 — Contract Phase 2: Legal-Affairs termsheet + 7-step endorsement chain + termsheet PDF (FL JET 4.9.6→4.10.0; DEPLOYED end-to-end incl. webtier release 20260714195221):** digitizes the Legal Affairs "New Contract/New MOU Approval Form and Termsheet" (plan `FL_CONTRACT_PHASE2_PLAN.md`, decisions D1–D8; i-Finance REPLACES Mersal for freelancer contracts). Layer-by-layer, each verified in PROD:
  - **DB (`db/25_fl_contract_phase2_ddl.sql` + `db/26_fl_contract_phase2_seed.sql`):** 20 new `dct_fl_contracts` columns (termsheet fields + `termsheet_ref` + line-manager identity + memo From/To user FKs), NEW `DCT_FL_APPROVER_MAP` (scoped endorser assignments; UNIQUE role_code+org_id) + `DCT_FL_INSTANCE_APPROVERS` (per-instance per-step named approvers). Seeds: roles `FL_FIN_BP`, `FL_AP_OPS_MGR` (FL module) + platform-wide `FIN_DIRECTOR`, `ORG_DEV_HEAD`, `TA_MANAGER`, `HR_DIRECTOR`; lookups `FL_CONTRACT_TYPE` (4) + `FL_FTE_CONVERSION` (3); doc types `PASSPORT_PHOTO`/`FAMILY_BOOK`/`CERTIFICATE`/`IDENTIFICATION_FORM`; 6 mandatory `DCT_DOC_REQUIREMENTS` rows (context `FL/CONTRACT`); `FL_CONTRACT_APPROVAL` + `FL_AMENDMENT_APPROVAL` + `FL_RENEWAL_APPROVAL` rebuilt to the real 7-step chain — LM (USER_SPECIFIC) → **FBP (USER_SPECIFIC + role, SCOPED)** → AP Ops → Finance Director → **Org Dev Head (`condition_type=CUSTOM`, `custom_condition=FL_DURATION_GE_6M`)** → TA Manager → HR Director. ⚠ steps are MERGEd by (template_id, step_seq), never deleted — historical `dct_approval_actions` FK `step_id`.
  - **Package (`db/07` recompiled — canonical):** `submit_contract` = termsheet completeness (**-20150/-20151**) + lookup validation + **document gate -20152** (a required type is satisfied by a CONTRACT-level upload OR an unexpired profile doc via `reference_id=freelancer_id`) + LM resolution (-20153/-20154) + `termsheet_ref` stamp (`FL-TS-` from the contract number) + TA-Manager CC notification; NEW `resolve_scoped_approver(role_code, org_id)` (public; walks `dct_organizations` parents — **department beats sector**, inactive rows skipped, no mapping → NULL → step falls back to the role's holders) + per-step stamping in `wf_create_instance(p_org_id)`; `wf_next_step(+p_months)` evaluates the CUSTOM 6-month condition from `wf_months_of` (contract/amendment/renewal term); `act_on_approval` guards USER_SPECIFIC steps (**-20147**, named approver only; SYS_ADMIN + FL_ADMIN exempt — FL_ADMIN drives force-approve) + notifies the next step's NAMED approver (`wf_notify_current`); amendments/renewals submit with the parent contract's LM + org (D7; -20153 when a legacy contract has no LM — set the line manager email on the contract first); `create_renewed_contract` copies the termsheet. Unit/E2E **19/19 PASS** (scoped resolution incl. dept-over-sector + inactive, validation codes, chain walk with ODH skip <6 months and fire ≥6, -20147 guards, reject → CANCELLED).
  - **Inbox (`db/v2/11c_dct_inbox_step_approvers.sql`, canonical `db/v2/11` patched in sync — re-run 11c after any 11 re-run):** `approvals/pending` routes a USER_SPECIFIC step to its **stamped named approver**; the step role's holders see it only when NO stamp exists (fail-open), and the legacy instance-level dynamic-approver clause is likewise scoped to unstamped steps (else the LM would see every USER_SPECIFIC step, e.g. FBP's). `approvals/:id/action` maps **-20147→403** and the -20160..-20131 band →400.
  - **ORDS (`db/28_fl_contract_phase2_ords.sql`, fresh session — re-run after any 08 re-run):** `dct_fl_contract_v` rebuilt (+termsheet cols, type/FTE lookup names, manager/memo display names); contracts GET/POST/:id GET/PUT redefined (all new fields; PUT does_exist-guarded; changing `lineManagerEmail` clears the resolved `line_manager_user_id`); the 3 submit handlers map the **-20160..-20131 band → 400**; NEW `GET contracts/:id/requirements` (checklist: satisfiedBy CONTRACT/PROFILE/missing — ⚠ APEX_JSON omits the empty value, client must treat absent as missing), `GET contracts/:id/approval-history` (per-step named approvers; instance resolved via `c.approval_instance_id` so resubmits never TOO_MANY_ROWS), `POST contracts/:id/force-approve` (FL_ADMIN), `approver-map/` GET+POST + `approver-map/:id` PUT+DELETE (mutations FL_ADMIN/SYS_ADMIN; POST 409 on duplicate role+org; GET also returns the scoped `roles[]` + active `orgs[]` tree for the page); `lookups` GET + `contractTypes`/`fteConversion`. API smoke **15/15 PASS**.
  - **Frontend (FL JET 4.10.0):** `contractEdit` = sectioned termsheet form (Engagement / Commercial Terms / Routing) with lookup-driven selects, email→user lookups (contract manager, LM, memo From/To), procurement-why toggle, **Required Documents checklist** (upload per missing type via `docUpload.choose`; "From profile" badge; appears after first save — KO gotcha: bound to a NEW `hasRecord` observable, a plain `isNew` property never re-renders); `contractDetail` = **Approval Chain tab** (7-step timeline w/ named approvers, 6+months badge, current-step highlight, Force Approve for FL_ADMIN while SUBMITTED) + termsheet fields on Details + **Termsheet PDF** button (enqueue → poll → download → auto-file, below); NEW **`approverMap` page** (Admin nav, `nav.approverMap` EN+AR) = assignments CRUD over `/fl/approver-map/` with indented org tree + email lookup; contracts list + Type/TS-ref. Browser-verified **11/11 PASS** (Playwright vs dev-proxy; ⚠ dev-proxy PORT now overridable via `FL_PROXY_PORT` env).
  - **Termsheet PDF (D1/D3):** Reporting-Platform definition **`FL_CONTRACT_TERMSHEET`** (`reporting/db/22_fl_termsheet.sql`, MULTI: `con` row from `dct_fl_contract_v` + `steps` chain; **deploy via python-oracledb — MERGE-bearing**) rendered by the DB-stored Word template **`fl_contract_termsheet.docx`** (generator `FL/docs/termsheet/` + scratch script; docxtpl gotchas hit: `{%p set %}` is NOT a docxtpl tag — use plain `{% set %}` in its own paragraph; a `:MI` inside a `TO_CHAR` format mask in a section SQL = phantom-bind DPY-4008 in the python datasource — build the colon with `CHR(58)`). FL-authed bridge **`db/29_fl_termsheet_bridge.sql`** (additive; re-run after any 08 re-run): `POST contracts/:id/termsheet-pdf` (enqueue as calling user) + run status + PDF download + **`/attach`** (files the PDF into `dct_documents` as NEW doc type `TERMSHEET`, idempotent per run via a notes marker — D3 system-of-record filing). Fleet E2E **7/7 PASS** (render ~30 KB PDF, download, attach×2 idempotent).
  - **Post-08 re-run list is now: 15, 23, 24, 28, 29** (all additive/DEFINE_HANDLER scripts on fl.rest). Deploy order used: 25 → 26 → 07 → 11c → 28 → rpt template upload → reporting/22 → 29 → frontend → webtier.
  - **Ops notes:** deployed via python-oracledb on vm180 (Linux SQLcl MERGE-swallowing rule); vm180 ssh sessions were flaky for long-running commands this round — long jobs go through `systemd-run` (a transient unit also needs `HOME=/root` before sourcing env.sh) or run directly on dev-vm (python-oracledb 4.0.1 + local wallet unzip works). Approver assignments to real users + role grants (FBP/AP Ops/Fin Director/ODH/TA/HR Director) are an **Admin data task before go-live**.

- **2026-07-06 — Registration Approval History region + Force Approval (FL JET 4.8.3→4.9.0; new `db/24_fl_reg_approval_history.sql`, additive to `fl.rest`):** the registration detail (`registrationEdit`) now shows an **Approval History** region once submitted — a table of every route step (approver, status, received-on, acted-on, by, comments), who submitted it + when, the overall status, and a **"pending here"** marker on the current step. New read-only endpoint `GET registrations/:id/approval-history` builds the trail from `dct_approval_instances` + `dct_approval_steps` + `dct_approval_actions` (+ the dynamic line-manager approver for the USER_SPECIFIC first step). **Force Approve** (FL_ADMIN/SYS_ADMIN, shown only while SUBMITTED): `POST registrations/:id/force-approve {comments}` drives the shared engine (`DCT_FL_PKG.act_on_approval`) to completion — each remaining step recorded as a `[FORCE APPROVAL]` action, and the final step runs the normal `create_freelancer_profile` callback. **`db/24` is additive (deft/defh, no module rebuild, no synonyms) — re-run after any `08_fl_ords.sql` re-run.** Note: `act_on_approval` never hard-checked the assigned approver (auth is in the ORDS layer), so force = FL Admin driving it. Gotchas: `dct_roles` label col is `role_name_en`; approval step_seq may start >1 (this template starts at 5) so "first step" = `ROW_NUMBER()=1`, not `step_seq=1`, for the received-on = submitted-at logic. Playwright **7/7 PASS** (region + 3-step route + pending-here + Force Approve button + received-on). Bump `APP_VERSION` (4.9.0) + hard-reload. (Force-approve happy-path left for the user to click on their own test reg — it is irreversible + creates a freelancer.)

- **2026-07-05 — UD-FL-01: AI nationality resolution on the staff form + portal robustness (FL JET 4.8.2→4.8.3; Portal 1.13.0→1.14.0, frontend only):** the AI passport extract returns nationality as an ISO-3 code (e.g. `EGY`) or a name, but `DCT_NATIONALITY` uses ISO-2 codes (`EG`) with adjective names (`Egyptian`). On the **staff** form the nationality field had `set: null` in `AI_MAP` — so it displayed but could never be applied (checkbox disabled/unchecked). Fix (`registrationEdit.js`): nationality now `set: self.nationalityCode` with `resolve:true`; a new `resolveNat()` (ISO-3→ISO-2 alias map for ~55 common nationalities + exact-code / exact-name / prefix fallback) maps the value; the modal shows the **resolved dropdown name** (e.g. "Egyptian") so the user sees what will apply, and `applyExtract` sets the code only on a confident match. The **portal** already had a `resolveNat` (prefix-only) — upgraded with the same ISO-3 alias map so it also handles cases like `ARE`→Emirati (`portal.js`). Playwright **7/7 PASS** (EGY/Egyptian/egypt→EG, ARE/Emirati→AE, IN→IN, junk→no false match). No DB/ORDS change — bump both APP_VERSIONs + hard-reload.
- **2026-07-05 — UD-FL-01: AI-extraction spinner + show all required documents up front (FL JET 4.8.1→4.8.2, frontend only):** two UAT observations on `registrationEdit`. (1) The AI-extraction modal showed only skeleton bars, so users couldn't tell it was working — replaced with a clear **spinner** (`.ai-spinner`, brand-coloured rotating ring) + "✨ Reading your document with AI…" message (`.ai-working` in `app.css`; reduced-motion aware). (2) The Required-Documents checklist **hid** Emirates ID (until nationality=AE or an EID was typed) and the Residence Visa (until a non-AE nationality was set), so "not all documents show initially". `docChecklist` now renders **all** configured documents from the start; whether a nationality-specific doc is *required* (red `*`) is computed dynamically to mirror the server rule in `submit_registration` (Emirates ID required for UAE nationals or anyone who entered an EID; Residence Visa required for expats) — so the full set is always visible while the required markers stay accurate. Playwright **7/7 PASS** (4 docs incl. Emirates ID + Residence Visa shown initially; spinner + message render while busy). No DB/ORDS change — bump `APP_VERSION` (4.8.2) + hard-reload.
- **2026-07-05 — UD-FL-01: staff registration documents-first + Phase-1 field persistence (FL JET 4.8.0→4.8.1; new `db/23_fl_reg_staff_handlers.sql`):** clicking **Upload** (or **Photo**) on a *new* staff registration was blocked ("save the draft first"), and the staff registration data path had deeper gaps that would have broken submission. Root causes + fixes:
  - **Frontend (documents-first, like the Portal):** `registrationEdit.js` `pickDocFile`/`photoSelected` now open the file picker first (inside the click gesture) and, when there's no draft yet, **auto-save a blank draft** (draft validation is format-only) before uploading — so a document/photo can be attached (and AI-extracted) before any detail is typed. Updated the card hint + How-to step. No more "not allowed".
  - **Staff ORDS handlers were never updated for Phase 1** (only the public/portal handlers in db/15 were). The deployed `POST registrations/` and `PUT registrations/:id` (from 08) **did not persist** `requestor_* / line_manager_* / bank_*`, so a staff-created registration lost its line manager → `SUBMIT_REGISTRATION` would raise `-20141`. They also ran `TO_DATE('')` on an empty date → 500 (blocked the blank auto-draft), and `email` was still `NOT NULL`. **`db/23`** redefines the staff **list GET / detail GET / POST / PUT** to persist every Phase-1 field, guards the date (`TO_DATE(NULLIF(...,''),...)`), relaxes `email` to nullable (idempotent), and rebuilds `dct_fl_registration_v` to expose the Phase-1 columns + **LEFT JOIN** nationality (so a documents-first DRAFT with no nationality still appears in GET + the list). Detail GET returns the new fields (draft reopens intact); DOB is plain `TO_CHAR` (a birthdate must not be TZ-shifted), audit stamps use single `dct_to_local`.
  - **⚠ CRITICAL ORDS GOTCHA (new):** on this ADB, **`ORDS.DEFINE_TEMPLATE` on an EXISTING template RECREATES it and DROPS the template's other handlers** — and it does **not** raise, so a `WHEN OTHERS THEN NULL` guard is no protection. An early db/23 that called `DEFINE_TEMPLATE` on `registrations/` and `registrations/:id` silently dropped the **list GET** and **detail GET**. **Rule: to add/replace one handler on an already-existing template, call ONLY `ORDS.DEFINE_HANDLER` (never `DEFINE_TEMPLATE`).** db/23 now does exactly that and re-adds the list GET it had dropped. (db/15 was unaffected only because it defines brand-new templates.)
  - **Deploy:** `sql -name prod_mcp @final apps/FL/db/23_fl_reg_staff_handlers.sql` (any session; no synonyms, no module rebuild). **Re-run 23 after any `08_fl_ords.sql` re-run** (08 rebuilds fl.rest with the pre-Phase-1 staff handlers). **Verification:** REST round-trip **8/8 PASS** — create persists line manager + bank, detail GET returns them, blank draft creates with no 500, list works; Playwright auto-draft-on-upload **4/4 PASS** (draft auto-created, document attaches). Test data wiped via `Scripts/fl_full_wipe.sql`.
- **2026-07-05 — UD-FL-01: staff Required-Documents layout fix + discoverable AI-fill (FL JET 4.7.0→4.8.0, frontend only):** on `registrationEdit`, once a document was uploaded the row crammed the name + **AI Extract / View / Replace / ×** onto one line in the narrow side panel, so the name truncated and the buttons overlapped (the user's screenshot). Separately, the AI-fill capability existed (`runExtract` → `/registrations/:id/documents/:docId/extract` → review-and-apply modal) but was **undiscoverable** — a per-document button that only appears post-upload, with no explanation. **Fixes:**
  - **Layout (`css/app.css`):** `.reg-doc-row` is now a **column** — document info on top, actions on their own wrapping line indented under the name (`.reg-doc-actions` `flex-wrap`, `padding-inline-start:32px`); uploaded rows get a soft green background. No more overlap/truncation with 1 button (Upload) or 4 (AI Extract/View/Replace/×).
  - **AI discoverability (`views/registrationEdit.html` + `viewModels/registrationEdit.js`):** new `self.aiEnabled` observable, read from `AI_FEATURES_ENABLED` via `flService.getSettings()`. When AI is on: a **"✨ Let AI fill the form for you"** callout (`.reg-ai-hint`) sits at the top of the Required Documents card, and a matching **"Save time with AI"** step is added to *How to Complete*. The per-document **AI Extract** button is now **gated on `aiEnabled`** (no dead button when AI is off) in addition to the doc being an AI-capable type (Passport/Emirates ID/Bank Letter).
  - **Verification (Playwright, staff app via injected ADMIN session):** AI callout renders on the empty state; uploaded-row shows AI Extract + View + Replace stacked **below** the info (bounding-box check confirms no overlap) — **4/4 PASS**. Screenshots captured. Test drafts cleaned via `Scripts/fl_full_wipe.sql`. No DB/ORDS change — just bump `APP_VERSION` (done: 4.8.0) and hard-reload.
- **2026-07-05 — UD-FL-01: Portal ↔ staff registration parity + unified structure (Portal 1.12.0→1.13.0; db/15 ORDS re-run):** the public Portal wizard captured fewer fields than the staff JET form (`registrationEdit`) and grouped them differently, so the two channels wrote different data shapes. Decision (user): **full field parity** + **keep the Portal's maroon/gold brand but mirror the staff form's section structure**. Findings: the Portal's `portal.js` and the public `PUT reg/public/:token` handler already wired almost every field (Arabic names, specialization, full bank block, requestor auto-set at OTP-verify, line-manager-required at submit) — the gap was purely that `index.html` never **rendered** them. **Fixes:**
  - **Portal `index.html`** — restructured the wizard's Details + Review steps into three sections that mirror the staff form: **Personal details** (First/Last EN + **First/Last Arabic** + DOB + Nationality + Emirates ID [conditional `*` for UAE nationals via new `rgNeedsEid`] + Passport + Mobile + **Specialization** + **Notes**), **Requestor & line manager** (Line Manager e-mail now shows a required `*`; hint = "first approver, must be a registered DCT user"), **Bank details** (Bank Name + IBAN + **Account Holder** + **Account Number** + **SWIFT** + **Currency**). New `.dlp-formsec` gold section-divider heading in `portal.css` (RTL-aware). Review "Back" now returns to Details.
  - **Portal `portal.js`** — added `rgNotes` observable + `rgNeedsEid` computed; `notes` added to the detail payload; `regSaveDetails` now defaults requestor = applicant (name from first+last). New i18n keys (EN+AR): `firstNameArLbl`/`lastNameArLbl`/`specializationLbl`/`notesLbl`/`secPersonal`/`secApprover`/`secBank`/`lineMgrHint`/`bankHint`.
  - **db/15 (ORDS, fresh session)** — public `PUT reg/public/:token` now reads/writes **`notes`** (INSERT + UPDATE) and defaults `requestor_email`/`requestor_name` to the applicant with `NVL(...)` so a detail save can't wipe the value the `/draft` endpoint seeded. No new columns (all already existed). Handlers re-verified present.
  - **Verification:** Playwright render check **16/16 PASS** (all new fields present on Details + Review; screenshots confirm the maroon/gold brand + three section bands). Test-created draft cleaned via `Scripts/fl_full_wipe.sql`. New reusable wipe script saved at **`final apps/FL/Scripts/fl_full_wipe.sql`** (FK-safe full FL data wipe; `ALTER SESSION DISABLE PARALLEL DML` for the ORA-12839 guard; keeps config/doc-requirements).
- **2026-07-05 — UD-FL-01 review fixes: Portal photo capture + configurable required/optional documents (FL JET 4.6.5→4.7.0, Portal 1.9.0→1.10.0):** three UAT-feedback items on Freelancer Registration (tracked in `FL/UD/UD-FL-01_Freelancer-Registration/`).
  - **US-09 — public Portal blocked submit on a required photo with no way to upload one.** Root cause: `submit_registration` (`07:1087`) hard-requires `photo_blob` when `PHOTO_REQUIRED='Y'`, but the Portal wizard had no photo control and there was no public photo endpoint (so every SELF reg had `photo_blob=NULL`); worse, the public submit handler didn't map `-20132` so it surfaced as a raw 500. **Fix (db/15, re-run fresh session):** new **`PUT reg/public/:token/photo`** (raw-binary → `photo_blob`, `MAX_UPLOAD_MB` 413 guard, mirrors the staff `registrations/:id/photo`) + added `-20132→400` to the public-submit error map. Portal (`index.html`/`portal.js`/`portal.css`): a **Personal photo** card in the docs step with file picker + live preview, honouring `PHOTO_REQUIRED` (badge Required/Optional); `regDocsContinue` blocks advancing when a required photo is missing. **Live E2E (Playwright): PASS** — email→OTP→docs, photo upload → `hasPhoto` true, preview + saved chip shown.
  - **US-10 — make required-vs-optional documents (and photo) configurable in Settings.** The submit check was **already** data-driven on `DCT_DOC_REQUIREMENTS.is_mandatory`; what was missing was a management surface. **Fix (db/15):** new **`PUT doc-requirements/:id`** (FL_ADMIN/SYS_ADMIN, sets `is_mandatory` Y/N, validates value). FL **Module Settings** page gains a **Registration Documents** section — a Required/Optional toggle per document (Passport / Emirates ID / Residence Visa / Bank Letter) plus **Photo required** (`PHOTO_REQUIRED`), saved together with settings. `flService.updateDocRequirement`; new i18n `set.grp.regdocs`/`set.regdocs.hint`/`set.req`/`set.opt` (EN+AR). **Live-verified via a minted admin session:** PUT valid→200, invalid value→400, no-auth→401, DB flipped then reverted.
  - **US-11 — confirm uploaded documents are saved.** **Verified in PROD**: for all recent SELF registrations `docs == docs_with_bytes` (bytes persist to `DCT_DOCUMENTS.file_blob`). Added a visible **"✓ Saved"** chip per uploaded document + photo on the docs step and an **Uploaded documents** summary on the review step.
  - Also: the public `GET reg/public/:token` now returns `photoRequired` / `docsRequiredForSubmit` / `hasPhoto` / `docRequirements[]` so the wizard reflects the admin config. **Deploy:** re-run `db/15_fl_reg_ords.sql` in a fresh SQLcl session (create-or-replace handlers; no other DB change — settings/columns already existed). No new go-live gate (photo capture uses the existing `PHOTO_REQUIRED`, still Y).
  - **US-12 follow-up (Portal 1.10.0→1.11.0, frontend only):** the docs step had a **hardcoded** 3-card list (Passport / Emirates ID / Bank Letter), so a non-AE applicant could never upload the required **UAE Residence Visa** → submit always failed `-20142`. The docs step is now **data-driven**: `portal.js` builds `rgDocs` from the `docRequirements[]` returned by `GET reg/public/:token` (one upload card per configured document, required **or** optional, with its badge; localized EN/AR card names). AI auto-extract + "Read with AI" only run for **AI-capable** types (`PASSPORT`/`EMIRATES_ID`/`BANK_LETTER` — the only ones `DCT_FL_AI_PKG` has prompts for); other docs (Residence Visa) are upload-only. Also stripped the raw `ORA-#####:` prefix from user-facing errors (in the Portal's `api()` wrapper). **Playwright E2E: 4 doc cards incl. Residence Visa, photo upload + saved chips — ALL PASS.** No DB/ORDS change; just hard-reload the Portal.
  - **Public duplicate message (db/15 re-run, ORDS only):** the exact-duplicate block in `submit_registration` raises `-20001` with *"…An FL administrator must override this duplicate before it can be submitted."* — correct for **staff** (they can override) but useless to a **public** applicant. The public `reg/public/:token/submit` handler now special-cases `SQLCODE=-20001` and returns self-service guidance instead: *"These details match a registration that already exists … Please go back and reset or correct your details, then try again. If you have already registered before … contact the Finance team."* Staff message unchanged (shared `submit_registration` untouched). Deploy = re-run `db/15` (fresh session).
  - **UX polish (Portal 1.11.0→1.12.0, frontend only):** (1) each document region now has a distinct **gold header band** flush to the card top (`.dlp-doc-lbl` restyled: bold gold uppercase + tinted band + bottom divider) so Passport / Emirates ID / Residence Visa / Bank Letter / Personal Photo are clearly delineated. (2) **Mobile number is now required** on the Details step — `regSaveDetails` blocks continue if it's empty (new i18n `regDetailsRequired`), and all five required detail fields (first/last/DOB/nationality/mobile) show a red required asterisk (`.dlp-req`). Playwright E2E: header bands render, empty-mobile blocked, filled-mobile proceeds — ALL PASS.
- **2026-06-15 — Region header flush + missing region headers (FL APP_VERSION 4.5.7→4.5.8; shared/ change → all 8 apps bumped):** `platform.css` now pulls a card's **first-child** `.section-heading`/`.section-heading-row` flush to the top/left/right border (negative margin + top-only radius; `!important` so it beats the legacy inline `margin-top:0` some views set). This removes the gap between every existing in-card header and the card border platform-wide. Added themed headers to previously headerless main regions: registrationEdit (Registration Details), freelancerDetail (Profile Details, Contracts; normalized Bank Accounts/Documents into `.section-heading-row`), contractDetail (Payment Schedule/Amendments/Renewals/Contract Details), contractEdit (un-pinned the stray heading), voucherDetail (Voucher Details). Frontend/CSS only — no DB/ORDS change.
- 2026-06-12 (Phase 4): `fl.rest` + 17-view JET app deployed live; Stage 6 seed (roles + 84k contract); UAT 35 cases (34P/1PARTIAL automated run).
- dct_auth delegation made ADDITIVE; registrationEdit lookup race fixed (UAT round).
- 2026-06-13: Save Draft/Submit moved to page headers; 5 modal footers moved into modal headers (platform top-right rule). Frontend-only; bump APP_VERSION on next deploy.
- 2026-06-13 (APP_VERSION 4.2.1): FL-CMP-02 fix (compliance doc row deep-links to Documents tab via one-shot `flDetailTab` sessionStorage key); `formGuard.track()` adopted in registrationEdit/contractEdit/voucherDetail; bulk voucher generation — new `POST /fl/schedule/bulk-generate` handler in `08_fl_ords.sql` (403 unless `ALLOW_BULK_VOUCHER_GENERATION=Y`; ≤100 rows/run; skips rows with open vouchers) + "Generate All Due" page action on Payment Schedule. ⚠ Requires rerun of `08_fl_ords.sql` (fresh SQLcl session).
- 2026-06-13: **Region appearance theme** — headers + borders themed via `THEME_REGION_*` (db/v2/22 seeds FL override rows, NULL = inherit; rows listed in Module Settings). Boots via `shell.initRegionTheme` + `flService.getSettings`. APP_VERSION **4.3.0** (shared/ change, all 7 apps). Module Settings SELECT options now split on `|` as well as `,` (seeded `allowed_values` are pipe-separated). Note: the new `Portal/` SPA has its own styling and is NOT covered by the region theme.
- 2026-06-13 (deployed): FL-CMP-02 fix + formGuard + bulk voucher generation live — `08_fl_ords.sql` rerun, UAT extended to **40 cases, 40/40 PASS** (`UAT/UAT_FL_Results_13-Jun-2026-01.docx`). FL UAT data cleanup script at `db/v2/23_fl_uat_cleanup.sql` (run after UAT passes; preserves seeded samples).
- 2026-06-13 (deployed): **Freelancer Self-Service Portal Phase 1** — new SPA at `Portal/` (plain KO, no requirejs, EN/AR; own `ifinance_portal_session` key, APP_VERSION 1.0.0 in `Portal/index.html`). DB: `09_fl_portal.sql` (portal columns on freelancers, `DCT_FL_PORTAL_INVITES`/`DCT_FL_PORTAL_SESSIONS`, `DCT_FL_PORTAL_PKG`, `FL_PORTAL_STATUS` lookup, `FEATURE_FL_PORTAL`/`PORTAL_SESSION_HOURS`/`PORTAL_INVITE_EXPIRY_HOURS` settings). ORDS: `/fl/portal/*` handlers + staff `POST /fl/freelancers/:id/portal-invite` (FL_ADMIN). **Security model: portal identities are NOT DCT_USERS** — portal tokens only open `/fl/portal/*`; staff tokens are refused there (verified both ways). Kill switch `FEATURE_FL_PORTAL` (currently Y for demo). Dev URL: `http://localhost:8080/FL/Portal/index.html` via the FL dev-proxy (proxy now also serves `/<App>/Portal/`).
- **2026-06-13 — Module Settings redesign (APP_VERSION 4.4.0):** settings page restyled to match Admin System Settings — top-right Save, category cards, switch-row toggles, dirty tracking, alert banners, and a **Region Appearance** palette picker (module-level `THEME_REGION_*` override with live preview + AA-contrast check). New shared helper `shared/js/regionPicker.js` (used by all 7 apps → APP_VERSION bumped 4.3.0→4.4.0 everywhere). Region keys are read/written through this module's existing `/settings` endpoint.
- **2026-06-15 — Registration field validation (FL APP_VERSION 4.5.1):** new-registration form had NO format validation (only presence of name/DOB/nationality/email). Added two-layer validation: **client** (`Jet/js/viewModels/registrationEdit.js`) and **server** (`db/07_fl_pkg_workflow.sql` `submit_registration`, errors `-20133…-20139`, recompiled in PROD — spec+body VALID). Rules: email format (becomes portal login), DOB not future + age ≥ 18, Emirates ID `784`+15 digits (required for AE nationals — pre-existing `-20131`), passport 6–12 alnum, at least one of Emirates ID/passport, mobile 7–15 digits intl-friendly.
  - **Client UX = inline per-field validation:** field validators in a `V` map + a `self.err.<field>` observable each; fired on **blur** (`change` for the nationality select) via `self.checkField(name)`, showing a red border (`.form-control--error`) and a message under the field (`.field-error`). `validate(opts)` has two modes: **Submit = full** (required + format + at-least-one-ID); **Save Draft = `{draft:true}`** = format-only (blank fields allowed, but any value entered must be well-formed). Both block on error (FL 4.5.3; 4.5.1 left Save fully lenient, 4.5.2 gated Save completely, 4.5.3 settled on format-only for drafts).
  - **Shared CSS:** added `.form-control--error` / `.field-error` to `final apps/shared/css/platform.css` (additive, no existing page uses them) → per convention bumped **APP_VERSION in all apps**: FL 4.5.0→4.5.1, AR/PC/HR/Admin/CC/DT 4.5.0→4.5.1, TM 4.6.1→4.6.2 (FL Portal excluded — separate SPA, doesn't load platform.css).
  - DB recompile only (no ORDS/synonym change). Note: submit ORDS handler still returns these as HTTP 500 with message (same as existing `-2013x`); 400-mapping not added.
- **2026-06-15 — Binary document upload, ~24 KB cap REMOVED (FL APP_VERSION 4.5.7; shared/ change → all 8 apps bumped):** the shared `documents/:id/file` PUT was rewritten to read the file's **raw bytes from `:body`** (deref ONCE — `:body_blob` binds wrong on this ORDS version per the AR note) instead of base64 in a `VARCHAR2(32767)` JSON field. Size now guarded by the new **`MAX_UPLOAD_MB`** module setting (default 10 → HTTP **413** above it). New shared platform layer: `shared/js/api.js` `putBinary()` / `fetchBlobUrl()`; `shared/js/docUpload.js` (`<doc-upload>` KO component + `docUpload.choose()` programmatic picker); `.docup-*` in `platform.css`; `docup.*` i18n keys (common.en/ar). `registrationEdit` dropped its bespoke base64 + canvas-downscale + 24 KB-reject logic for `docUpload.choose()` → `flService.uploadDocumentFile(id, file)` (now `putBinary`, signature changed `(id,b64,mime)`→`(id,file)`). Submit handler `registrations/:id/submit` now maps `-20131..-20142`/`-20001`/`-20090` → **HTTP 400** (was 500) — supersedes the prior note. DB: `db/v2/27_max_upload_mb.sql` (seeds MAX_UPLOAD_MB ×4 modules: FL/CC/TM/HR) + FL `db/10_fl_reg_documents.sql` also seeds it + `08_fl_ords.sql` rerun (fresh session). **Verified live: 1.5 MB PDF round-trip OK, 11 MB → 413, submit 400.** ⚠ supersedes the "~24 KB cap" warning in the entry below.
- **2026-06-15 — Registration required documents (FL APP_VERSION 4.5.4):** registration page now collects the **required documents** (passport always; Emirates ID if AE or ID entered; residence visa for non-AE; bank letter) before Submit, with a right-side **How to Complete** instructions panel. All layers deployed + verified in PROD:
  - **DB:** `db/10_fl_reg_documents.sql` (NEW, idempotent) seeds setting `DOCS_REQUIRED_FOR_SUBMIT=Y` + 4 `DCT_DOC_REQUIREMENTS` rows (context `REGISTRATION`). `db/07_fl_pkg_workflow.sql` recompiled: `submit_registration` raises **`-20142`** listing any missing mandatory docs (gated by the setting; conditional applicability for Emirates ID / visa applied in code); `create_freelancer_profile` (also mirrored in `04_fl_pkg.sql`) re-points the registration's docs to the new freelancer on approval (source_type REGISTRATION→FREELANCER). Spec+body VALID.
  - **ORDS (`08_fl_ords.sql`, rerun fresh session):** `GET /registrations/:id/documents`, `GET /doc-requirements?context=…`, `DELETE /documents/:id` (soft delete `is_active='N'`). Synonyms `DCT_DOC_REQUIREMENTS`/`DCT_DOCUMENTS`/`DCT_DOCUMENT_TYPES` already existed.
  - **Frontend (FL-only, no shared/ change):** registrationEdit two-column layout (`.reg-layout` in `css/app.css`) — left form, right `<aside>` with the document checklist (upload/replace/remove/view per item, ✓/missing status, conditional rows by nationality) + instructions. flService: `getDocRequirements`/`getRegistrationDocuments`/`deleteDocument`. Submit blocked until mandatory applicable docs attached (mirrors server). ⚠ **Document file size cap ~24 KB** (the shared `documents/:id/file` PUT binds `VARCHAR2(32767)`): images are auto-downscaled; larger PDFs are rejected client-side. Lift later by switching that handler to a CLOB bind if real scans are needed.
- **2026-06-27 — Phase 1 Registration revamp (AI extraction · dual-channel · dedup · line-manager-first) — FL APP_VERSION 4.6.0, Portal 1.1.0:** registration can now be started by a freelancer themselves (public Portal, email-OTP verified) OR by any DCT employee (JET); AI reads Passport / Emirates ID / Bank Letter and pre-fills the fields; the request captures requestor + line-manager emails with the **line manager as the first approver**; duplicates are blocked (exact) / flagged (fuzzy). DB scripts (run in order, fresh session for ORDS):
  - `db/11_fl_reg_phase1_ddl.sql` — `DCT_FL_REGISTRATIONS` +17 cols (requestor/line-manager identity, `email_verified`, bank capture, dedup state), new `DCT_FL_DOC_EXTRACTS` + `DCT_FL_REG_OTP`, shared additive `DCT_APPROVAL_INSTANCES.dynamic_approver_user_id`, 6 settings (`AI_FEATURES_ENABLED`=N, `AI_MODEL`=claude-sonnet-4-6, `AI_MAX_TOKENS`, `REG_OTP_*`, `DUP_BLOCK_ON_EXACT`), 3 lookup sets. Idempotent (guards use `all_*` owner=PROD).
  - `db/12_fl_ai_pkg.sql` — `DCT_FL_AI_PKG.extract_document` (Anthropic vision via DBMS_CLOUD, reuses AR ANTHROPIC key; gated by `AI_FEATURES_ENABLED`). **Also grants `EXECUTE ON DBMS_CLOUD TO PROD`** — that grant had been lost platform-wide, leaving AR/TM/ATD/PC `*_AI_PKG` bodies INVALID; all recompiled VALID. PL/SQL gotchas hit: `JSON_VALUE` on a CLOB / param / OUT-param fails in PL/SQL expr context → used `JSON_OBJECT_T`; package functions + `SQLERRM` not usable inside SQL DML → locals.
  - `db/13_fl_reg_pkg.sql` — `DCT_FL_REG_PKG`: `find_duplicates` (exact EID/passport/email/IBAN + fuzzy name+DOB across registrations & freelancers), `add_duplicate_override` (FL_ADMIN), OTP `start_public_registration`/`verify_public_otp` + token helpers. OTP times use SYSDATE (avoid TIMESTAMP-vs-TZ session skew). OTP email via `APEX_MAIL` (best-effort — needs instance SMTP configured).
  - `db/07_fl_pkg_workflow.sql` recompiled — `submit_registration` resolves the line manager → DCT user (`-20140/-20141`), runs dedup (blocks exact unless `dup_status=OVERRIDDEN`, flags fuzzy), passes the LM as `wf_create_instance(p_dynamic_approver=>…)`; `wf_create_instance` writes `dynamic_approver_user_id` + notifies the named approver for USER_SPECIFIC step 1; `create_freelancer_profile` creates the primary bank account from the registration bank fields. (Mirror edit in `04_fl_pkg.sql`, but 07 is canonical — it "replaces 04 output".)
  - `db/14_fl_reg_seed.sql` — prepends USER_SPECIFIC **Line Manager Endorsement** step (seq 5) to `FL_REGISTRATION_APPROVAL` (resolved per-instance via `dynamic_approver_user_id`; chain stays editable in Admin Approval-Templates).
  - `db/v2/11b_dct_inbox_userspecific.sql` — surgical re-define of the shared `approvals/pending` GET handler: `LEFT JOIN dct_roles` + `OR (step_type='USER_SPECIFIC' AND dynamic_approver_user_id = me)` so the line manager sees the item. **Additive — role-based routing for all other modules unchanged.** (Canonical source also patched in `db/v2/11_dct_ords.sql`.)
  - `db/15_fl_reg_ords.sql` (fresh session — synonyms) — 11 new `fl.rest` handlers: AUTH `registrations/:id/documents/:docId/extract`, `registrations/:id/duplicates`, `registrations/:id/duplicate-override`, `users/lookup`; PUBLIC (gate = `FEATURE_FL_PORTAL` OR `ALLOW_SELF_REGISTRATION`) `reg/public/{start,verify,nationalities,:token GET/PUT,:token/documents POST,:token/documents/:docId/file PUT,:token/documents/:docId/extract POST,:token/submit}`. Adds a `fl.selfservice` system DCT user (instance.submitted_by FK for SELF channel). ⚠ The `DCT_FL_AI_PKG` synonym needed a manual re-create (first run skipped it) — verify it after running 15.
  - `db/16_fl_ar_fix.sql` — repairs the Arabic lookup/step labels via **UNISTR** (SQLcl read the UTF-8 seed files as cp1252 → mojibake; UNISTR is encoding-independent). Verified Arabic renders correctly.
  - **Smoke-tested in PROD:** AI gate (disabled → 400), `find_duplicates` (caught exact email match), OTP create + expiry, dedup JSON well-formed. **All FL/AI objects VALID.**
  - **Frontend:** staff JET `registrationEdit` — requestor (auto from session) + line-manager email (with `/users/lookup` hint) + bank fields, **AI Extract** per uploaded doc → review-and-apply modal (confidence + warnings), duplicate banner with FL-Admin override. New `flService`: `extractRegistrationDocument`/`getRegistrationDuplicates`/`overrideRegistrationDuplicate`/`lookupUser`. Portal: new self-registration **wizard** (email→OTP→details→docs+AI→review→submit→done). APP_VERSION FL 4.5.9→**4.6.0**, Portal 1.0.0→**1.1.0**.
  - **Pending live E2E (needs ops):** set `AI_FEATURES_ENABLED='Y'` for the AI vision test; configure instance SMTP for OTP email delivery.
  - **2026-06-27 — Portal v1.2.0 (UX round: AI gating · wrong-doc detection · documents-first):** three changes from self-registration feedback.
    1. **AI button gated by the live setting** — `GET reg/public/:token` and `POST reg/public/verify` now return `aiEnabled` (= `AI_FEATURES_ENABLED='Y'`); the Portal hides the **Read with AI** button when AI is off (server still 400-gates the extract endpoint regardless).
    2. **Wrong document in a slot is caught** — `DCT_FL_AI_PKG` (db/12) prompts now also ask the model for `document_kind`; `extract_document` compares it to the slot and returns `detectedKind` + `typeMismatch:true` + a warning. The Portal shows the warning in **red and does NOT fill the form** on a mismatch. Verified live: an Emirates-ID file in the Passport slot → `typeMismatch:true`.
    3. **Documents-first flow** — after OTP the wizard now goes **docs → details → review** (was details → docs → review). New `POST reg/public/:token/draft` creates an empty DRAFT up-front so files can be uploaded before any details; on upload the Portal **auto-runs** the AI read (when enabled) and accumulates the fields. To allow an empty draft, **`db/17_fl_reg_docfirst.sql`** relaxes `DCT_FL_REGISTRATIONS.{FIRST_NAME_EN,LAST_NAME_EN,DATE_OF_BIRTH,NATIONALITY_CODE}` to NULL (idempotent); required-field enforcement is unchanged (the detail PUT + `submit_registration` still reject an incomplete row, so nothing reaches approval with nulls). Deploy order: `db/17` then re-run `db/12` then `db/15` (fresh session). Portal `APP_VERSION` 1.1.0→**1.2.0**.
  - **2026-06-27 — Public Portal landing redesign (Portal APP_VERSION 1.6.0→1.7.0):** imported the **"DCT Freelancers Portal — Landing"** design from the Claude Design project (`b44e08d2-…`, via the DesignSync MCP) and implemented it as the Portal's public login/landing page. Maroon-wash + gold-accent theme, **Newsreader** (display) / **Libre Franklin** (UI) fonts, full-bleed Abu Dhabi photo slider, glass **AI self-registration** card (3 steps + "Register with AI →" → `openRegister`), hero, trust row (Government-backed, UAE PASS), benefit strip, slider dots/nav, location tag. All new markup is scoped under **`.dlp`** in `css/portal.css` (the in-app screens keep the editorial-paper theme). The email/password sign-in + invite set-password forms moved into a **glass overlay** (`.dlp-auth`, toggled by `loginOpen`; auto-opens for the `setpwd` invite link). EN/AR + full RTL mirror; responsive (hero/card stack < 960px). `portal.js` adds `slides`/`slideIdx`/`slideGo`/`slideNext`/`slidePrev` (5.5 s autoplay) + `openLogin`/`closeLogin` + `dlp*` i18n. **Photos:** the slider references `assets/photos/abudhabi-1..4.jpg`; the design's own photos exceed the MCP `get_file` 256 KiB cap so they were **not** imported — each slide has a distinct maroon **gradient fallback** that shows until the 4 JPGs are dropped into `FL/Portal/assets/photos/`. Verified via Playwright (desktop / mobile / sign-in overlay / Arabic RTL — no KO/JS errors, only the expected photo 404s). Frontend only — no DB/ORDS change.
  - **2026-06-27 — Self-registration wizard re-skin to match the landing (Portal APP_VERSION 1.7.0→1.8.0):** the public self-registration **wizard** was restyled from the old editorial-paper `.login-stage`/`.login-card` chrome to the **same maroon-wash + gold-accent `.dlp` theme** as the new landing, so the whole public portal is visually consistent. The wizard now renders as a full-screen `.dlp dlp--wiz` panel that **reuses the landing's background photo slider** (`slides`/`slideIdx` autoplay) + maroon wash + gold pattern, a matching top bar (brand · لغة pill · Sign in → `regBackToLogin`), and a centered **glass wizard card** (`.dlp-wizbox`). Added a **step rail** (`.dlp-rail`, E-mail → Verify → Documents → Details → Review) with on/done states driven by a new `regStepIdx` computed in `portal.js`; document slots are themed cards (`.dlp-doc` + gold file-button + `.dlp-ghost` "Read with AI"); details/review use a two-column `.dlp-grid2`; the OTP field is centered/letter-spaced; the DEV-echo banner, error box, and a green-check confirmation are all `.dlp`-styled. New i18n keys `regStep{Email,Verify,Docs,Details,Review}` (EN+AR). All new CSS is scoped under `.dlp` (`.dlp-wiz*`/`.dlp-rail*`/`.dlp-doc*`/`.dlp-grid2`/`.dlp-done*`) with RTL letter-spacing resets + a < 560px single-column responsive rule. **No KO binding/handler names changed** — only markup classes + the new `regStepIdx` computed — so all existing wizard logic (start/verify/draft/extract/submit) is untouched. Verified via Playwright across all 6 steps + Arabic RTL + mobile (no KO/JS errors, only the expected photo 404 gradient fallbacks). Frontend only — no DB/ORDS change.
  - **2026-06-27 — Grouped Module Settings page (FL JET APP_VERSION 4.6.0→4.6.1):** the Freelancers **Module Settings** page now groups settings into separate themed `.section-heading` cards (same format as Region Appearance) instead of one flat list — **Document AI** (`AI_*`), **Self-Registration** (`ALLOW_SELF_REGISTRATION`/`FEATURE_FL_PORTAL`/`REG_OTP_*`/`DUP_BLOCK_ON_EXACT`), **Document Uploads** (`MAX_UPLOAD_MB`), and **General** (anything unmatched, so nothing is hidden). Grouping is client-side in `moduleSettings.js` (`GROUP_DEFS` + `groupItems`, builds `self.groups` from the same item objects so dirty-tracking/save are unchanged); view renders `foreach: groups`. New i18n keys `set.grp.*` (EN+AR). Frontend only — no DB/ORDS change.
    - **Rolled out to all module settings pages (2026-06-27):** same grouping applied to **PC / DT / HR / CC** `moduleSettings` (identical `GROUP_DEFS`/`groupItems` + `foreach: groups` wrapper). Group titles live in **shared `i18n/common.{en,ar}.json`** (`set.grp.*`) so every app resolves them via `t()` (common is the base dict, app overrides). Canonical `GROUP_DEFS` families: `AI_*`→Document AI, self-registration keys→Self-Registration, `*UPLOAD*`→Document Uploads, `*NOTIF*`→Notifications, `FEATURE_*`→Features, else→General. **TM** has no module-settings page (skipped); **Admin** systemSettings is already grouped (the reference format). APP_VERSION bumped on all JET apps (shared `common.*` change): PC/DT/CC 4.5.6, HR 4.6.1, FL 4.6.1, Admin 4.5.7, AR 4.5.6, ATD 1.15.2, TM 4.8.1.
  - **2026-06-27 — Cross-document name check toggle:** setting `AI_NAME_MATCH_CHECK` (Y/N, default Y, `db/21_fl_name_check.sql`) gates the different-person warning in `DCT_FL_AI_PKG`.
  - **2026-06-27 — Automatic Gemini→Claude AI fallback (db/12 + db/22):** to make extraction reliable despite Gemini free-tier 429s, `DCT_FL_AI_PKG.call_ai` now **transparently falls back to Claude** when the active provider is GEMINI and the request fails (429 / throttle / outage). Refactor: the payload-build+send+parse moved into a nested `do_call(format,base,model,key)` (raises -20002 on non-200); the base64 is encoded **once** and reused across attempts. `call_ai` runs the primary provider, and on failure — if `AI_FALLBACK_CLAUDE='Y'` (new setting, **db/22**, BOOLEAN default Y) and an active ANTHROPIC key exists (`get_provider_by('ANTHROPIC',…)`) — re-runs the SAME document through Claude. Package body vars `g_used_provider`/`g_used_model`/`g_fellback` record what actually served the call; `extract_document` writes the **real model** to the audit row, adds an info note ("…read with Claude instead.") to `warnings`, and includes `"provider"`/`"fellback"` in the envelope (the Portal already surfaces warnings in the success message). **Live-verified:** forced Gemini to a throttled model (`gemini-flash-latest`→429) → envelope returned `"provider":"ANTHROPIC"`, `"fellback":true`, `DONE`, audit model `claude-haiku-4-5-20251001`. Deploy: `@db/12` then `@db/22` (any session). No frontend change. Setting appears in Module Settings → Document AI group automatically (`/^AI_/`).
  - **2026-06-27 — Show extracted data immediately + Gemini per-model 429 (Portal v1.9.0):** end-user request — *"display the extracted info immediately after extraction"*. The docs step now renders a gold **"Extracted by AI"** summary card directly under each document the moment a read succeeds, listing the read field label→value pairs (Passport: first/last name, passport no., nationality, DOB, expiry; Emirates ID: name EN/AR, EID, nationality, DOB, card expiry; Bank Letter: bank, IBAN, holder, account no., SWIFT, currency). Implemented purely client-side: each `rgDocs` row gets an `extracted` observableArray, populated in `regExtract` from `res.fields` and cleared on re-pick/re-extract; new `.dlp-extract*` CSS + i18n keys (`extractedTitle`, `expiryLbl`, `nameEnLbl`, `nameArLbl`, `accountNameLbl`, `accountNoLbl`, `swiftLbl`, `currencyLbl`, EN+AR). Verified via Playwright. **Also (ops):** Gemini's free tier rate-limits **per model** — `gemini-flash-latest` was returning HTTP 429 while `gemini-2.5-flash` returned HTTP 200 with the same key (the user had selected `gemini-flash-latest` in Settings). Reset `AI_MODEL=gemini-2.5-flash`. Note for go-live: the free Gemini quota is low/bursty; if 429s persist, switch `AI_PROVIDER=ANTHROPIC` (Claude, reliable) — both paths produce the same envelope. Portal `APP_VERSION` 1.8.0→**1.9.0**.
  - **2026-06-27 — THE real "Could not read this document" bug: invalid JSON `confidence` (db/12):** even with a working model and a server-side `DONE` extract ("data extracted well"), the Portal/JET always showed **"✕ Could not read this document"**. **Root cause (provider-independent — broke BOTH Claude and Gemini):** `extract_document` built the response envelope with `... ',"confidence":' || NVL(TO_CHAR(v_conf),'null')` — and Oracle's `TO_CHAR(0.98)` yields **`.98` with no leading zero**, which is **invalid JSON**. The browser's strict `JSON.parse` rejected the whole envelope, the shared `api.js` wrapper swallowed the parse error to `{}`, so `extractId`/`fields` were missing → the honest-error branch fired. Any confidence < 1 (almost always) triggered it, which is why it *never* worked on the client regardless of model. **Fix:** emit a valid JSON number — `TO_CHAR(ROUND(v_conf,4),'FM9999990.0000','NLS_NUMERIC_CHARACTERS=''.,''')` (e.g. `0.9900`), NLS-forced so a `,` decimal can't sneak in. **Verified:** re-extracting the real passport now returns `IS JSON: VALID`, `"confidence":0.9900`, `"extractId":38`. Server-side only — no frontend change, no cache bust needed; just retry the extraction. (This was the actual cause of the "extracts well but client says could not read" reports; the model/masking fix below was a separate, real issue.)
  - **2026-06-27 — Gemini extraction fix: bad model + masked error (db/12 + db/20):** users reported AI passport extraction "didn't work" on Gemini. **Root cause:** the selected model **`gemini-2.0-flash` returns HTTP 429** (free-tier quota/rate exhausted) on the production key, and **`gemini-1.5-flash` returns HTTP 404** (retired) — while **`gemini-2.5-flash` and `gemini-flash-latest` both return HTTP 200** and extract correctly with the *same* key (verified by a direct `DBMS_CLOUD.SEND_REQUEST` probe of all four). The failure was disguised because `DCT_FL_AI_PKG.call_ai`'s retry loop caught the raised `ORA-204xx` and, after 4 tries, always reported the generic **"AI service is busy"** (-20002), hiding the real 429/404. **Fixes:** (1) `db/20` now drops `gemini-2.0-flash` + `gemini-1.5-flash` from the `AI_MODEL` picker (allowed = `gemini-2.5-flash|gemini-flash-latest|claude-sonnet-4-6|claude-haiku-4-5-20251001`) and defaults to **`gemini-2.5-flash`**; (2) `db/12` `call_ai` now parses the HTTP code out of `SQLERRM`, **only retries genuinely transient codes (429/5xx)** and on any 4xx (400/401/403/404) raises immediately with the **true error + the model name** ("…request failed: ORA-20404 …HTTP 404. Pick a different AI Extraction Model in Settings"), so a misconfigured model is diagnosable instead of looking "busy". **Live-verified:** with `AI_PROVIDER=GEMINI` + `gemini-2.5-flash`, `extract_document(90,77)` on the real passport returned `DONE`, confidence 0.98, all fields parsed (+ correct cross-doc `nameMismatch` on the mixed-person test reg). Current PROD settings: `AI_PROVIDER=GEMINI`, `AI_MODEL=gemini-2.5-flash`. Claude path unchanged and still works (toggle `AI_PROVIDER=ANTHROPIC`). Both scripts recompiled VALID. Deploy: `@db/12` then `@db/20` (any session). **No frontend change** (existing honest-error UI already shows the raised message).
  - **2026-06-27 — Configurable AI provider (Anthropic ↔ Gemini) + extract robustness (Portal v1.4.0):**
    - **Provider switch** — new setting **`AI_PROVIDER`** (SELECT `ANTHROPIC|GEMINI`, default ANTHROPIC; `db/19_fl_ai_provider.sql`, currently **GEMINI**). `DCT_FL_AI_PKG` (db/12) now reads the active provider's `api_format`/`base_url`/`model_id`/`api_key` from the shared `dct_ar_ai_providers` registry (`get_provider`) and builds the right payload: Anthropic messages API **or** Google **Gemini** `generateContent` (inline_data + `response_mime_type:application/json`, key via `x-goog-api-key` header). Model now comes from the provider row (Gemini = `gemini-flash-latest`), not the FL `AI_MODEL` setting. Added `generativelanguage.googleapis.com` to the network ACL. (Gotcha: `active_code` is a pkg fn → assign to a local before using in `get_provider`'s SQL, else PLS-00231.) Verified live: Gemini extracts + classifies (`typeMismatch`) correctly.
    - **Extract robustness (root-cause of "green ✓ but empty form")** — the model's raw JSON could be invalid for the browser's strict `JSON.parse`, so the shared client wrapper swallowed it to `{}` → no `typeMismatch`, no fields, yet a green "filled in" message. Fixed both ends: server **canonicalises** `fields` via `JSON_OBJECT_T.parse(...).to_clob` (guaranteed-valid JSON, `{}`+warning on failure); client now (a) treats a response with no `extractId` as an error, (b) **counts applied fields** and shows `✓ Read — N field(s) filled`, or an amber `⚠ Read, but no <doc> fields were found` when nothing usable came back (so a wrong file can no longer look successful).
    - **Transient-error retries** — Gemini's free tier intermittently returns `503` ("overloaded") / `429`; `call_ai` now retries up to 4× with backoff (2/4/6s) on `429/500/502/503/529` and, if still failing, raises a friendly "service is busy, try again / enter manually" message (mapped to HTTP 502 for the client). Verified the retry recovers from a live 503.
    - **dev-proxy timeout** raised 30s→180s in `FL/Jet/dev-proxy.py` (Gemini on a 2–3 MB PDF exceeds 30s). **Dev only** — production serves ORDS directly. Restart the proxy after pulling.
    - Portal `APP_VERSION` 1.3.0→**1.4.0**.
  - **2026-06-27 — Selectable AI model + better EID field mapping (Portal v1.5.0):**
    - **`AI_MODEL` is now a dropdown** (`db/20_fl_ai_model.sql`; value_type SELECT, options `gemini-2.0-flash|gemini-2.5-flash|gemini-flash-latest|gemini-1.5-flash|claude-sonnet-4-6|claude-haiku-4-5-20251001`, current **gemini-2.0-flash**). `DCT_FL_AI_PKG.get_model` (db/12) uses the setting **only when it matches the active provider family** (gemini* with GEMINI, claude* with ANTHROPIC); otherwise falls back to the provider row's model — so flipping `AI_PROVIDER` can't leave a mismatched model. End users switch provider+model in Admin > Freelancers > Module Settings.
    - **Cross-document identity check (catch DIFFERENT people)** — `DCT_FL_AI_PKG` now compares the name on each uploaded doc against the others via **token overlap** (`name_key`/`shares_token`: pipe-set of ≥3-letter tokens); if a new doc shares **no** name token with a previously-read doc it sets `nameMismatch:true` + a clear warning. Replaces the old loose substring check. Portal surfaces it as a prominent amber **⚠** on the doc. Verified live: passport(Ahmed Ali Mohamed) + EID(Hany Ghareb) → `nameMismatch:true`. (It's a strong warning that flows to human approval, not a hard block — name variations/transliteration are common.) Needs ≥2 docs read to compare. **Toggle: setting `AI_NAME_MATCH_CHECK` (Y/N, default Y, `db/21_fl_name_check.sql`) — N skips the check entirely.**
    - **`call_ai` model bug fixed** — it was sending the provider row's `model_id` and ignoring the `AI_MODEL` setting (the setting only fed the audit row). Now `v_model := get_model` so the configured model actually drives the request.
    - **Retry on raised transient errors** — `DBMS_CLOUD.SEND_REQUEST` *raises* (ORA-20503) on some 503s instead of returning the status, so the retry loop now also catches exceptions (backoff 3/6/9s) before giving the friendly "service busy" message.
    - **Emirates-ID extraction now fills Last Name + Nationality** (Portal): EID returns a single `name_en` and a nationality *name* — the portal now splits `name_en` into first/rest, fills Arabic name from `name_ar`, and **resolves the nationality name/code → dropdown code** (`resolveNat`: matches code, exact name, or prefix so "Egypt"/"Egyptian"/"EGY" → `EG`). Passport branch also maps nationality now; ALL-CAPS scans are Title-Cased. Portal `APP_VERSION` 1.4.0→**1.5.0**. (Frontend only.)
  - **2026-06-27 — DEV OTP echo switch (Portal v1.3.0):** gated testing aid for when instance SMTP isn't configured. New setting **`REG_OTP_DEV_ECHO`** (BOOLEAN, default **N**, `db/18_fl_otp_dev_echo.sql`). When `Y`, `start_public_registration` returns the plaintext code via a new OUT param and the `POST /reg/public/start` response includes `"devCode"`; the Portal prefills the code field and shows a "DEV mode — your code: …" banner. **MUST be set back to N before go-live** (it exposes verification codes). Deploy: re-run `db/13` (proc signature changed: added `p_dev_code OUT`) then `db/15` (fresh session) then `db/18`. Currently **Y** for the testing round.
  - **2026-06-27 hotfix (db/15) — public draft save was HTTP 555:** the `PUT reg/public/:token` handler declared a local `FUNCTION g()` and called it **inside the INSERT/UPDATE SQL** → `PLS-00231: function 'G' may not be used in SQL` → the inline block fails to compile → ORDS returns **555** (its "block didn't compile" status; client surfaced "HTTP 555" on *Save & continue*). Fix: read every JSON field via `g()` into a **local variable** in PL/SQL context first, then use the locals in the DML (locally-declared subprograms can't appear in SQL; `APEX_JSON.get_*` built-ins still can, which is why the other public handlers were fine). Re-ran `db/15` (fresh session) — verified `PUT` now returns 200 + `registrationId`.
- **2026-06-15 — Form Layout shared component PROMOTED (FL APP_VERSION 4.5.5→4.5.6):** the unified create/edit **Form Layout** pattern (Shared UI design-lab `components/form-layout/`, modelled on the Oracle JET Cookbook job-application sample) was promoted into `final apps/shared/css/platform.css` as the additive **`.fl-*` block** — `.form-grid--1`/`--label-start`/`--compact`, `.form-group--span2`, `.form-label .opt`, `.fl-legend`, `.fl-section`/`.fl-section-intro`/`.fl-rail`, and **review mode** (`.fl-review` ancestor + `.fl-readonly` value twins). Overrides no existing class, so it is inert until an app opts in (bump-on-adopt per the shared-change rule — only FL bumped now). Frontend/CSS only — no DB/ORDS change. Adopters (both in FL APP_VERSION 4.5.6):
  - **`registrationEdit`** — added the `.fl-legend` "Fields marked * are required" line only (page had in-flight doc-checklist WIP, kept light).
  - **`contractEdit` (full review-mode showcase)** — wrapped the card with `css:{ 'fl-review': !editable() }` so a **submitted/active** contract renders as clean read-only value rows (`.fl-readonly` twins) instead of greyed-out disabled inputs; added `.fl-legend` + `.opt` optional tags + `*` required markers. ViewModel gained 6 display computeds (`freelancerDisp`/`orgDisp`/`glDisp`/`totalAmountDisp`/`endDateDisp`/`billingComboDisp`) that resolve select IDs → labels and format AED amounts (Latin digits) for the read-only twins. No DB/ORDS/endpoint change. Verified both states (DRAFT edit + ACTIVE review) via a KO harness against the real platform.css (no binding errors).
