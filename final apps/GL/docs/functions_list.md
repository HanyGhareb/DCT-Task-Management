# Financial Planning and Budgeting — FP (App 210) — Functions List

> **2026-07-22 (v1.42.0) renames:** the app is now **Financial Planning and Budgeting** (`appName`, sub "FP"); the **Actuals** tab is now **General Ledger** (`navActuals`, `view()==='actuals'` unchanged); **Budget Utilization** is now **Project Budget Utilization** (`navButil`/`buTitle`); **Overview** and **Explorer** are merged into one **Chart of Accounts** page — see below.


User-facing functions by area. Each area = a `view()` section in `Jet/index.html`; each bullet is a
public method on the single KO viewModel in `Jet/js/app.js`. Mandatory maintenance artifact —
update on any view/method/endpoint change.

## Chart of Accounts (`view()==='overview'`) — the ONE CoA hub: values + mapping + explorer + overview (v1.73.0)
**Four sub-tabs since v1.73.0 (2026-08-21, user-specified order): 1 Classification values | 2 Manage CoA Mapping | 3 Combinations explorer | 4 Classification overview** — `.coa-tabs` strip under the page head; `coaTab('cls'|'map'|'exp'|'ov')` (default `'cls'`) drives which single region shows; the collapsible chevrons and the four `coa*Open` observables + `toggleCoa` are RETIRED. `loadCoa()` still loads all three data sets once on landing / `go('overview')` (`coaLoaded` guard). **`classifications`, `mapping` and `explorer` view ids + nav entries are removed — `go()` redirects them to `overview`.**
- **Tab 4 — Classification overview**: coverage stats `combinationCount`, `pctClassified`, per-dimension `valueCount`/`mappedSegments` (from `/boot`) + "How it works".
- **Tab 1 — Classification values** (was the Classifications page): `clsType` dimension selector + `addValue`/`editValue`/`saveValue`/`deleteValue` + row-click `openClsDrill` assignments drawer. Own loader flag `clsLoading` (was the shared `loading`, which now only drives the Explorer table). `clsType.subscribe` reloads while on `overview`.
- **Tab 2 — Manage CoA Mapping / Segment mapping** (was the Segment Mapping page): `mapType`/`segSearch`/`segOptions`/`mapSegment` picker + effective-history timeline + `addMapping`/`editMapping`/`deleteMapping`. `mapType.subscribe` reloads while on `overview`.
- **Tab 3 — Combinations explorer** (`.bu-results`, **maximizable** via `toggleCoaMax()`/`coaMax` — forces `coaTab('exp')`, Esc restores): filters `expSearch`/`fSector`/`fChapter`/`asOf` → `loadCombos(offset)`, `comboRange`, `exportCsv()` (≤500 rows).

## Classifications — MERGED into Chart of Accounts Tab 1 (v1.42.1)
- See the Chart of Accounts section; `view()==='classifications'` removed, `go()` redirects to `overview`. All value-management methods unchanged.

## Segment Mapping — MERGED into Chart of Accounts Tab 2 (v1.42.1)
- See the Chart of Accounts section; `view()==='mapping'` removed, `go()` redirects to `overview`. All mapping methods unchanged.

## Explorer — MERGED into Chart of Accounts (v1.42.0)
- The combinations explorer is now Tab 3 of the Chart of Accounts page (see above); `view()==='explorer'` and its nav entry are removed. `loadCombos`/`exportCsv`/`comboRange` and the combo observables are unchanged.

## General Ledger (`view()==='actuals'`) — Budget vs Actual report (YTD per GL combination)
- full-viewport width (the `.wrap.wide` modifier is applied on Actuals + Dashboard).
- search criteria: `acPeriod` (**mandatory**, defaults to current period), `acSector`, `acChapter`, `acProgram`, `acAppr`, `acAccount`, `acCostCenter`, `acSource` (transaction-source — keep rows whose measure is non-zero; reactive list `acSources` = budget/commitment/obligation/**openCommitment**/**openObligation**/glActual/grn/apDirect), `acSearch` — populated by `loadAcFilters()` (`/actuals/filters`, now also returns `accounts` + `costCenters` LOVs).
- `runActuals()` — **one-shot loader (v1.57.0)**: page-merges `/actuals` (server clamp 1,000 rows/request) up to 10,000 rows client-side for the IR grid (`AC_MAX`/`AC_PAGE`, `acTruncated` note; the pager is gone); `acReset()` — clear filters, keep current period; a `.bu-load-ov` **oj-progress-circle busy overlay** covers the `.ac-body` wrapper (KPI band + register) on load and every search.
- **Report-parameters drawer (v1.23.0)** — the filter card moved OFF the page into a right-edge slide-in drawer (`.dw-filter`, min(720px, 94vw)): `openAcFilters()`/`closeAcFilters()`/`applyAcFilters()` (Apply = search + close; Reset re-runs and keeps the drawer open; Esc closes), page-head **Filters** button with an active-criteria count badge (`acActiveCount`, `.fbadge`), and an **applied-criteria chip bar** on the page (`.ac-chipbar` — period chip + one labelled chip per active filter via `acFilterChips`; click anywhere on it to open the drawer). **v1.39.0** — Sector/Chapter/DCT-Program/Appropriation/Account/Cost-Centre are **multi-select** (pick→chip, `ac*Sel` arrays + `acAddSel`/`acChipRemove`, pipe-delimited any-of request), plus a MANDATORY **Account Type** multi-select filter (`acAccTypes` 1=Assets…5=Owner's Equity; defaults to Expense, cannot be emptied, required marker on the label) and an **Account Type** column in the results table + CSV. Selected values render as a removable-chip tray in the drawer. **v1.57.0** — the six big LOVs (Sector/Chapter/DCT Program/Appropriation/Account/Cost Center) are **type-ahead `<input list>` datalist fields** (value = code, label = name; `acLovCommit` matches exact code → `code · name` → exact name → unique contains, then chips the pick; Enter or change commits); Account Type / Transaction Source / Period remain selects. **v1.59.0** — a **"Figures in" display-unit select** (the SHARED `buUnit`/`buUnitOpts` attribute from Budget Utilization, persisted in `gl_bu_ui.unit`): KPI band formats via `buNum`, the IR register scales money columns by the unit divisor + `(B/M/K)` label suffix in `acIr` (Auto/Exact = full numbers; drills/popover keep unscaled rows); display-only, instant.
- **Grouped KPI band (v1.23.0)** — the 14 flat `.kstat` tiles became **6 measure-group cards** (`.kgrid`/`.kg-*`): Budget (+ `acUtilTxt`/`acPctW` utilization bar), Commitment (PR) = Total + Open/Pipeline rows, Obligation (PO) = Total + Open/Pipeline rows, Open Encumbrance = total + Open PR/Open PO rows, GL Actual = total + GRN/AP Direct/SLA rows, Funds Available (GL) = total + Calc row + `acFundsTxt` remaining-% (red `.neg` when negative). Each card: accent spine + icon chip + Fraunces value + sub-rows.
- business-question answer cards (14): `tot(k)` reads the `totals` summary — budget, **Total PR** (`prTotal`), **Open Commitment** (`openCommitment`), **Commitment Pipeline** (`commitmentPipeline`), **Total PO** (`totalPo`), **Open Obligation** (`openObligation`), **PO Pipeline** (`poPipeline`), **Open Encumbrance** (`openEncumbrance`), GL actual, GRN actual, AP direct, SLA actual, **Funds Available (GL)** (`fundsAvailable`), **Funds Available (calc)** (`fundsAvailableCalc`).
- result register — **SHARED `<interactive-report>` since v1.57.0** (`reportCode GL_BUDGET_ACTUAL_IR`, section `ac`; envelope `acIr` pureComputed): flat columns replacing the old stacked `.grp-cell` table — frozen **Combination** (mono) + **Cost centre** columns, grouped header bands **Commitment (PR)** = Total/Open/Pipeline/PR-count, **Obligation (PO)** = Total/Open/Pipeline/PO-count, **GL Actual** = Actual/GRN/AP direct/SLA, **Funds available** = GL/Calc, per-group tints (`.acg-*`/`.acc-*`) + per-column ⓘ hints, zebra; the IR's own search / column show-hide / filters / multi-sort / CSV+XLSX / saved layouts all apply. Figure semantics unchanged: Commitment from `ATD_PR_*` (**Total** = Reserved+Liquidated / **Open** = Reserved / **Pipeline** = Not-reserved); Obligation from `po_distributions` (**Total** = all except Failed/Passed / **Open** = Reserved+Partially-Liquidated **GRN-netted** / **Pipeline** = Failed/Passed); **Open Encumbrance** = Open PR + Open PO; **AP Direct** = AP lines with `po_number IS NULL`; **SLA** = GRN + AP direct. Delegated wrapper handlers (`acGridOver`/`acGridMove`/`acGridClick`, `ko.contextFor`): the Combination cell shows the 10-segment popover (side map `acRowMap` recovers the full source row) and **every figure cell drills** (`AC_METRIC` column→metric map → `openDrill`).
- **KPI-card aggregate drill (v1.40.0)**: `openAcAgg(metric)` opens the shared `.dw-*` drill drawer with the supporting lines across the WHOLE filtered set (`GET /actuals/lines` with the filter set and no `cc` = aggregate mode; leads with Cost-centre/Account/Combination columns, capped 500 w/ `count` "top N of M", total reconciles to the KPI). Every KPI-band figure is drillable (`.kg-drill`). **v1.58.0** — `fillAcAgg` post-processor: Cost-centre column = `code - name` (resolved from the `/actuals/filters` LOV), rows guaranteed amount-descending + a **sort-criteria pill** above the table (`drillSortNote`/`.dw-sort`, cleared on close), and Combination cells show the 10-segment popover (delegated `drillGridOver/drillGridMove` on the drawer table — plain-KO cell context `$data`=col/`$parent`=row, source row via `acRowMap`).
- drill-down (per cell): `openDrill(row, metric)` (`/actuals/lines`, metric ∈ budget|commitment|opencommitment|commitmentpipeline|obligation|openobligation|popipeline|glactual|grn|apdirect) → wide modal; `closeDrill()`. **commitment/openCommitment/commitmentPipeline** → real PR lines (PR #, description, requester, funds status); **obligation/poPipeline** → PO lines; **openObligation** → PO lines with Ordered / Received (GRN) / Open columns (the netting made visible). Modal header carries an **Export CSV** button (`drillExportCsv()`, shown once lines are loaded).
- `acExportCsv()` — CSV of the full filtered set (≤5000 rows, incl. all 3-figure Commitment/Obligation + Open Encumbrance + both Funds Available); `acRange` — "loaded of total". The IR toolbar adds its own CSV/XLSX of the visible grid.

## Financial Performance (`view()==='fmr'`) — Budget Overview, Entity + Sector level (v1.99.0)
Replicates `docs/Reports/FMR/FMR_Dashboard.pdf` pages 2-3 (the corporate Power BI "Budget Overview" report) as a live nav tab, sitting right after Dashboard in the General Ledger group. Sources the same `dct_budget_actual_period_v` the Budget vs Actual page reads, plus the GL Cashflow plan table.
- criteria: `fmrYear` (select, this year ± 2/+1) + `fmrPeriod` (MM-YYYY, all 12 months of the picked year via `fmrPeriods`). `loadFmr()`/`go('fmr')` seeds the default from `GET /actuals/filters`' `defaultPeriod` (current calendar month if it has data, else the latest loaded period) — NOT the browser's real "today", which can be ahead of the last extract.
- **Entity Level section**: KPI band (`fmrOverall`: Budget/Actual/Plan/Actual-vs-Budget%/Actual-vs-Plan%/Funds Available, reusing the `.bu-kpis`/`.bk-*` tiles) + 5 half-donut gauges (`fmrEntities`: DCT/ALC/Museums/Masterpieces/**Unclassified** — a real, sizeable "budgeted but not yet in the COA snapshot" bucket, reported rather than folded into DCT; reuses the Sector Performance report's `.sp-gauge`/`spGaugeDash()` verbatim) + a YTD Trend chart (`fmrTrend`: Payroll/Opex/Capex/CWIP/Other/**Unclassified**, grouped Budget+Actual+Plan bars via the `.sp-cols`/`.sp-b` CSS-height pattern — no Chart.js, this app hand-builds every chart).
- **Current Month Variances / Expected Variances** — two free-text narrative panels (`fmrNoteCurrentText`/`fmrNoteExpectedText`, `fmrSaveNote(noteType)` → `PUT /fmr/notes`) backing the PDF's two commentary boxes; one note per (accounting period, type), upsert, empty text deletes.
- **Sector Level section**: entity filter toggle buttons (`fmrToggleEntityFilter(code)`/`fmrSectorEntitySel`, any-of, "Select all" = none picked) narrowing the sector table (`fmrSectors`: Sector/Budget/Actual/Actual-vs-Budget%/Plan/Actual-vs-Plan%, sorted by Actual-vs-Plan% desc). **"Comments" button per row is NOT a new comment store** — `fmrOpenSectorComments(row)` opens the **existing** SECTOR-level Budget Utilization Comments thread (`self.openCmt({level:'SECTOR', ekey: row.code, ...})`) for the same sector/period, so a note left here is the same thread as the one opened from the Budget Utilization page's own Sector aggregate tab.
- entity/type classification and the one-off Payroll Plan 2026 data load are documented in `GL/db/37_gl_fmr_ords.sql`'s header and `docs/deployment-notes.md`.
- **round 3 (v1.101.0)**: collapsible Search region (`fmrSecSearchOpen`/`toggleFmrSec`, `gl_fmr_ui`) w/ Search/Reset + **Generate Report ▾** dropdown (`fmrGenOpen`/`toggleFmrGen`/`runFmrReport('PDF'|'XLSX'|'PPTX')` — enqueue `POST /fmr/report`, 5s poll, auto-download); all 5 KPI tiles drill (`fmrOpenKpiDrill`) and sit on ONE row (`.bu-kpis.sp-kpis` 5-col grid); entity gauges carry icon discs (`fmrEntIcon(code)` inline SVG: DCT landmark / ALC book / Museums columns / Masterpieces framed art); ⓘ hint popovers (`.fmr-hint`, formula-first EN+AR `fmrHint*` keys) on every KPI, gauge, the YTD Trend header and the Sector Level header; trend bars show permanent value labels + styled hover tooltips (`.sp-b-val`/`.sp-b-tip`); Bar Build loading spinner (`.fmr-bars`).

## Budget Status (`view()==='fd'`) — FD Dashboard (v1.102.0 → v1.105.0)

**Deep review, v1.116.1:** `fdNoBudget(scope)` and scope-aware `fdIs(kind,scope)` show Ledger rows with a notice for all-zero-budget maps. `runFd()` ignores stale requests; cached period changes clear obsolete errors. Search keyboard/accessibility and configured Entity names corrected. Export numbers match the dashboard; long entries paginate intact. Entity report validation uses active classification values. Existing print-permission compatibility policy is unchanged pending the user decision. Round-4 UAT records the full review.

**Deployed v1.113.0 (2026-09-06):** compact Presentation rail; `fdShowSummary` checkbox (false on load/Reset) controls formatted ring/fund, sector and department summaries in all presentations (v1.115.1), without changing selection or drills. `fdPrintOpen` / `toggleFdPrint()` expose Print → PDF/PPT; `runFdReport(format)` freezes current filters/presentation and runs the Projects-style authenticated report queue, polling and download flow. All departments are included beyond the screen's top-N. See [budget-status-review.md](budget-status-review.md) for validation, format details and deployment checklist.

The user's notebook sketch (`final apps/BI/docs/FD Dashboard/`) as mockup A "Ring Bands": a Sectors card strip on top, then one band per Chapter with Budget / Actual / Encumbrance / Fund Available, across every business unit. General Ledger sub-tab right after Financial Performance. **Same basis as the FMR tab** (GL balances, Budget Group 1, Chapters 1–3, entity from the ES segment) so the two pages always reconcile.
- **Entity classification (v1.114.0)**: Chart of Accounts → Classification values lists the ENTITY dimension (Default chip, `vDefault` in the editor); the assignments drawer adds a GL-segment column + per-row picker (`segments`, `segName/segNameOf`, `clsLoadSegOptions(key)`), Manage CoA Mapping a segment picker (`mapSegKey`), the explorer an Entity filter/column (`fEntity`, `entityOpts`); `fdEntName`/`fmrEntName` read the classification names.
- criteria (collapsible Search region, `fdSecSearchOpen`/`toggleFdSec`, `gl_fd_ui`): `fdYear` + `fdPeriod` (MM-YYYY YTD; `loadFd()` seeds the default from `GET /actuals/filters` `defaultPeriod`, the FMR rule) + **Entity** segmented toggle (labelled Business unit until v1.109.0; `fdEntity`/`fdSetEntity(code)`: All entities / DCT / MSS / ALC / Masterpieces from the response `entities[]`) + the SHARED `buUnit` "Figures in"; `runFd()` = ONE `GET /fd/status` per period, everything else recomputes client-side; `fdReset()`.
- **Sectors** (`fdSectors`: every sector with figures in the current business-unit scope, budget-desc; icon per sector code via `fdSectorIcon(code)`, name/budget/% used/utilisation bar): click = `fdToggleSector(code)` any-of multi-select (`fdSel`, `fdIsSel`), `fdSelSummary()` "{n} selected" / "Showing all sectors", `fdClearSectors()`; switching the business unit drops picks whose card disappeared.
- **Chapter bands** (`fdChapters`: CH1 Payroll / CH2 Opex / CH3 Capex over `fdRows` = scope ∩ picked sectors): Budget / Actual / Encumbrance ring gauges (`fdRingDash(v,budget)`, `fdPct`) + the Fund Available square (`fdFundSub`: "% of budget remains" / "over budget", red when negative). Footnote = basis + `fdExcluded()` (no-chapter / Chapters 4–6 budget, reported never dropped) + `fdTotals()` grand total.
- **Departments (Cost Centres) region (v1.104.0)**: second card region under Sectors — `fdDepts` = the cost centres of the picked sectors (all when none picked; `costCenters[]` LOV attributed to one sector each, scope totals recomputed from `rows[]`, budget-desc), multi-select any-of (`fdCcSel`/`fdIsDeptSel`/`fdToggleDept`/`fdClearDepts`, `fdDeptSelSummary()`), header text filter `fdDeptQ` narrows the cards only; `fdRows` = business unit ∩ sectors ∩ departments feeds the bands, the footnote and the drills (`costcenter=`); `fdPruneDepts` drops picks that leave the sector/business-unit scope.
- **Presentation switch (v1.105.0 → v1.106.0)**: Search-region parameter `fdLayout` (v1.106.0: three **radio option cards** `.fd-pres-opt` from `fdPresOpts` [name + description], `fdSetLayout(v)`, followed by the formatted hint box `fdPresHint()` = reading text + state-pill thresholds + legend; Composition tiles [default] · Ledger rows · Proportional map, remembered in `gl_fd_ui.layout`) renders BOTH the Sectors and Departments regions in the picked style over the same selection state — tiles = big compact budget (`fdBig`) + share of scope (`fdShare`) + composition bar (`fdSeg`, Actual/Encumbrance/Fund colours) + state pill (`fdState`/`fdStateCss`/`fdFreeTxt` on the echoed `thresholds{near,over}`) + header chips (`fdSelChips`) + **sort on both regions** (`fdSecSort`/`fdDeptSort` over `fdSortOpts` Budget amount [default] · % used · % free · Name → `fdSectorsSorted`/`fdDeptsSorted`; none under map) + **coloured hint strips** (`.fd-hint`: lead + body + `fdHintTip()` reading tip + sort statement + `fdDeptTopTxt()` + legend); rows = ledger on one shared scale (`fdRowW`) with four figure columns; map = squarified treemap (`fdSectorTiles`, `fdDeptGroups` nested per sector, `fdHeatBands` legend, `fdMapW` measured by the `fdMeasure` binding, `fdTileStyle`/`fdGrpStyle` RTL-safe). Departments show the top N (`fdDeptTopN` 12/18) with `fdToggleDeptAll` "Show all" (`fdDeptsShown`, `fdDeptHasMore`, `fdDeptMoreTxt`, `fdDeptCountTxt`); the map always draws every department (unbudgeted items get a sliver).
- **Year series + monthly-movement hint (v1.107.0, approach A)**: one `GET /fd/status` per YEAR — `fdCurRows` derives any loaded period of the year from the response `series[]` (`fdRowsAt`; `fdPeriodsLoaded`), so a period change inside the year is client-side (`fdPeriod.subscribe` fetches only for another year); hovering/focusing a ring or the Fund square → `fdTrOver(item, metric, e)` → `fdTrend(item, metric)` = one bar per loaded period ≤ the selected one under the band's scope (ytd, mov = ytd − previous, brought-forward pale + movement strong; negative in red) shown in the fixed `.fd-trend` popover (`fdTrShow/fdTrX/fdTrY/fdTrData`, re-anchored on scroll, `fdTrOut` hides). **v1.108.0**: the same ladder on every SECTOR and DEPARTMENT card in all three presentations — `fdTrCard(kind, item, metric, e)` (card = Actual; a figure inside it = that measure; anchored on the card) → `fdTrendSector(s, metric)` (BU scope) / `fdTrendDept(c, metric)` (BU ∩ picked sectors) via the shared `fdTrBuild` builder, + a 4-measure chip strip (`metrics[]`) under the popover header; no request. **v1.109.0**: Presentation = a vertical radio stack pinned to the last two Search-grid columns (`.fd-pres-field` grid-column -3/-1 spanning the criteria + hint rows; mirrors in RTL); Business unit relabelled **Entity** / **All entities**.
- **Figure drill (v1.103.0)**: every ring and the Fund square is a button → `fdOpenDrill(item, metric)` opens the SHARED drill drawer with that chapter's rows at full 10-segment combination grain under the band's own scope (period · business unit · picked sectors), total == the figure clicked; the response `combos{}` feeds `fdComboMap` so hovering a Combination cell shows the styled 10-segment code + description popover (same as the Budget Utilization drills).

## Budget Utilization (`view()==='butil'`) — project budget vs actual (per task & expenditure type)
- full-viewport width (`.wrap.wide` also applied on this view). One row per **Budget Year × Project × Task × Expenditure Type**, budget > 0 lines only.
- page layout (v1.14.0) = three **brand-headed regions** (`.bu-sec`): **Search** (filters, collapsible), **Overview** (KPI band, collapsible), **Results** (table + pager, maximizable). `toggleBuSec(k)` collapses with a rotating chevron; state persists in `localStorage('gl_bu_ui')`; collapsed headers show live summaries (`buSearchSummary` = year + active-filter count via `buActiveFilters`; `buKpiSummary` = compact Budget · Fund). `toggleBuMax()`/`buMax` maximizes the Results region to a full-screen surface (z 55, below the drill drawer/modal/toast so drills open above it; sticky thead + pinned pager; body scroll locked; **Esc restores**, guarded while a drawer/modal is open).
- search criteria: `buYear` (**mandatory**, defaults to latest year), `buPeriod` (**Accounting period, v1.16.0** — Full year + the 12 MM-YYYY months of the selected year via `buPeriodOpts`; **YTD semantics** with a ⓘ hint; defaults to the **current period** when the year is current — first load, Reset and year-change), **Project Type = MULTI-SELECT since v1.60.0** (`buTypeSel` chips + `buTypePick`/`buTypeAdd`; `buType` is a computed returning the pipe-joined any-of list so all senders — butil/encumbrances/pending/report bridges — are unchanged; **defaults to the single `DCT OPEX Project Type` chip** on first load + Reset; server = INSTR any-of in GL/db/07/12/13/15 + the 4 report definitions), `buSector`, plus four **autocomplete LOV filters** (v1.12.0, native `<datalist>`): `buCc` (Cost Center — option label = department), `buProject` (Project — option value = number, label = name; contains-match on number OR name), `buTask` (Task), `buEtype` (Expenditure Type), and free-text `buSearch`. Select LOVs from `loadBuFilters()` (`/butil/filters`); autocomplete lists from `loadBuLovs()` (`/butil/lov?year=`, reloaded when `buYear` changes).
- **Business Unit multi-select** (`buBus`/`buBuSel`/`buBuAdd`, v1.34.0) — every budget line takes its PROJECT's Business Unit (projects master; real names since the 2026-07-18 extract fix); picks become chips on the shared mchips bar, sent as `bu=` pipe-delimited EXACT any-of on `/butil`, `/butil/lines`, `/encumbrances` and all three report bridges (Reset clears). Shared with the Projects Encumbrances page; the Pending Approval page keeps its own snapshot-document BU filter.
- `runButil(offset)` — run the report (`/butil`, server-paginated); `buReset()` (clears filters + all multi-select chips, restores the DCT OPEX default type); `btnSearch`/`btnReset` + Enter-to-search on every filter input; `yearRequired` toast when no year.
- **Multi-select Chapter / Cost centre / Project (v1.23.0)** — every Chapter pick (`buChapterPick`/`buChapterAdd`) and every exact Cost-centre/Project LOV pick (`buCcCommit`/`buProjCommit` on the datalist inputs' change/Enter) becomes a **chip** in a labelled bar under the filter grid (`.mchips-bar`/`.mchip`, × removes via `buChipRemove`); `buChapterSel`/`buCcSel`/`buProjSel` observableArrays. Requests send the chips **pipe-delimited** (`buChapterParam()`/`buCcParam()`/`buProjParam()` — a pipe list = exact any-of match server-side; a single free-typed pipe-less value keeps the original contains-match). Flows into `/butil`, the pager, CSV export, both drill modes AND the Briefing Book.
- **Period-aware Budget — Annual + YTD columns (v1.37.0)** — `ATD_PROJECTS_BUDGET` carries `ACCOUNTING_PERIOD` (MM-YYYY), so the view's `budget` = **YTD** (periods ≤ the selected Accounting Period; = annual on Full year) and new `budget_annual` = all periods. Results table shows BOTH columns, each drillable (`openBuDrill(d,'budgetannual'|'budget')` → drawer of the line's period records: period/amount/updated by/on); Budget KPI tile = duo tile (hero = YTD; rows **Annual Budget** / **YTD Budget** with YTD-of-annual % via `buYtdOfAnnual()`, aggregate drills `openBuAgg('budgetannual'|'budget')`; seg bar = YTD share of annual); CSV export gains Annual Budget. Fund Available and Utilization % are computed on the YTD budget. Server: `/butil` emits `budgetAnnual` (rows+totals); `/butil/lines` metrics `budget`/`budgetannual`.
- **Calculation Logic region (v1.36.0)** — fourth collapsible `.bu-sec` at the BOTTOM of the page (`buSecCalcOpen`, **collapsed by default**, state in `localStorage('gl_bu_ui').calc`, toggled via `toggleBuSec('calc')`): explains how every figure is computed from the search criteria — colour-coded formula pills (`.calc-formula`/`.cf-*`, palette matches the KPI band: Fund = Budget − (Actual AP + Actual GRN + Commitment PR + Obligation PO)), a 3-column per-figure table (what it counts / Accounting-Period YTD effect — annual budget NOT period-filtered; AP by invoice accounting date, GRN by accounted-else-transaction date, PR/PO by budget date, PO GRN-netted) and Budget-Year / YTD / Utilization-% notes. EN/AR, static content (no fetch).
- **"Figures in" display-unit selector (v1.24.0)** — Search-region field (`buUnit`: Auto B/M/K | Billions | Millions | Thousands | Exact number, i18n `fUnitL`/`unit*`) driving the unit-aware formatter `buNum(n)` used by the 4 KPI tiles, the duo breakdown rows, the collapsed-band summary (`buKpiSummary`) and all 6 results-table money columns. Display-only (instant re-render, no re-query); persisted in `localStorage('gl_bu_ui')`; exact tooltips + raw CSV unchanged.
- **Plan Insights (v1.88.0, GL/db/32 — mockup combo D1 + D2/D3-optional + K1 + K2)** — two server-computed indicators on every row: **vs Plan** (hand 👍/👎/✋ + execution % = Total Actual ÷ EFFECTIVE YTD plan, revised-when-exists; states WITHIN/BELOW/AHEAD/NOPLAN) and **Plan Coverage** (pill, annual plan ÷ annual budget; FULL/UNDER/OVER/NONE). Always-on D1 columns (`piHand/piCls/piVsTxt/piCovTxt/piRowTip`; vs-Plan cell drills to the plan month rows); Search LOV **"Show Plan"** (`buPlanMode`, v1.88.1: **Yes** default = plan cols + D1 indicators / **No** = no plan columns at all / **Plan with insights** = + D2/D3; `buPlanOn`/`buPlanIns` computeds gate the columns, frontend-only) — INSIGHTS reveals the **D2 micro-chart** column (`piFillW/piTickW/piCovW` — track = plan YTD, fill = actual, tick = YTD budget, plum coverage bar) + **D3 composite** stack (Plan/Actual/Δ + coverage). **K1**: plum **Plan Performance** tile (hero `piExecPct()` + hand; Within/Below/Ahead rows call `setPlanState(s)` → re-runs with `planstate=` filtering totals AND items; Plan-YTD row keeps `openBuPlanAgg`) + steel-blue **Plan Coverage** tile (hero `piCovPct()`; Plan Annual drill + plan-gap row) — `bu-kpis7` band. **K2**: amber execution + green coverage insight strips (`piExecBandOn/Msg`, `piCovBandOn/Msg`) with View-lines CTAs; hidden while a verdict filter is applied — the Results-header **chip** (`.pi-chip`, click = `clearPlanState()`) takes over; `buReset()` clears both. Thresholds from the response echo `planThresholds` (GL module settings PLAN_EXEC_TOL_LOW/HIGH + PLAN_COV_LOW/HIGH, seeded 90/110/95/105); CSV gains Effective Plan YTD / Plan Utilization % / Plan Variance / Plan Status / Plan Coverage % / Coverage Status. Smokes `tests/plan_insights_api_smoke.py` 20/20 + `tests/plan_insights_browser_smoke.py` 23/23 EN+AR.
- **vs Budget verdict column (v1.96.0, GL/db/35 = the live GET /gl/butil owner, CLOB-pieces define)** — always-on column after YTD Budget on ALL THREE results tabs (independent of the Show Plan LOV): hand 👍/✋/👎 + utilization % = displayed Total Actual ÷ the ADJUSTED **ANNUAL** budget (user rule 2026-08-31: Annual always, never YTD); states OK / NEAR / OVER / NOBUDGET on GL module settings `BUD_UTIL_NEAR_PCT` (90) / `BUD_UTIL_OVER_PCT` (100), echoed per response as `budgetThresholds` (`buBudThr`; helpers `bdHand/bdCls/bdLbl/bdTxt/bdRowTip/bdHdrTip`); cell click = the annual-budget drill. Same fields per group on `/butil/agg` (GL/db/33 re-run) and on the Excel register sheet 1 (`budget_utilization_pct`/`budget_variance`/`budget_status`, reporting/db/25).
- **Manage Columns (v1.96.0)** — Results-header button (beside maximize) → `.dw-cols` drawer: both results tables are COLUMN-REGISTRY driven (`BU_LINE_COLS`/`BU_AGG_COLS` + `buc-*`/`bua-*` cell templates; `buColsLine`/`buColsAgg` computeds render the ACTIVE view, feature gates still apply); checkbox show/hide + ↑↓ reorder (`colMove`) + Select/Clear all; **named views per level** (`colSaveView`/`colSetDefault`/`colDeleteView`/`colReset`, `colApply` applies without saving) persisted to `/dct/prefs` key `gl.butil.colviews` (server wins; `localStorage('gl_bu_colviews')` mirror). The active view drives the CSV (`c.csv` expansions + audit tail) AND the Excel Register sheet 1 (`buSheetCols()` → body `sheetcols` → runner `_apply_sheet_cols`; null while the standard layout is untouched). Smokes `tests/vsbudget_cols_api_smoke.py` 13/13 + `tests/cols_vsbudget_browser_smoke.py` 27/27 EN+AR.
- loading states (v1.15.0): while `/butil` runs the results tbody shows **8 shimmer skeleton rows** (`skRows`/`skCols`, `.sk-row`/`.sk`), an indeterminate `.ldbar` sweep under the Results header, and the Search button goes `.busy`; every other load slot app-wide (Actuals/Dashboard/Classifications/Explorer/drill drawers/modal) uses the **branded `.ldr` loader** (GL diamond in twin counter-rotating arcs + i18n `loadingData` text).
- totals **answer band** (**4 tiles since v1.23.0** — `.bu-kpis`/`.bk-*`, scoped to this view) via `buTot(k)`: **Budget** | **Total Actual** (`buActualTot` = actualAp + actualGrn) | **Total Encumbrance** (`buEncumbTot` = commitmentPr [open PR] + obligationPo [open PO, GRN-netted]) | **Fund Available** (= Budget − AP − GRN − Open PR − Open PO). The two composite tiles (`.bk-duo`) show a **stacked composition bar** (`.bk-seg`, `buSegW(part,total)`) + a `buTotPctTxt` %-of-budget line + two **drillable breakdown rows** (`.bk-duo-row`: AP/GRN and PR/PO with colour dots and per-component amounts + a bracketed **%-of-budget suffix** per row via `buPartPct(v)` [v1.23.1] — click drills that component via `openBuAgg`). Each tile = per-measure gradient tint + accent spine + icon chip + Fraunces value + **%-of-budget context line** + **mini utilization bar** (`buPct(k)`/`buBarW(k)`/`buPctTxt(k)`; Budget tile shows the filtered row count, Fund tile uses `buFundSub` — "% of budget remaining", or red `.neg` "over budget" when negative). The 4 measure tiles (`.bk-click`) drill to all supporting lines; responsive 6→3→2→1 columns.
- result table (17 cols): Type / Sector / Department / Cost centre / Project (number + name) / Task / GL Account / Appropriation / Chapter / Program / Expenditure type + the 6 measures. Attributes resolve task-first (task `COST_CENTER`→SECTOR map, task `APPROPRIATION`→CHAPTER map, task `PROGRAM`), then transactions, then project.
- `buExportCsv()` — CSV of the full filtered set (≤5000 rows; **no Project Type column since v1.74.0** — mirrors the results table); **since v1.89.0 exports the ACTIVE level tab** (Department/Sector = client-side via `buAggExportCsv()`, filenames `…_departments_/…_sectors_<year>.csv`); `buRange` — “a–b of total”.
- **Results-region level tabs (v1.89.0, layout B)** — pill segmented switcher with icons in a toolbar under the Results header (`.bu-lvl-bar`/`.bu-lvl-seg`/`.bu-lvl-tab`): **Budget Line** (the untouched paged table) · **Department** (cost-centre grain) · **Sector**, plus a live **summary strip of six icon KPI chips** (`.bu-lvl-sum`/`.bu-lvl-kv`, `buLvlSum` computed: count · Budget · Plan YTD · Actual · **Encumbrance = Commitment PR + Obligation PO** · Fund per level; tinted icon discs matching the Overview band, formula tooltips, Fund chip red when negative — v1.91.0). `buLevel`/`setBuLevel(lvl)`; Department/Sector load `GET /butil/agg?level=` via `runBuAgg(level)` (param-keyed cache, re-fetched automatically after a new Search from `runButil`) into `buAggItems`/`buAggTotals`/`buAggLoading`. Aggregated register = same figure columns + Lines count + vs Plan/Coverage verdict pills (reuses `piHand/piCls/piVsTxt/piCovCls/piCovTxt`), amber **Plan only** rows (`planOnly='Y'`, `.tr-planonly`/`.chip-po`) for groups whose only content is uploaded plan with no budget line, `planExtra` fold-in note (`buAggExtra`/`lvExtraNote`) and a **Comments column showing the recorded comment text of the department/sector at the selected Accounting Period** (v1.90.0: `commentsText` per group + 💬 opening the **COST_CENTER/SECTOR** comment levels via `openCmtGroup(row)`; closing the drawer on an agg tab re-fetches — `buAggInvalidate`); `buAggRevOn`/`buAggAnyPlanOnly`/`buAnyAggAdjNote` computeds. **Every column header on the line AND agg tables carries a formula-first hint** (`ch*` keys, v1.90.0; vs Plan/Coverage/Procash hint keys rewritten formula-first). **Every figure cell drills (v1.92.0)** — `openBuAggDrill(row, metric)` (budgetannual/budget/ap/grn/pr/po) + `openBuAggPlan(row, type, annual)` (plan cells + the vs Plan cell) open the shared drill drawer in **aggregate mode scoped to the group** (`buAggGrpScope`: page filters with `costcenter=`group CC / `sector=`group sector; costadj rows appended per `CADJ_DRILL_METRICS` when the group `hasAdj`); plan drills send **`extra=Y`** so plan rows with NO budget line (folded into the group by db/33) appear and the drawer total ties even where `planExtra` > 0. **Missing-classification guard (v1.93.0 — cost centre is MANDATORY, user rule)**: a keyless group (`buAggCanDrill(row)` false) fires the existing missing-CC warning band (`buMissBandOn` = `/butil` `missingCc` OR `buAggMissRow`, message from whichever tripped it), renders red (`.tr-noclass`) and every cell of it opens the missing-lines list (`openBuMissCc`). Smokes `tests/butil_agg_api_smoke.py` 67/67 + `tests/butil_tabs_browser_smoke.py` 54/54 EN+AR.
- **Missing-Cost-Centre red alert band (v1.43.0)** — `.bu-alert` under the page head, shown ONLY when the current filtered set has budget lines (annual budget ≠ 0) with **no cost centre** (`buMissCc`/`buMissCcBudget` from `/butil`'s `missingCc`/`missingCcBudget` aggregates, recomputed on every search); message = count + annual-budget sum + the fix (maintain the Cost Center task attribute in Fusion); the whole band click-drills via `openBuMissCc()` → shared drawer over `/butil?nocc=Y` (same filters, ≤5000 rows: Project/Name/Task/Etype/Department/Appropriation/Annual Budget; total reconciles to `missingCcBudget`, CSV export included). Browser smoke `tests/butil_misscc_browser.py` 14/14 EN+AR.
- **Over-budget warning band + red row flags (v1.74.0, 2026-08-22; drill drawer added v1.85.0, 2026-08-24)** — `.bu-alert--negfund` directly above the Overview region, shown when the current filtered set has lines with **negative Fund Available** (`buNegFund`/`buNegFundTotal` from `/butil`'s `negFund`/`negFundTotal` server aggregates — full set, not page rows; with procash=Y the effective fund − procash is tested); message = line count + total over-spend + guidance, EN+AR. Each negative Fund Available cell renders bold red on a soft red tint with a triangle warning flag (`.nf-flag`) and an explanatory tooltip. The results-table **Type column was removed the same round** (Project Type remains a Search criterion + applied-filter chip; `skCols` 19). **The band is clickable (CTA "View lines") since v1.85.0** — `openBuNegFund()` opens the shared `.dw-*` drill drawer over `/butil?negfund=Y` (same filters incl. procash=/costadj=, ≤5000 rows: Project/Name/Task/Etype/Department/YTD Budget/Actual AP/Actual GRN/Commitment PR/Obligation PO/Fund Available; total reconciles to `negFundTotal`, CSV export included) — same pattern as `openBuMissCc()`. Smoke `tests/negfund_browser_smoke.py` 16/16 EN+AR (v1.74.0 baseline) + `tests/negfund_drill_browser_smoke.py` 15/15 EN+AR (v1.85.0 drill).
- drill-down (two entry points, one **right-edge slide-in drawer** — `.dw-*`, mirrors the platform edit-drawer; brand-themed header + eyebrow + context; `closeDrawer()`):
  - `openBuDrill(row, metric)` — the four money **table cells** (`.money-cell`) drill that single budget line (`/butil/lines?year=&project=&task=&etype=&metric=`); eyebrow = project·name, context = task·expenditure type.
  - `openBuAgg(metric)` — the four **KPI cards** (`.kstat-click`: Actual AP / Actual GRN / Commitment PR / Obligation PO) drill **all** supporting lines across the current filter set (`/butil/lines?year=&metric=&projecttype=&sector=&search=&costcenter=&fproject=&ftask=&fetype=` — the LOV filters travel f-prefixed so they don't collide with the row-drill key params); eyebrow = "All budget lines · <year>", context = active filters, adds Project/Task columns, and a `drillCapNote` "showing top N of M" when capped at 1000.
  - metric ∈ ap|grn|pr|po; totals always reconcile: **ap**→AP invoices (invoice #, date, vendor, currency, invoice amount, distribution AED, validation, payment, description), **grn**→GRN receipts (receipt #, date, currency, rate, amount AED; rows sorted by GRN # then line — cap selection stays top-by-amount; **v1.46.0 register parity**: + Project name/Expenditure type (aggregate mode) and per-PO-distribution Invoiced (AED) / Uninvoiced (AED) / **Related Invoices** comma-separated list — same columns as the BUDGET_UTIL_REGISTER GRN sheet; Amount (AED) stays last so the total footer reconciles; **v1.47.0**: every related-invoice number is its own Fusion deep-link via `relatedInvLinks(row)` + the hidden `relatedInvPairs` number~id field), **pr**→open-commitment PR lines (PR #, description, budget date, currency, line amount AED), **po**→open-obligation PO lines GRN-netted (PO #, line, budget date, vendor, funds status, PO amount, line amount AED, Open AED). Reuses the shared drill observables (`drillCols`/`drillRows`/`drillTotalV`/`drillCount`/`cell`).
  - `drillExportCsv()` (v1.11.0) — **Export CSV** button in the drawer header (and the Actuals drill-modal header): serializes the loaded `drillCols`/`drillRows` (server-driven columns, raw values, quote-escaped) + a Total reconciliation footer row; UTF-8 BOM for Excel-safe Arabic; filename `gl_drill_<title>_<sub>.csv`; hidden while loading / when no lines.
  - `drillLink(row, col)` (v1.20.0) — **Fusion deep-links** in the drawer: Invoice/PO/PR numbers render as new-tab anchors (`.fus-link`, ↗) via the SHARED `shared/js/fusionLinks.js` (UMD → `window.FusionLinks`; loaded by index.html before app.js) when the row carries the FUSION internal id — `invoiceId` (ap), `poHeaderId` (grn/po), `prHeaderId` (pr) from `/butil/lines`; plain text otherwise. CSV export keeps numbers only.
- **Generate Report dropdown (v1.32.1)** — the Excel + Briefing Book page-head buttons are consolidated into ONE **"Generate Report ▾"** split button (`genOpen`/`toggleGen`/`closeGen`, `.gen`/`.gen-menu`/`.gen-item` in app.css; click-outside `.gen-back` closes) offering **Briefing Book (PDF)** → `runBuBook`, **Sector Performance Report (PDF)** → `runBuSpb`, **Excel Register (XLSX)** → `runBuXlsx`, **FBP - Projects Budget Utilization (XLSX)** → `runBuFbp` (v1.120.0), **MSS - Projects Budget Utilization (XLSX)** → `runBuMss` (v1.122.0), **PowerPoint (PPTX)** → `runBuPpt`. The button shows a "Generating…" busy label while any of the six runs (`buGenBusy` computed). **Export CSV** (client-side grid export) / Rebuild views / Refresh actuals stay separate.
- **"The Binder" generation popup (v1.80.0, user-picked from 3 motion studies)** — while ANY of the three report runs, a centred popup over the dimmed page (`.rg`/`.rg-card`, Data Conveyor pattern) animates the briefing book assembling from its 7 REAL sections (Cover → Overview → AP register → GRN register → Open obligations → Open commitments → Insights: chips light gold in turn, a sheet flies into the green book, the book pulses). Headline names the running format (`rgLabel` ← `rgGenBook/Xlsx/Ppt`), the **elapsed clock is real** (`rgElapsed`, 1s interval) beside the true poll cadence (`rgPoll` 6/5/6s); **× hides the popup, the run continues** (`rgHide`/`rgHidden`, reset on the next run); it closes itself on EVERY exit path via a `buGenBusy` subscription (`rgStop`). RTL mirrors the fly + book; `prefers-reduced-motion` freezes the loops. EN+AR `rg.*` keys; smoke `tests/rg_browser_smoke.py` 13/13 incl. a LIVE register run.
- `runBuXlsx()` — **Excel Register** (dropdown, v1.30.0, internal analysis): generates the Reporting-Platform `BUDGET_UTIL_REGISTER` workbook (reporting/db/25 — 6 worksheets: utilization lines, direct AP invoices, GRN receipts, open purchase orders, open requisitions, pending-approval PR/PO queue) with the page's full filter set; `POST /butil/xlsx` → 5-s poll → auto-download `Budget_Utilization_Register_<year>.xlsx`. **Sheet 1 carries the plan-indicator columns since v1.88.0** (Plan Utilization % / Plan Variance / Plan Status ▼●▲ / Plan Coverage % / Coverage Status — thresholds read from the GL module settings; glyphs = `TO_CHAR(NCHR(…))`, never `CHR()`/`UNISTR` in the MULTI JSON) **and the budget-verdict columns since v1.96.0** (Budget Utilization % / Budget Variance / Budget Status ●▲▼ after Ytd Budget); the body also sends `sheetcols` = the Manage-Columns saved view, so sheet 1 renders exactly the chosen columns in order (omitted while the standard layout is untouched).
- `runBuFbp()` — **FBP - Projects Budget Utilization** (dropdown, v1.120.0, entry 4 of 5 after Excel Register): generates the `FBP_BUTIL_REGISTER` workbook (reporting/db/45 — a copy of BUDGET_UTIL_REGISTER whose sheet 1 carries the **FIXED 21-column FBP layout**: Task Name in place of Task Number, the FBP sample's 19 red-marked columns removed; the layout is baked into the definition, so **no `sheetcols` is sent and Manage Columns never affects it**) with the page's full filter set. Flow: `POST /butil/fbpxlsx` → 5-s poll (`GET /butil/fbpxlsx/:id`) → authed download `FBP_Projects_Budget_Utilization_<year>.xlsx`; `buFbpBusy` drives the busy state; Binder label `rgGenFbp`.
- `runBuMss()` — **MSS - Projects Budget Utilization** (dropdown, v1.122.0, entry 5 of 6 after FBP): generates the `MSS_BUTIL_REGISTER` workbook (reporting/db/46 — a copy of BUDGET_UTIL_REGISTER with the **FIXED 26-column MSS sheet 1** [Task Number kept + Task Name added; appropriation/program/YTD-budget/plan columns dropped], a **Requester** column on sheets 2–6 [AP = the matched PO line's requester via the line-grain PO rule, GRN/PO = the PO distribution requestor, PR = the requisition requester, Pending = per document] and a **Task Name** column beside every Task Number on ALL sheets (v1.122.1); layout baked into the definition, **no `sheetcols`**) with the page's full filter set. Flow: `POST /butil/mssxlsx` → 5-s poll → authed download `MSS_Projects_Budget_Utilization_<year>.xlsx`; `buMssBusy`; Binder label `rgGenMss`.
- `runBuSpb()` — **Sector Performance Report** (dropdown, v1.115.0): generates the `SECTOR_PERF_BOOK` PDF — the Briefing Book pack re-covered for distribution (DCT logo top-right + copyright line on every page via the render_pdf.py pdf-header/pdf-footer hook, simplified cover "YTD MM-YYYY", prepared by Financial Planning and Reporting) — for the page's full filter set. Flow: `POST /butil/sectorbook` → 6-s poll (`GET /butil/sectorbook/:id`) → authed download `Sector_Performance_Report_<year>.pdf`; `buSpbBusy` drives the busy state.
- `runBuPpt()` — **PowerPoint** (dropdown, v1.32.1): generates an executive PowerPoint deck of the same data as the Briefing Book — enqueues `BUDGET_UTIL_BOOK` with `formats='PPTX'` (reporting/runner/render_pptx.py: cover + KPI overview + utilization-by-sector chart+table + budget composition + lines under pressure + actuals & supplier concentration + open obligations/commitments + observations & insights + methodology, native editable slides). `POST /butil/ppt` → 6-s poll (`GET /butil/ppt/:id`) → authed download `Budget_Utilization_Briefing_<year>.pptx`; `buPptBusy` drives the busy state.
- `runBuBook()` — **Briefing Book** button (page head; **v1.22.0: sends ALL page filters**; **v1.30.0: the book gained Part 5 "Pending Approvals — PR & PO Queue"** + an Approval-queue insight, Observations renumbered Part 6): generates the Reporting-Platform `BUDGET_UTIL_BOOK` PDF for the page's **full filter set** — Year, Period (YTD), Project type, Sector, Chapter, Cost center, Project, Task, Expenditure type, Search — with the page's exact predicate semantics, so the book scope always matches the page (the cover prints every applied filter). Flow: `POST /butil/book` (GL-gated bridge, `11_gl_butil_book_ords.sql`) → toast "queued — run #N" → 6s polling of `GET /butil/book/:id` (≤ ~6 min) → authed blob download of `GET /butil/book/:id/pdf` as `Budget_Utilization_Briefing_Book_<year>.pdf`; `buBookBusy` drives the "Preparing book…" label. The run is rendered by the normal reporting worker fleet (also runnable from the BI app). **Since v1.88.0 the book carries Part 6 "Expenditure Plan Performance"** (plan-vs-actual KPIs, per-sector execution + coverage bars, sector table, top-15 deviations; sections `plan_sector`/`plan_dev`/`plan_cov` in reporting/db/21; Observations renumbered Part 7).

## Projects Encumbrances (`view()==='encumbrances'`) — open PO/PR encumbrance lines, full GL combination (v1.25.0)
- **"Open Projects Encumbrance Follow-up"** — every OPEN encumbrance line (Open Commitment PR + Open Obligation PO, GRN-netted) for the budget lines matching the Budget Utilization scope, exploded to the FULL GL combination (all ten segments, **code + name**) plus sector/chapter names. Rendered in the **SHARED `<interactive-report>` component** (the same APEX-IR grid AP/BI use): column show/hide/reorder/rename, filter chips, multi-sort, calculated columns, aggregates, control breaks, highlight rules, CSV + SheetJS XLSX export, and per-user localStorage layouts (`reportCode:'GL_PROJ_ENCUMBRANCES'`, `layoutsApi:null`).
- **Search criteria are the SAME as Budget Utilization** — the tab reuses the `bu*` filter observables (`buParams`) and the shared `bu-*` autocomplete datalists verbatim (Year req, Period YTD, Project type, Sector, Chapter[multi], Cost centre[multi], Project[multi], Task, Expenditure type, Search); the Search button runs this tab.
- `runEncumbrances()` — one-shot capped fetch (`GET /encumbrances`, limit 10 000) → builds the IR envelope (`enData`) from the server's `columns[]`+`items[]`. `enCount` / `enOpenTotal` (+ `enOpenTotalTxt`) / `enTruncated` drive the summary line; the **Open total reconciles to the butil Total Encumbrance** (Commitment PR + Obligation PO). Auto-runs on first open (via `go('encumbrances')`), re-runs on Search.
- `enIrError` — set from `window._irLoadError` if the shared component failed to load (index.html requirejs boot); the tab then shows a graceful notice instead of the grid.
- IR enablement: GL (a portal-style app) bootstraps requirejs in `index.html` (shim `knockout`→the existing global, `shared`/`text`/`xlsx` paths), requires `shared/components/interactiveReport` (+`shared/editDrawer`, `shared/i18n`) so the `<interactive-report>`/`<edit-drawer>` components register on the app's KO before `app.js` boots; `toggleLang` also flips `shared/i18n` so the grid re-renders in AR. `platform.css` is linked **before** `app.css` (portal styles still win) to supply the IR base classes; the IR var bridge in `app.css` themes the grid green.

## Encumbrances – Pending Approval (`view()==='pending'`) — PR/PO documents awaiting approval (v1.27.0)
- **"Encumbrances – Pending Approval Follow-up"** — every PR / PO document currently PENDING APPROVAL in Fusion (daily BIP snapshot `atd_pr_po_pending_approval`, otbi-atd/db/47) joined to the open-encumbrance line detail (project / task / expenditure type, budget date, AED amounts, full 10-segment GL combination code+name) PLUS the approval trail (preparer/buyer, submitted date, pending-with approver, days pending, business unit, funds status). Data = `PROD.DCT_PR_PO_PENDING_V` (db/v2/52). **Zero-value lines are excluded (v1.30.1)** — the page follows budget utilization; a zero line never reserves funds (the reserved/not-reserved split stays visible; book + Excel register are additionally reserved-only).
- **Search criteria are the SAME as Budget Utilization** — reuses the `bu*` filter observables + shared `bu-*` datalists verbatim; the Search/Reset buttons run this tab (`runPending()`).
- **Monitoring KPI band** (server-aggregated over the FULL filtered set, correct even when the register is capped): Pending documents (PR/PO split + seg bar) · Pending amount AED (PR/PO split) · **Funds reserved vs not-reserved** (reserved = already inside the butil open-encumbrance figures; not-reserved = would hit Fund Available on approval) · **Approval aging** (avg days, oldest, over-30-days docs+amount, red flavour when any are stalled). **Every tile breakdown row DRILLS (v1.30.0)** — `openPnDrill('pr'|'po'|'reserved'|'unreserved'|'over30'|'within30')` opens the SHARED right-edge drill drawer with the matching pending lines (client-side slice of `pnItems`, cap 1,000 w/ top-N note, reconciling total, CSV export; Document # cells deep-link to Fusion via the generic `drillLink` docNumber rule).
- **Aging analysis + Top pending approvers mini tables** (`pnAging`/`pnApprovers` — buckets 0-7/8-15/16-30/31+ at document grain; top 8 approvers by value held w/ doc count + max days — the follow-up chase list). Rows drill too (`openPnBucket`/`openPnApprover`, v1.30.0); headers are **#79C5AC region-header bands** (user-specified).
- **Busy overlay** (v1.30.0) — the butil oj-progress-circle replica over the KPI band + Results while `/gl/pending` runs (`pnLoading` on the `.bu-body` wrapper).
- **Extract-coverage note** (`pnUnmatchedTxt`) — snapshot documents with no matched project budget line (`in_extract='N'`) are counted, never silently dropped; the Briefing Book annex lists them.
- Register in the **SHARED `<interactive-report>`** (`reportCode:'GL_ENC_PENDING'`, `section:'pend'`, cap 10 000, default sort days-pending desc) with the same GL-combination hover popover as the encumbrances tab; `pnIrError` fallback notice.
- **Source filter** (`pnSource`, v1.28.0) — All / PR / PO dropdown in the Search region (page-local, cleared by Reset; `source=` on `/pending`) + **"Showing figures in"** (the shared `buUnit` selector — KPI tiles and mini-table amounts format via `buNum`).
- **Business Unit multi-select** (`pnBus`/`pnBuSel`/`pnBuAdd`, v1.31.0) — dropdown fed by the `businessUnits[]` LOV in the `/pending` response; each pick becomes a chip on the shared mchips bar (Reset clears). Sends `bu=` pipe-delimited EXACT any-of list, scoping the register, every KPI/aging/approver aggregate AND the unmatched extract-coverage KPI; forwarded to both the Briefing Book and the Excel register. The BIP snapshot is the platform's only cross-BU source (the OTBI extracts are DCT-scoped), which is why the filter lives on this page.
- **Fusion deep-links on Document #** (`pnGridOver`/`pnGridClick`, v1.28.0) — hovering a docNumber cell shows the link affordance (`.pn-doclink` + tooltip), clicking opens the Fusion PR/PO deep link in a new tab via the shared `fusionLinks.js` builders over `fusionHeaderId` (kept in a VM side-map keyed source|doc# — the shared IR grid drops undeclared row fields). Same delegated ko.contextFor pattern as the combination popover; no shared-component change.
- `runPnBook()` — **Briefing Book** button → `POST /pending/book` with the full page filter set, 6-s poll (`GET /pending/book/:id`), auto-download of the PDF (`ENC_PENDING_BOOK`, reporting/db/23 — **funds-reserved non-zero lines only**, registers carry Sector/Cost centre/Appropriation code+name, full untruncated text).
- `runPnXlsx()` — **Export Excel** button (v1.29.0, internal analysis) → `POST /pending/xlsx` with the full page filter set + source, 4-s poll (`GET /pending/xlsx/:id`), auto-download of `Encumbrances_Pending_Approval_Register_<year>.xlsx` (`ENC_PENDING_REGISTER`, reporting/db/24 — sheet 1 = the 29-column flat pending PR/PO register on the book scope rule; sheet 2 = the extract-coverage annex).

## Costing Adjustments (`view()==='costadj'`) — Projects Costing Adjustments (v1.78.0)
- **Register** on the SHARED `<interactive-report>` (`reportCode:'GL_PA_COST_ADJ'`, `section:'cadj'`, cap 2 000, newest first; `stateRev:2`) — column order (user-annotated, v1.79.0): **Status pill** (✎/✓/✕ via `row._rowClass` + `td[data-key=status]` CSS — no shared change) · Project · Task · Etype · signed Adjustment (AED) · signed Budget Override (AED) · **Invoice (Fusion deep-link via the IR `cellLink` hook + `FusionLinks.invoice(invoiceId)` side-map)** · Supplier · Ref `PCA-#####` · Reason · Year (comma-free `text`) · Period · Classification · created/actioned audit. Filters: year (butil `buYears`), status (lookup LOV), free-text search; `runCostAdj()`, `loadCaLookups()`. Row click (`caGridClick`, delegated `ko.contextFor` + `caRowMap` side-map by ref — the IR drops undeclared row fields) opens the drawer.
- **Drawer** (`.dw-ca`, `openCaNew`/`openCaEdit`/`saveCa`) — Source AP invoice distribution region (optional): search `costadj/meta/dists` (≥2 chars, cap 50), result mini-table shows each dist's current coding **or "No project coding"**, Select (`pickCaDist`) links it + snapshots the original coding + defaults the amount; Unlink (`clearCaDist`). Corrected budget line region: year + **MANDATORY accounting period** (12 real months, no Full-year; defaults to the current month / December for prior years, month kept on a year change) + project on the shared butil datalist + **DEPENDENT Task / Expenditure-Type lists** (`ca-task-dl`/`ca-et-dl` ← `costadj/meta/tasks|etypes` on the inputs' change events) + classification (lookup `PA_COST_ADJ_CLASS`) + **signed Amount** + **signed Budget override** + Reason (required) + Comments. DRAFT-only editing (server 400s otherwise); non-DRAFT rows render read-only with a status ribbon (actioned by/at).
- **Approve / Reject / Delete** (`actionCa`/`deleteCa`, manage gate = `GL_MANAGE_COST_ADJ` w/ legacy SYS_ADMIN; client `canManageCadj`) — confirm dialog; approval is manual (user decision). Only APPROVED rows reach the figures.
- **Budget Utilization reflection** — "Include Cost Adjustment" checkbox in the butil Search region (`buCadj`, **checked by default**; `toggleBuCadj` re-runs in place; unticked sends `costadj=N` — the server default is Y). When included, `/butil` rows + totals arrive already adjusted (Actual +Σamount, Budget annual+YTD +Σoverride, Fund +Σ(override−amount)); adjusted lines carry `hasAdj='Y'` → **`(*)` prefixed in the first results-table cell** (`.adj-star`, tooltip) + the `.cadj-note` under the table explaining the marker — both gated on the response echo `buCadjOn` so the page never claims an adjustment the loaded figures don't carry. CSV export appends Cost Adjustment / Budget Override (Adj) / Has Adjustment.

## Comments (`view()==='comments'`) — Budget Utilization Comments register + Sector/CC entry (v1.82.0)
- **Register** on the SHARED `<interactive-report>` (`reportCode:'GL_BUTIL_CMT'`, `section:'bucmt'`, cap 2 000, newest first) — Level · Period · Entity (composed per level) · Comment · Replies · Files · Created by/at · Updated · Ref `BUC-#####` · Year. Filters: year (butil `buYears`), period, level (client-side), free-text search; `runCmtReg()`. Row click (`cmtRegGridClick`, delegated `ko.contextFor` + `cmtRegMap` side-map by ref) re-opens the thread drawer (`openCmtReg`).
- **Sector / Cost-Center entry bar** (`.cmt-new`, visible with `canAdd`) — level select + value on the butil `buSectors`/`buCcs` datalists → `openCmtNew()` opens the thread drawer for that key (those two levels have no grid row of their own).
- **Thread drawer** (`.dw-cmt` — deliberately the LAST drawer in the DOM: z-71 stacking is DOM order and it must open ABOVE the drill drawer) — `openCmt(ctx)` / `loadCmtThread()`: sectioned threads (a BUTIL_LINE thread also shows the row's TASK + PROJECT-level sections), root cards + indented replies with author avatar/initials, timestamps, `· edited` tag, `BUC` ref, per-period 🔒 when that period is CLOSED (`closedPeriods[]` from the server); add-a-comment box (`postCmtRoot`, gated `canAdd`; period select defaults like costadj) + reply box (`startCmtReply`/`postCmtReply`, gated `canReply`) + edit/delete own (`startCmtEdit`/`saveCmtEdit`/`deleteCmt`; SYS_ADMIN override; a root with replies refuses delete) + per-comment **attachments** (`toggleCmtDocs`/`cmtUpload` raw-binary PUT / `cmtDocDownload` authed blob / `cmtDocDelete`); closed-period banner disables the add form. Caps from `loadCmtCaps()` (`/butilcmt/meta/caps`, loaded at boot — also drives the Settings tab visibility).
- **Butil grid integration** — `(**)` star (`.cmt-star`) beside the costadj `(*)` + a **Comments column** (chat button + count badge → `openCmtLine(row)`), the second `.cadj-note` under the table, all gated on the `commentsEnabled` response echo (`buCmtOn`); CSV appends Comments / Has Comments / Comments Text; posting from the butil page auto-refreshes the grid (`cmtAfterWrite`).
- **Drawer search region + photos + sort (v1.83.0 feedback round)** — the thread loads YEAR-WIDE and a **collapsed Search region** (`.cmt-flt`: Posted by + Accounting Period) filters it client-side (`cmtSectionsView`); the period filter **defaults to the dashboard's accounting-period parameter** (`cmtFltPer`), the scope line follows it; roots sort **newest first** (server); the period chip is a labelled amber chip ("Accounting period: MM-YYYY"); avatars show the author's profile photo when present (`authorId`/`hasPhoto` → authed blob fetch of `/dct/users/:id/photo`, cached per user, initials fallback — `cmtPhoto`/`cmtFetchPhotos`).
- **"Display Comments" report LOV (v1.83.0)** — butil Search select `buCmtDisp` (None default / Selected period only / All → `cmtdisp=PERIOD|ALL`); when on, `/butil` rows ship `commentsText` and the results table + CSV gain a clamped Comments column (`buCmtDispMode` echo gate); the Excel register receives `cmtmode` → sheet-1 `comments` column + sheet 7 "Comments - Other Levels" (BUDGET_UTIL_REGISTER, reporting/db/25).
- **Drill-drawer integration** — the AP / PR / PO drills gain a synthetic `_cmt` column (`cmtDecorateDrill`, `drillCmtLevel`); the row 💬 opens the drawer at AP_INVOICE / PR / PO level with `entity_key` = the document number (`openCmtDoc`).

## Comment Roles (`view()==='cmtroles'`) — role → capability grants (v1.82.0, Settings)
- Tab visible only with `GL_MANAGE_CMT_ROLES` (`cmtCaps().canManageRoles`; NAV `hidden` accepts a function since v1.82.0). Table of all active JOB/ABSTRACT roles (name AR-aware, code, active member count) × three capability checkboxes (Add comments / Reply / Close periods) — `loadCmtRoles()`, `cmtRoleToggle(row, cap)` writes the grant to the COMMON `dct_role_permissions` and the server calls `dct_sec.refresh_flat`; granted roles sort first.

## Reporting Periods (`view()==='cmtperiods'`) — close / reopen comment periods (v1.82.0, Settings)
- Tab visible only with `GL_CLOSE_BUTIL_PERIOD` (`cmtCaps().canClosePeriod`). Year select (`cmtPerYear`, butil years) → 12 period rows: status pill (Open/Closed), active comment count, closed by/at, reopened by/at, stored remarks + per-row remarks input (`p.rem`), Close / Reopen buttons with confirm (`cmtPerAction`) — `loadCmtPeriods()`. A CLOSED period 403s every comment write in it server-side. **Rows are normalised in the VM (APEX_JSON omits NULL/empty keys — bare-name foreach bindings would abort).**

## Project Portfolio (`view()==='portfolio'`) — every project in the budget year, ranked and health-scored (v1.70.0)
Four `.bu-sec` regions (state in `localStorage('gl_pf_ui')`), one `.bu-load-ov`/`.ojpc` busy overlay over the answer area. **Shares the Budget Utilization criteria observables** (`buYear`/`buPeriod`/`buType`/`buSector`/`buChapterSel`/`buCcSel`/`buProjSel`/`buBuSel`/`buOvr`/`buSearch`/`buUnit`), which is why drilling into a project needs no parameter passing and why every figure reconciles to the butil page.
- **Search criteria** — the butil filter set plus three page-local ones: `pfStatus` (project status), `pfHealth` (band), `pfManager`, and `pfSort` (health / budget / consumed / committed / funds / utilisation). `togglePfSec('search'|'kpis'|'health')`, `loadPfFilters()`.
- **Executive KPI band** — 7 `.bk` tiles: projects, budget, consumed (AP+GRN duo), committed (PR+PO duo), paid, funds available (`.neg` when negative), needs-attention. Helpers `pfT(key)`, `pfActualTot`, `pfCommitTot`, `pfUtilPct`, `pfPaidPct`, `pfSegW`, `pfOfBudget`, `pfBands`, `pfFlags`, `pfGaps`, `pfRiskCount`.
- **Health and momentum** — `pfGauge` (hand-computed radial SVG: consumed arc + consumed-plus-committed arc, `2*pi*r` dasharray, no chart library), `pfHealthBands` (4-segment distribution bar), `pfPressure` (top-12 largest consumers, click-through to 360), `pfInsights` (auto-generated executive sentences via `{}` substitution).
- **Projects register** — the SHARED `<interactive-report>` (`GL_PROJ_PORTFOLIO` / section `pf`) built by `pfBuildIr()`: frozen Health + Project + Manager + Status, grouped bands (Budget / Consumed / Committed / Position / Revenue and pipeline), ⓘ hints on health, paid, billed and utilisation, `zebra`, `stateRev:1`, and `_rowClass` health tints. Money columns are scaled by the shared `buUnit` divisor with a `(B/M/K)` label suffix; `pfUnitNote()` says so. Rebuilt (not re-fetched) when `buUnit` or `lang` changes.
- **Row click → Project 360** — `pfGridClick`/`pfGridOver` are delegated handlers using `ko.contextFor(td)`; the project number is recovered from the merged identity cell, because the interactive report drops undeclared row fields.
- `runPortfolio()`, `pfParams()`, `togglePfMax()` (Esc restores, guarded while the drill drawer is open).

## Project 360 (`view()==='proj360'`) — one project, end to end (v1.70.0)
Reached from a portfolio row (`openProj360(num)`) or the nav tab, which is **hidden until a project is open** (`hidden:true` in `NAV_GROUPS`, filtered by `activeGroupItems`). `backToPortfolio()`. `loadProj360()` fires the header and all five child requests in parallel.
- **Identity card** — manager, type, business unit, appropriation, chapter, sectors, departments, cost centres, start/finish, tasks, last activity. Every field binds `$data.…` because APEX_JSON omits null keys. Shows a banner when the project has no budget line in the year.
- **KPI band + health** — the same measure vocabulary as the portfolio so a project's tiles equal its portfolio row, plus `p3Components` (per-component score bars) and the advisory-score disclaimer.
- **Budget → payment funnel** — `p3Funnel` computes six proportional SVG bars (Budget → PR → PO → GRN → Invoiced → Paid) with connector bands and stage-to-stage conversion %. Each stage calls `p3StageDrill(stage)`, which opens the SHARED drill drawer through the EXISTING `/butil/lines` endpoint, so a stage total and its document list can never disagree. The Paid stage has no line endpoint and is not clickable.
- **Tasks and schedule** — planned start/finish and a plan-date pill (`p3SchedClass`/`p3SchedLabel`). **Actual dates are not shown**: they are empty in the source (0 of 6,059 tasks), so slippage is not computable and is not scored; the region says so.
- **Documents and pipeline** — `p3PipeTabs`/`p3PipeRows` over open PR, open PO, received-not-invoiced and pending approval.
- **Revenue (AR)** — billed revenue with a visible `DATA GAP` badge and an amber note: no AR receipts extract exists, so collections/DSO are deliberately absent rather than estimated. Only 51 of 991 projects carry any AR line, so the empty state is the common case.
- **Budget transactions** and **Cashflow plan** — read-only registers; cashflow shows an explicit empty state pointing at the Cashflow tab (the Fusion budget is not cashflow-phased and cannot substitute).
- Status pill tone: `p3StTone`/`p3StClass` — failure patterns are tested FIRST and the mute tier before the ok tier, because "Inactive" contains "active".

## Dashboard (`view()==='dashboard'`) — executive analytics
- `dashPeriod` selector → `loadDashboard()` (`/dashboard`); KPI strip via `kpis()` (budget, actual, funds, encumbrance, PO total & count, utilisation/commitment %).
- `gauge()` — utilisation radial gauge (SVG); `trend()` — period-over-period actual SVG area+line; `sectorBars`/`programBars`/`apprBars` — horizontal bar charts (actual / PO commitments); `insights()` — auto-generated executive sentences. All charts are hand-built SVG/CSS (no chart library).

## Refresh (Overview + Actuals + Dashboard) & Rebuild (Overview + Budget Utilization)
- `refreshActuals()` — `POST /actuals/refresh` safely rebuilds `DCT_GL_COA_SNAP` through validated staging so the report reflects the latest GL/ATD data + mapping edits; concurrent runs are refused, the prior committed snapshot survives any load/validation failure, and each run records counts/duration/error in `DCT_ACTUALS_REFRESH_LOG` (90-day retention). Also runs hourly (`DCT_ACTUALS_REFRESH_JOB`, db/v2/35 + resilient procedure db/v2/118) and is mirrored as a button in the ATD app (`POST /atd/actuals/refresh`).
- `rebuildViews()` — **Rebuild views** button (Overview + Budget Utilization page heads, tooltip hint + confirm): `POST /actuals/rebuild` → `prod.dct_views_rebuild` (db/v2/38). For **structural** reloads (new/renamed columns): re-creates the 16 `SELECT *` base pass-through views over the `ATD_*` tables, recompiles INVALIDs, refreshes the snapshot; toast reports any views still invalid (those need a script edit). For a plain data reload, Refresh actuals is enough.

## Shell
- `t()` / `toggleLang()` (EN/AR + RTL), `go(view)`, `signOut()` (→ Admin), `refreshFilters()`.
- formatters: `money(n)` (full AED with separators), `compact(n)` (1.71B / 868.0M / 12K), `fmt(n)`.

## Reconciliation (`view()==='recon'`) — Actuals ↔ Budget Utilization data-integrity (v1.41.0)
- **Purpose** — verifies data integrity between the Actuals and Budget Utilization dashboards. Consumption (AP/GRN/PR/PO) is the SAME source both sides, so it reconciles to residual 0 via named leakage buckets; budget = two different Fusion ledgers (GL `GL_BALANCES` Expense scoped vs PPM `PROJECTS_BUDGET`) shown side by side. Data = `DCT_GL_RECON_FACT_V` + `DCT_GL_RECON_GLBUDGET_V` (db/v2/105).
- **Filters** — Budget Year (req) + Accounting Period (YTD, sets `BUTIL_END` so both sides align) + multi-select Sector / Chapter / GL Account / Cost Centre / Appropriation / DCT Program (`rcSector/rcChap/rcAcct/rcCc/rcAppr/rcProg`, each a `rcMulti()` picklist→chips) + **GL budget chapters** (`rcBChap`, Expense scope, default CH2–CH5). `runRecon()` fetches summary + rows; `rcReset()`.
- **Status band** — 5 KPI tiles: Coverage % (butil/actuals) + total consumption both sides · **Non-project (Actuals only)** = spend on combinations with no project/task/etype (can't enter Budget Utilization) · **Orphan consumption** = spend on a project line with no budget (the actionable flag) · GL vs Project budget · Fund available each side. Charts (hand-built SVG/CSS): `rcDonut` composition donut (Matched / No-project / Not-validated / Orphan via conic-gradient) + `rcMeasures` per-measure Actuals-vs-Butil bars with status dots. Every tile figure has an ⓘ info hint (`hRc*` keys) and is a **drill link** (v1.42.4).
- **KPI drill-down (v1.42.4)** — every KPI tile figure opens the SHARED drill drawer with the full supporting detail:
  - Coverage / Non-project / Orphan → `rcKpiDrill(bucket,titleKey)` calls `/recon/drill?measure=all` (cross-measure AP+GRN+PR+PO source documents, with a **Source** column; `bucket`=matched/no_project/no_budget_line). Reconciles to the tile total to the riyal.
  - GL / Project **Budget** and GL / Project **Fund** sub-values → `rcBudgetDrill(side,focus,titleKey)` calls `/recon/budget?side=gl|ppm` (budget-line detail = Budget · Consumed · Fund per project-line [ppm] or per GL combination [gl]; `focus`=budget|fund picks the reconciling drawer total). GL side FULL-OUTER-JOINs consumption so un-budgeted-combination spend surfaces as negative-fund rows and GL Fund reconciles to the tile.
- **Register** — measure tabs (`rcSetMeasure` AP/GRN/PR/PO) + grain segmented control (`rcSetGrain`, 8 grains); table shows per-row Actuals/Butil/Diff + Non-project/Validation(AP)/No-budget-line, every leakage cell a drill link. Column headers carry ⓘ hints. `rcExportCsv()`.
- **Difference drill** — `rcCellDrill(row,bucket)` → `rcDrill(measure,bucket,row)` calls `/recon/drill` and opens the SHARED drill drawer (`drillCols/drillRows/drillTotalV/drillCount`) with the source documents (doc #/line, project/task/etype, amount, hasProject/validation/onBudgetLine). Verified: every drill total reconciles to its summary bucket.

## Legacy (EBS) (`view()==='legacy'`) — Fusion/EBS COA mapping + historical balances (v1.48.0)
- `runEbsMap()` — loads the COA mapping register (GET /coamap; segment/status/search filters) into the SHARED `<interactive-report>` (client-built columns)
- `xmGridClick(d,e)` — delegated row click → edit drawer (admins; `ko.contextFor(td)` + segment|ebsValue side-map)
- `openXmNew()` / `openXmEdit(row)` / `closeXmDrawer()` / `saveXm()` — add/edit drawer (POST /coamap, partial PUT /coamap/:id; deactivate instead of delete)
- `uploadEbs()` / `ebFileChosen(d,e)` — EBS balance Excel upload: SheetJS client parse (synonym-priority header map w/ column claiming — accepts the full EBS "GL Period Balances" export layout incl. CC_ID/descriptions/Account Type; bare `Budget` = amount when `Budget Group Code` present), chunks of 500 → POST /ebs-balances with per-chunk progress
- `ebTemplate()` — downloads the EBS balances upload template workbook (SheetJS)
- `loadEbsSummary()` — per-year coverage tiles + top-unmapped lists (GET /ebs-balances/summary)

### Balances YoY (`#pg-yoy`, 2026-08-02; interactive report since v1.55.0)
- `refreshProjectsData()` — butil page "Refresh source data": queues ALL 15 full extracts behind the page via POST /butil/refreshdata (PROJECTS_DATA set + AP Invoices/Lines/Distributions + PO Headers/Lines/Schedules/Distributions + PR Headers/Lines/Distributions + GRN Temporary Job/GRN Gap — v1.75.0, was PROJECTS_DATA only in v1.62.0), polls to completion (~15 min ceiling), re-runs the search; **v1.76.0/v1.77.0: while running, the `.cvb` Data Conveyor POPUP** (centred fixed overlay; x hides it while the run continues — `cvbHidden`) (belt animation Fusion→i-Finance + 15-segment bar + `cvbCount`/`cvbJob` captions from `pdataDone`/`pdataJob`, fed by the poll via `pdataProgress()`; RTL-mirrored, reduced-motion aware); **v1.77.0: the butil header's 3 refresh buttons = ONE `Refresh data ▾` dropdown** (`rdOpen`/`toggleRd`/`closeRd`/`rdBtnLabel`; Rebuild views SYS_ADMIN-only; `rdLast` = latest lastFinished fetched on open)
- `runYoy()` — loads the year-over-year comparison (GET /ebs-balances/yoy) for the selected year chips (max 6) + Balance-as-of month
- `yoToggleYear(y)` / `yoMonthLabel(o)` — criteria helpers (year chips, Full year / month YTD labels)
- `yoView` / `yoPivot` — client-side pivot (one row per Fusion account, values per year) + live search/type filtering
- `yoIr` — SHARED `<interactive-report>` envelope (`GL_BAL_YOY`): year columns ASCENDING, dynamic "Change YY-YY" / "Change % YY-YY" headers for the two latest selected years, per-column ⓘ hints, trailing **Chart** column (shared IR `spark` type — inline sparkline, hover = cross-year trend chart popover); rebuilds on run/measure/search/type; **v1.56.0 layout round**: alternating per-year column tints (`yoc-a`/`yoc-b`), Change/Change % on a gold tint (`yoc-chg`) with ▲ green / ▼ red delta arrows (shared IR `column.delta`), zebra rows (envelope `zebra:true`)
- `yoMeasure` + `yoVal/yoValTxt/yoChange/yoChangeTxt/yoChangePct/yoTotal` — measure toggle (Actual/Budget/Encumbrance) + delta vs previous year + totals
- `yoCsv()` — CSV export of the visible pivot (ascending years, selected measure, totals row)
- `runYoyXlsx()` — Export Excel Register: enqueue EBS_GL_YOY_REGISTER via POST /ebs-balances/yoy/xlsx, poll, auto-download
- `runEbsRegister()` — enqueues EBS_GL_BALANCE_REGISTER via the bridge, polls, auto-downloads the XLSX

## Cashflow (`view()==='cashflow'`) — budget cashflow plan uploads (v1.49.0)
- `loadCfSummary()` — GL + Projects per-year/type coverage tables + top unmapped appropriations (GET /cashflow/summary)
- `uploadCfGl()` / `cfGlChosen(d,e)` — GL cashflow Excel upload (10 segments + period + CF type + amount): SheetJS client parse (fuzzy header map), chunks of 500 → POST /cashflow with per-chunk progress
- `uploadCfPj()` / `cfPjChosen(d,e)` — Projects cashflow Excel upload (project/task/etype grain) → POST /cashflow/projects. Reads **two shapes**: the classic LONG sheet (PERIOD + AMOUNT columns) and the **WIDE template** (one amount column per accounting period, detected by `cfWideCols()` on the header — `01-2026` / `Jan-2026`); blank month cells are SKIPPED (an untouched template row never posts a zero), a typed 0 posts and clears a saved amount, CF_TYPE defaults to APPROVED
- `loadCfTplYears()` — budget-year LOV for the template picker (GET /cashflow/projects/template?meta=Y); the year is re-asserted after the options land (a KO `<select>` bound with an empty option list blanks its value)
- `cfPjTemplate()` / `cfPjBuildTemplate(d, year)` — **pre-filled Projects-cashflow template (v1.71.0)**: pulls every budget line of the picked year (GET /cashflow/projects/template?year=) and writes a WIDE workbook — PROJECT · PROJECT_NAME · TASK · EXPENDITURE_TYPE · CF_TYPE, one column per accounting period, then reference columns GL_COMBINATION (full 10-segment canonical) · SECTOR · DEPARTMENT · COST_CENTER · GL_ACCOUNT · APPROPRIATION · CHAPTER · PROGRAM · BUSINESS_UNIT · PROJECT_TYPE · ANNUAL_BUDGET, plus a "How to use" sheet. Amounts already saved for that year are pre-filled (APPROVED row per line + a REVISED row only where revised data exists), so the download is a round-trip editor
- `cfGlTemplate()` / `cfPjTemplate()` — download the upload template workbooks (SheetJS)

## Budget Transactions (`view()==='budgettrx'`) — project budget transactions, master-detail (v1.63.0)
Mirrors the source Project Budget Transactions VBCS screen. Read-only over the ATD PBT extract
(`PA_BUDGET_TRX_*`, `otbi-atd/db/77`) — it shows what the last sync pulled, not live Fusion.
- `loadBtFilters()` — criteria LOVs: budget types, business units, project types, statuses, years, approvers + (v1.65.0) sectors, chapters, programs, appropriations, accounting periods (GET /budgettrx/filters); fires `loadBtLov()` **without awaiting it**
- `loadBtLov()` (v1.65.0) — the four big type-ahead lists for the `<datalist>` autocompletes (GET /budgettrx/lov?type=&year=, **scoped to the mandatory Budget Type + Year**). Loads in PARALLEL with the grid — awaiting it raced the first page-open and rendered an empty grid; a failure is swallowed, the criteria are free-text inputs that just lose their suggestions
- `btApplyDefaults(d, force)` (v1.68.0) — the page's opening scope, `BT_DEFAULTS`: **Additional Fund · current year · Department of Culture and Tourism · DCT OPEX Project Type**. Each applied only when the live LOV offers it; `force` (used by Clear) resets all four
- `btRescope()` (v1.66.0, **debounced 60ms** v1.68.0) — Budget Type / Year / Business Unit / Project Type subscriptions land here: the scope changed, so the criteria LOVs and the type-ahead lists are re-fetched for it. Keyed, so it never re-fetches the same scope twice; the debounce coalesces the four subscription firings that applying the defaults produces (a plain flag missed the ones KO's `<select>` write-back defers a tick)
- `btLineFilterCount` — how many of the nine LINE-level criteria are active (drives the chip beside Search)
- `runBudgetTrx(page)` — master grid for all 10 criteria (GET /budgettrx); clears the selection because the child regions belong to the previous row
- `btSearch()` / `btClearCriteria()` / `btPrevPage()` / `btNextPage()` — criteria actions and paging (`btPage`/`btPages`/`btTotal`)
- `btSelectRow(row)` / `btIsSelected(row)` — row click loads the header + its lines + its approval trail (GET /budgettrx/:num?type=) and highlights the row
- `btLineCols` / `btCell(row,col)` — the details table is metadata-driven (`BT_LINE_COLS`): the line shape differs per budget type (34 / 21 / 38 source fields); a column's 4th flag marks it a **status** column
- `btTone(value)` / `btStClass(value)` / `btCellClass(row,col)` / `btCountClass(n)` (v1.64.0) — status-pill tone system: header status, line/baseline/journal status and approval state render as tinted pills with an icon disc (ok ✓ / err ✕ / warn ! / info • / mute –). **Tones are matched case-insensitively on keywords, and the failure patterns are tested FIRST** — the source vocabulary is inconsistent (`SUCCESS`/`Success`, `PASS`/`Pass`, `Draft`/`DRAFT`) and "Baselining Failed" also contains "baselin", so equality- or prefix-matching would paint a failed transaction green. `btCellClass` also reddens negative figures; `btCountClass` dims a zero line/approval count
- `btExportCsv()` — the master grid as CSV (UTF-8 BOM)
- **Budget Type and Transaction Year are MANDATORY** (v1.66.0, performance): marked `*`, no "All" option, defaulted at page open, server 400s without them, and `btClearCriteria()` resets to that default scope rather than emptying it.
- Criteria observables — header: `btcType`, `btcBu`, `btcProjType`, `btcStatus`, `btcYear`, `btcApprover`, `btcTrxNum`, `btcDecree`, `btcFrom`, `btcTo`; **line-level (v1.65.0)**: `btcSector`, `btcChapter`, `btcProgram`, `btcApprop`, `btcCc`, `btcProject`, `btcTask`, `btcEtype`, `btcPeriod`, plus free-text `btcSearch` and the SHARED `buUnit` ("Figures in"). A transaction matches when ONE OF ITS LINES satisfies ALL the line criteria

## Sector Performance (`view()==='sectorperf'`) — Sector Financial Performance report (v1.72.0)
Rebuild of `docs/Reports/GL/Sector Report_October.pdf` as a live page. Five regions mirroring the
source pack's four pages plus a data-quality band. Reads `/gl/sectorperf*` (`db/24`), which reads
`DCT_SECTOR_PERF_V` — built ON TOP of the Budget Utilization view, so Budget / Actual /
Encumbrance / Funds Available reconcile to the Budget Utilization page by construction. **Plan is
the only added measure**, and while no real plan is loaded it is generated sample data.
- `loadSpFilters()` — every criteria LOV + the sample-data banner state (GET /sectorperf/filters); re-asserts the year after the option list lands (KO blanks a `<select>` bound to an empty list) and applies `spDefaultPeriod()`
- `spDefaultPeriod()` — the CURRENT month when the selected year is the current year (the Budget Utilization rule). Without it the page opens on "full year", where YTD Plan == FY Plan and the source pack's *Target [YTD Plan / FY Plan]* column reads a useless flat 100% on every row
- `runSectorPerf()` — one parallel fetch of all five endpoints (overview / sectors / departments / trend / revenue); `spSearch()`, `spClear()`, `spToggleExpand()`
- `spQuery()` — the shared criteria string: year (required), period, sector, costcenter, kind, projecttype, bu, plantype
- `spMatrix` — page 1's four rows (Revenue / Opex / Capex / **Opex & Capex**). The total row is COMPUTED from the two above it, so it can never disagree with them
- `spRag(pct)` — the source pack's RAG bands: 0–33 red, 33–67 amber, 67–100 green; `spPct(v)` renders a nullable percentage as `—`
- `spGaugeDash(pct)` — the Opex/Capex gauge arc length (`stroke-dasharray` on a half-circle SVG path; this app has no chart library, every visual here is hand-built SVG/CSS)
- `spBarH` / `spDeptW` / `spRevW` / `spTrendMonthH` — bar geometry for the cumulative Budget-vs-Actual columns, the department bars, the revenue-category bars and the MTD strip
- `spStream(code)` — the Sovereign / Commercial revenue cards; `spSampleActive` drives the amber banner
- `spPurgeSample()` — removes every sample plan row for the year (DELETE /sectorperf/sample, `GL_MANAGE_PLAN_SAMPLE`); confirms first, and can never touch a plan Finance uploaded
- `spExportCsv()` — the sector/project table with its reconciling total row (UTF-8 BOM)
- Criteria observables: `spcYear` (required), `spcPeriod`, `spcPlanType`, `spcProjType`, `spcBu`, the MULTI-SELECT chip arrays `spSectorSel` / `spDeptSel` / `spKindSel` (+ their `*Pick` feeder selects and `spChipRemove`), and the SHARED `buUnit`. **Sector and Department (Cost Centre) are multi-select as specified; kind defaults to `Opex|Capex`, the source pack's own footnote.**

## Navigation (v1.63.0) — three groups, sub-tabs per group; **landing page = Projects › Budget Utilization** (v1.64.0)
`NAV_GROUPS` in `app.js` is the single source: Projects (Budget Utilization · Projects Encumbrances ·
Encumbrances – Pending Approval · Budget Transactions · **Sector Performance** · Cashflow) / General Ledger (Dashboard ·
Budget vs Actual · Reconciliation · Legacy (EBS) · DOF Submissions · Balances YoY) / Settings
(Chart of Accounts).
- `navGroups` / `activeGroup` (**derived from `view()`**, so a deep link lights the right group) / `activeGroupItems` / `goGroup(id)` (opens the group's first page)
- **URL hash deep links (v1.121.0)** — `routeFromHash()` at boot + `go()` writes `#<tab>` back (`history.replaceState`); friendly alias `#budget-status` ⇄ `fd` (`HASH_ALIAS`/`HASH_LABEL`); unknown hash → default page; session gate/401 append `?return=` so login round-trips back to the deep link; short URL `/budget-status` (nginx)
- the app boots on `butil` and init routes through `go(view())` — the same path a nav click takes — so changing the landing page is a one-word edit to the `view` observable, never a second load branch in init

## DOF Submissions (`view()==='dof'`) — YoY / Budget Utilization / Quarterly (v1.49.0, reworked v1.52.0)
- `runDof()` — loads the selected dataset (GET /dof/yoy | /dof/butil | /dof/quarterly; Year **2016..current+1** + optional Period YTD-end) into the SHARED `<interactive-report>`; YoY includes chapter + grand total rows; cashflow-missing hint when the plan is not loaded (suppressed for EBS-era years < 2026, which show the era note instead); **column headers carry the run year** ("Revised Budget 2026", "Actual FY 2025"…) via `dofColsYoy/Butil/Quarterly(yr)` + `{y}`-substituted i18n keys, and the figure columns declare **`hint`** (ⓘ hover popover, v1.53.0 — generic `column.hint` support added to the shared IR component) explaining FY-vs-YTD prior actuals, the variance formula and cashflow/utilization semantics EN+AR
- **v1.56.0 layout round** — columns ordered in YEAR BLOCKS (frozen Chapter/Appr/Account code columns via shared-IR `column.sticky` → current-year block → prior-year block → Variance → Reasons) under **grouped FY header bands** (`column.group`; butil = Budget/Performance bands, quarterly = Budget + Quarter 1–4 bands); current-year columns brand-tinted / prior-year grey / quarters alternating (`dofc-*` colClass in app.css); Variance columns carry ▲ green / ▼ red delta arrows; long texts one-line ellipsis w/ hover full text; zebra rows
- `dofEmit()` + `dofUnit` / `dofZero` — display-only Search parameters re-projecting the CACHED run (no re-query): **"Showing figures in"** (AED exact / Thousands / Millions / Billions — money values scaled in the envelope) and **"Near-zero display"** (Dimmed 0.00 / Dash / Blank — shared-IR `column.nearZero`, threshold = rounds-to-zero at the current unit); both persisted in `gl_dof_ui`
- **v1.56.1** — identity columns MERGED (Appropriation / Account / Entity = "code — name"; `apprFull`/`accountFull`/`entityFull` computed on ingest, side-map + drawer keyed on them); envelopes carry `stateRev: 2` (shared-IR hook — stale saved column order yields to the year-block default)
- `dofChSel` / `dofApSel` + `dofChAdd` / `dofApAdd` / `dofApprOpts` / `dofApLabel` — **multi-select Chapter + Appropriation criteria** (butil chips pattern, any-of; Appropriation LOV scoped to picked chapters; applied client-side in `dofEmit` — whole-chapter picks keep server CHTOTAL rows, appropriation picks get `dofGrandOf()` recomputed grand w/ ratios recomputed)
- `toggleDofSec(k)` / `dofSearchSummary()` / `dofResSummary()` / `dofReset()` — butil-pattern collapsible Search + Results regions (state in `localStorage('gl_dof_ui')`), collapsed-state summaries, criteria reset
- `dofGridClick(d,e)` — delegated row click → Reasons/Remarks drawer (admins; `ko.contextFor(td)` + apprCode[|accountCode] side-map; YoY totals not clickable)
- `openDofNote(row)` / `closeDofDrawer()` / `saveDofNote()` — notes drawer (yoy/butil = one Reason; quarterly = 4 quarter Reasons + Remark; PUT /dof/notes per field, empty text deletes; saved notes pre-fill every future run); since v1.56.0 the drawer opens with a **Record details grid** (`dofNInfo` — every dataset column of the clicked row) above the note fields; v1.56.2 (2026-08-04 (3)): the grid mirrors the TABLE format — unit scaling, 2-decimal money, near-zero mode, ▲/▼ colored variance
- `runDofRegister()` — Generate Workbook (XLSX): enqueues DOF_YOY_PERF (yoy + butil — one workbook, two sheets) or DOF_QUARTERLY_PERF via POST /dof/register, polls, auto-downloads

## API Endpoints (ORDS) — `db/05_gl_ords.sql`, base `/ords/admin/gl/`
| Method | Path | Purpose |
|---|---|---|
| GET | `/budgettrx/filters` | Budget Transactions criteria LOVs — budget types (lookup `PA_BUDGET_TRX_TYPE`), business units, project types, statuses, years, approvers + sectors/chapters/programs/appropriations/accounting periods from `V_PA_BUDGET_TRX_LINE` (db/20) |
| GET | `/budgettrx/lov` | the four big type-ahead lists — projects, tasks, expenditure types, cost centres; `?type=` scopes them to one budget type (db/20) |
| GET | `/budgettrx` | master grid over `PA_BUDGET_TRX_HEADERS`, paged; header criteria `?type=&bu=&projecttype=&status=&year=&approver=&trxnum=&decree=&from=&to=` + LINE criteria `&sector=&chapter=&program=&appropriation=&costcenter=&project=&task=&etype=&period=` + `&search=` + `&page=&size=`; line criteria match when ONE line satisfies ALL of them, via `WITH /*+ MATERIALIZE */` CTEs over `V_PA_BUDGET_TRX_LINE` — **never a correlated EXISTS, see deployment-notes** (db/20) |
| GET | `/budgettrx/:num` | one transaction — header + its detail lines (from the per-type table matching `?type=`) + its approval trail (db/20) |
| GET | `/recon/filters` | Reconciliation LOVs — years, periods, budgetChapters default (CH2..CH5) + code+label dimension lists (db/14) |
| GET | `/recon/summary` | per-measure AP/GRN/PR/PO {actuals, butil, diff, noProject, apValidation, noBudgetLine} + derived totals + GL vs PPM budget + fund available each side (db/14) |
| GET | `/recon/rows` | reconciliation register at `?grain=` (account/sector/chapter/costcenter/appropriation/program/combination/budgetline) + optional `?measure=`; one row per dimension value with both sides + leakage buckets (db/14) |
| GET | `/recon/drill` | source records behind a difference cell — `?measure=ap\|grn\|pr\|po\|all&bucket=&grain=&key=` (bucket ∈ all/no_project/ap_validation/no_budget_line/matched; `measure=all` = cross-measure KPI drill, adds a Source column); generic doc rows + hasProject/validation/onBudgetLine flags (db/14) |
| GET | `/recon/budget` | budget-line detail behind the Budget & Fund KPI tiles — `?side=gl\|ppm`; ppm = PROJECTS_BUDGET project-lines, gl = GL_BALANCES per-combination (FULL-OUTER-JOINed to consumption); returns Budget/Consumed/Fund + `{total,consumedTotal,fundTotal,count}` (db/14) |
| GET | `/butil/agg` | **Results-region Department / Sector tabs (v1.89.0)** — `?level=dept\|sector(req)&year(req)=` + the full `/butil` filter set (period/sector/chapter/projecttype/costcenter/project/task/etype/search/bu/appropriation/program/ovr/procash/costadj): the SAME butil join + predicates GROUPed to cost-centre or sector grain (figures reconcile byte-exact to `/butil` totals) **plus the uploaded plan with NO budget line folded into the group plan figures** (attributed via `dct_butil_key_cache` task→project cascade; `planExtra` per row/totals = the folded amount = `/butil` `planUnmatched`; a group with plan but no lines ships `planOnly='Y'`); group vs-Plan/Coverage verdicts on the summed figures (same thresholds, `planThresholds` echo); per-group `cmtCount` + `commentsText` (v1.90.0 — the level's recorded comments at the selected period, `[period] author: text` newest first) from the COST_CENTER/SECTOR comment levels; no paging — the whole level ships (GL/db/33) |
| GET | `/costadj` | Costing Adjustments register (`?year=&status=&search=`, cap 2000, newest first; ships classification name + audit) (GL/db/25) |
| POST | `/costadj` | create DRAFT adjustment — corrected line + signed amount / signed budgetOverride (one must be non-zero) + reason req + **period MM-YYYY REQUIRED within the year** + optional dist ref/original-coding snapshot + classification (validated) (GL_MANAGE_COST_ADJ / SYS_ADMIN) (GL/db/25) |
| PUT | `/costadj/:id` | full-document edit — DRAFT only, else 400 (GL/db/25) |
| DELETE | `/costadj/:id` | remove an adjustment, any status (manage gate) (GL/db/25) |
| POST | `/costadj/:id/action` | `{action: APPROVE\|REJECT, note}` on a DRAFT row — stamps actor/date; APPROVED rows fold into `/butil` (GL/db/25) |
| GET | `/costadj/meta/dists` | AP invoice distribution search (`?search=` ≥2 chars, cap 50) — invoice/line/dist, effective supplier, AED amount, current project coding or none (GL/db/25) |
| GET | `/costadj/meta/lookups` | classification (`PA_COST_ADJ_CLASS`) + status LOVs (GL/db/25) |
| GET | `/costadj/meta/tasks` | tasks of a project (`?year=&project=`, butil key cache db/v2/120, cap 500) (GL/db/25) |
| GET | `/costadj/meta/etypes` | expenditure types of a project(+task) (`?year=&project=&task=`) (GL/db/25) |
| GET | `/butilcmt` | Budget Utilization Comments — thread mode (`?level=&year=&period=&project=&task=&etype=&ekey=`; BUTIL_LINE threads add TASK+PROJECT sections; ships `closedPeriods[]` + `authorId`/`hasPhoto`; roots newest first) or register mode (`?year=&period=&search=`, roots only, cap 2000) (GL/db/26) |
| POST | `/butilcmt` | create root (GL_ADD_BUTIL_COMMENT) or reply (`{parentId}`, GL_REPLY_BUTIL_COMMENT, parent must be a root); period MM-YYYY within the year; **CLOSED period → 403** (GL/db/26) |
| PUT | `/butilcmt/:id` | edit own comment text (author or SYS_ADMIN; ACTIVE only; closed period → 403) (GL/db/26) |
| DELETE | `/butilcmt/:id` | soft delete own comment (a root with active replies → 400; closed period → 403) (GL/db/26) |
| GET | `/butilcmt/meta/caps` | caller capability flags `{canAdd, canReply, canClosePeriod, canManageRoles}` via dct_sec.has_priv (GL/db/26) |
| GET | `/butilcmt/:id/docs` | attachment list (shared dct_documents GL/BUTIL_COMMENT) (GL/db/26) |
| PUT | `/butilcmt/:id/docs` | raw-binary attachment upload (`?file_name=&mime_type=`, `v_blob := :body` FIRST; MAX_UPLOAD_MB → 413; author or SYS_ADMIN; closed period → 403) (GL/db/26) |
| GET | `/butilcmt/docs/:docid` | attachment download (media handler) (GL/db/26) |
| DELETE | `/butilcmt/docs/:docid` | soft delete an attachment (uploader or SYS_ADMIN; closed period → 403) (GL/db/26) |
| GET | `/butilcmt/admin/roles` | roles × comment-capability grants (GL_MANAGE_CMT_ROLES / SYS_ADMIN) (GL/db/26) |
| POST | `/butilcmt/admin/roles` | grant/revoke `{roleId, capability: ADD\|REPLY\|CLOSE_PERIOD, granted}` on the COMMON dct_role_permissions + dct_sec.refresh_flat (GL/db/26) |
| GET | `/butilcmt/admin/periods` | 12 periods of a year + status/audit/active-comment counts (GL_CLOSE_BUTIL_PERIOD) (GL/db/26) |
| POST | `/butilcmt/admin/periods` | `{year, period, action: CLOSE\|REOPEN, remarks}` — CLOSED freezes every comment write in the period (GL/db/26) |
| GET | `/coamap` | EBS mapping register (`?segment=&search=&active=`, cap 5000) + segments LOV (GL/db/16) |
| POST | `/coamap` | create mapping row (GL_MANAGE_EBS_MAPPING / SYS_ADMIN; dup → 400) (GL/db/16) |
| PUT | `/coamap/:id` | partial update / deactivate mapping row (GL/db/16) |
| POST | `/ebs-balances` | bulk upsert EBS balance rows (≤500/req, per-row results, upsert on 7 segments + period; measures ptd/budget/encumbrance all optional →0; optional ccId/accountType/per-segment *Desc; adjustment period `13-YYYY` accepted, dated 31-Dec) (GL/db/16) |
| GET | `/ebs-balances/summary` | per-year coverage (rows/periods/combos/actual+budget+encumbrance totals/mapped) + top unmapped EBS accounts + Future2 values (GL/db/16) |
| POST | `/ebs-balances/register` | enqueue EBS_GL_BALANCE_REGISTER (year req) (GL/db/16) |
| GET | `/ebs-balances/register/:id` | poll register run status (GL/db/16) |
| GET | `/ebs-balances/register/:id/file` | download the register XLSX (GL/db/16) |
| GET | `/ebs-balances/yoy` | year-over-year YTD comparison on the Fusion account basis (years= pipe list max 6, month=0 FY or 1-12 YTD cutoff, search/atype/chapter; EBS years = stored YTD slices via the account map, 2026+ = Fusion fact view) (GL/db/18) |
| POST | `/ebs-balances/yoy/xlsx` | enqueue EBS_GL_YOY_REGISTER with the same params (GL/db/18) |
| POST | `/butil/refreshdata` | queue a FULL source-data re-extract → {queued:n} — `atd_set_pkg.run_now('PROJECTS_DATA')` + the 12 transaction full jobs set READY (in-flight CLAIMED jobs skipped, never re-queued); drained by the ATD worker fleet (GL/db/19, v1.75.0 scope) |
| GET | `/butil/refreshdata` | refresh status: {busy, jobs[{job,queueStatus,lastStatus,lastRows,lastFinished}]} — **all 15 full jobs since v1.75.0** (Projects/Tasks/Budget + AP Invoices/Lines/Distributions + PO Headers/Lines/Schedules/Distributions + PR Headers/Lines/Distributions + GRN Temporary Job/GRN Gap) (GL/db/19) |
| GET | `/ebs-balances/yoy/xlsx/:id` | poll YoY register run status (GL/db/18) |
| GET | `/ebs-balances/yoy/xlsx/:id/file` | download the YoY register XLSX (GL/db/18) |
| POST | `/cashflow` | bulk upsert GL budget-cashflow rows (≤500/req, per-row results; segments zero-padded to canonical widths; GL_MANAGE_CASHFLOW / SYS_ADMIN) (GL/db/17) |
| POST | `/cashflow/projects` | bulk upsert Projects cashflow rows (project/task/etype grain) (GL/db/17) |
| GET | `/cashflow/summary` | GL + Projects per-year/type coverage + top cashflow appropriations without a chapter (GL/db/17) |
| GET | `/cashflow/projects/template?year=&meta=` | Projects-cashflow upload template feed — `meta=Y` returns years[] only (picker); otherwise every budget line of the year (project/task/etype + full 10-segment GL combination + sector/department/cost-centre/GL account/appropriation/chapter/program/BU/project type/annual budget) with the cashflow amounts already saved as `a01..a12` (APPROVED) / `r01..r12` (REVISED); keys with no budget line still emitted (`inBudget='N'`), cap 20,000 (GL_MANAGE_CASHFLOW / SYS_ADMIN) — **GL/db/23** |
| GET | `/dof/yoy` | DOF YoY dataset — `?year=(req)&period=` (MM-YYYY YTD end, default latest); account grain (4xxxxx only) + EBS codes; **any loaded fiscal year 2016+** — current AND prior legs = Fusion ∪ legacy-EBS (bg-1, STORED YTD slices) + chapter/grand totals + reasons (GL/db/17) |
| GET | `/dof/butil` | DOF Budget Utilization dataset — appropriation grain: initial/revised budget FY, APPROVED/REVISED cashflow YTD, actual YTD, variance, utilization %, reasons; EBS-era years: Initial = Revised (one EBS budget measure); chapter/grand total rows via GROUPING SETS (`rowType` DETAIL/CHTOTAL/GRAND) (GL/db/17) |
| GET | `/dof/quarterly` | DOF Quarterly dataset — appropriation grain: approved budget, revised budget Q1/Q2, per-quarter approved/revised CF vs actuals + variance/% + quarterly reasons + remarks; EBS-era years via stored-YTD quarter slices; chapter/grand total rows (`rowType`) (GL/db/17) |
| GET | `/dof/notes` | saved Reasons/Remarks notes for a year (GL/db/17) |
| PUT | `/dof/notes` | upsert one note by natural key (year/entity/appropriation[/account][/quarter]/noteType; empty text = delete; GL_MANAGE_DOF_NOTES / SYS_ADMIN) (GL/db/17) |
| POST | `/dof/register` | enqueue DOF_YOY_PERF / DOF_QUARTERLY_PERF (`{report:YOY\|QUARTERLY, year, period?}`) (GL/db/17) |
| GET | `/dof/register/:id` | poll DOF report run status (GL/db/17) |
| GET | `/dof/register/:id/file` | download the DOF workbook XLSX (GL/db/17) |
| GET | `/boot` | dimensions catalog + combination/classified counts |
| GET | `/class-types` | dimensions |
| GET/POST | `/class-values` | list (by `?type=`) / create classification value | **GL/db/48**: `isDefault` (Entity only; one per dimension). |
| PUT/DELETE | `/class-values/:id` | update / delete value |
| GET | `/segments` | **GL/db/48**: the 10 canonical GL segments (key, position, width, EN/AR names) — the Entity rule picker |
| GET | `/segments/:key/values` | distinct segment values + description (picker; `?search=&limit=`) | **GL/db/48**: ANY of the 10 segment keys (was 3), values from the COA snapshot, `?type=` for currentValueId. |
| GET/POST | `/mappings` | list (`?type=&segment=&valueid=`; each row carries `segmentDesc` from the GL_SRC_* dimension) / create assignment (overlap-validated). GET upgraded by `09_gl_class_drill_ords.sql` (ADDITIVE, DEFINE_HANDLER-only; source synced into 05 so an 05 re-run already carries it) | **GL/db/48**: rows carry `segmentKey/segmentKeyName/segmentDesc`; POST takes `segmentKey` (Entity only) and fires the snapshot + butil-cache refresh jobs (`refreshQueued`). |
| PUT/DELETE | `/mappings/:id` | update / delete assignment. **PUT re-defined by `46_gl_mappings_put_fix.sql` (2026-09-04)**: it had been an uncatchable ORDS 555 on every call since birth (scalar subquery inside a PL/SQL expression); now reads the current row INTO locals — partial bodies keep the stored class value / start date; end < start = 400, unknown id = 404 | **GL/db/48**: PUT takes `segmentKey`; both fire the refresh jobs. |
| GET | `/combinations` | unified COA view, paginated, `?search=&sector=&chapter=&program=&asof=` | **GL/db/48**: + `entityClassCode/Name/Source` + `?entity=` (UNCLASSIFIED = no rule and no default). |
| GET | `/combinations/:ccId` | single combination |
| GET | `/balances` | budget balances enriched with Sector, `?asof=&sector=` |
| GET | `/actuals/filters` | report LOVs in one call: periods (+default), sectors, chapters, programs, appropriations, **accounts**, **costCenters** |
| GET | `/actuals` | Budget-vs-Actual per combination for one period (YTD), `?period(req)=&sector=&chapter=&program=&appropriation=&account=&costcenter=&accounttype=&source=&search=&limit=&offset=`; **sector/chapter/program/appropriation/account/costcenter/accounttype accept pipe-delimited any-of lists** (multi-select, v1.39.0); `accounttype` filters on the leading account digit (1=Assets…5=Owner's Equity); each item carries `accountTypeCode`; totals + items include `prTotal`, `openCommitment`, `commitmentPipeline`, `totalPo`, `openObligation` (GRN-netted), `poPipeline`, `openEncumbrance`, `fundsAvailable` (GL), `fundsAvailableCalc`, `slaActual`, `poCount`/`prCount` |
| GET | `/actuals/lines` | drill-down lines behind a figure, `?period=&cc=&metric=` (budget\|commitment\|opencommitment\|commitmentpipeline\|obligation\|openobligation\|popipeline\|glActual\|grn\|apDirect); commitment/openCommitment/commitmentPipeline→real PR lines (`pr_*`), obligation/poPipeline→PO lines, openObligation→PO lines GRN-netted (Ordered/Received/Open); **apDirect** shows the real AP Invoice # + Line (not `invoice_id`), zero-amount lines suppressed; **grn** shows GRN # + Line (`10_gl_actuals_lines_docnum.sql`, ADDITIVE DEFINE_HANDLER-only, source synced into 05) |
| GET | `/dashboard` | executive analytics for a period: KPIs + by sector/program/appropriation + trend, `?period=` |
| POST | `/actuals/refresh` | rebuild `DCT_GL_COA_SNAP` + recompile INVALID PROD views (manual; same proc as the hourly job) |
| POST | `/actuals/rebuild` | structural-reload recovery: re-create the 16 base pass-through views + recompile + refresh snapshot via `prod.dct_views_rebuild` (db/v2/38) → `{rebuilt, invalidRemaining, refreshedAt}` (`08_gl_views_rebuild_ords.sql`, ADDITIVE — re-run 07 AND 08 after any 05 re-run) |
| GET | `/butil/filters` | Budget Utilization LOVs: years (+`defaultYear`), projectTypes, sectors, chapters, **businessUnits (v1.34.0)** (`07_gl_budget_util_ords.sql`, ADDITIVE — re-run after any 05 re-run) |
| GET | `/butil/lov` | Budget Utilization **autocomplete LOVs** (v1.12.0), `?year=` (optional → all years) → `{costCenters[{cc,dept}], projects[{p,n}], tasks[], etypes[]}` — distinct values of `DCT_BUDGET_UTILIZATION_V` feeding the filter-bar datalists (`07_gl_budget_util_ords.sql`) |
| GET | `/butil` | Budget Utilization report, `?year(req)=&period=&projecttype=&sector=&chapter=&costcenter=&project=&task=&etype=&search=&limit=&offset=&nocc=` → totals + items over `DCT_BUDGET_UTILIZATION_V` (db/v2/37); `costcenter`/`project`/`task`/`etype` are contains-match LOV filters (project matches number OR name); **`chapter`/`costcenter`/`project` also accept a pipe-delimited MULTI list (v1.23.0, `a\|b\|c`) matched EXACTLY (chapter text / CC code / project number) — a single pipe-less value keeps the original semantics**; **`bu=` (v1.34.0) pipe-delimited EXACT any-of Business-Unit list (project attribution via the projects master; also on `/butil/lines` + `/encumbrances` + every report bridge)**; **`period=MM-YYYY` (v1.16.0, must belong to `year`, else 400) = YTD through that period** — sets `GL_CTX.BUTIL_END` via `DCT_GL_CLASS_PKG.set_butil_end` before the queries and always clears it after (+ on error) **v1.43.0:** response always carries `missingCc`/`missingCcBudget` (count / annual-budget sum of filtered rows with annual budget ≠ 0 and NO cost centre — drives the red alert band); `nocc=Y` filters the register to exactly those rows (the band's drill drawer). **v1.44.0 / reworked v1.66.0:** `ovr=Y` = **Select to include Budget Override** — sets `GL_CTX.BUTIL_OVR` so the view **ADDS the signed budget change** (`DCT_PROJECT_BUDGET_USER.budget_change`, db/v2/106 v2 — a negative change subtracts) to the annual AND the YTD budget; every budget-derived figure (budget/budgetAnnual/fundAvailable/utilization) then reflects it; always cleared after (+ on error). A change counts in YTD from **its own accounting period** onward. Response echoes `considerOverride` and totals + rows gain `overrideBudget`/`overrideBudgetAnnual`/`overrideLines` (= change YTD / change annual / changed-line count; ALWAYS emitted, flag-independent — feeds the separate Budget Change KPI). **The view aggregates change rows INDEPENDENTLY of the Fusion period rows (`pb_src` UNION ALL in db/v2/37): the budget is un-phased, so a change booked at a period with no budget row must still count.** **v1.74.0 (2026-08-22):** response also carries `negFund`/`negFundTotal` — count / sum of filtered lines whose effective Fund Available < −0.005 (with `procash=Y` the test uses fund − procash, matching the displayed figure) — drives the over-budget warning band; lives in **GL/db/21** (the live GET /butil owner), mirrored in 07. **v1.85.0 (2026-08-24, GL/db/28; REBASED 2026-08-26 as GL/db/31 after a parallel edit to 21 dropped it — see deployment-notes.md):** `negfund=Y` (any non-null value, same convention as `nocc`) filters the register to exactly the lines whose EFFECTIVE Fund Available is negative (same expression as `negFund`/`negFundTotal`, honouring `procash=`/`costadj=`) — the over-budget band's drill drawer. GL/db/31 is now the live GET /butil owner. |
| GET | `/butil/lines` | Budget Utilization figure drill-down, `?year(req)=&metric(req)=` (ap\|grn\|pr\|po) `[&period=MM-YYYY]` (YTD — inline date bounds on all four metric queries incl. the PO GRN-netting, so drill totals reconcile to the period-filtered figures; GRN dates = `NVL(accounted_date, transaction_date)`) + EITHER `&project=&task=&etype=` (row drill) OR `&projecttype=&sector=&chapter=&search=&costcenter=&fproject=&ftask=&fetype=` (KPI-card aggregate; the LOV filters are f-prefixed to avoid the row-drill key params; `chapter`/`costcenter`/`fproject` accept the same pipe-delimited MULTI lists as `/butil`, v1.23.0) → `{metric, aggregate, total, count, columns[], rows[]}`; one `kys` key-set CTE drives both modes; filters/amounts mirror `DCT_BUDGET_UTILIZATION_V` so totals reconcile to the on-screen figure; rows capped 1000 (window `total`/`count` are full). PO/PR header+line joins de-duped to avoid fan-out. **Every drawer shows document # + line** (ap = Invoice #/Line via `line_number`, grn = GRN #/Line via `receipt_line_number` **+ PO #/PO Line/Supplier** via de-duped `po_headers`/`po_lines` joins, pr = PR #/Line via `pr_lines.pr_line`, po = PO #/Line) and **zero-amount lines are suppressed** (ap/grn/pr amount ≠ 0, po open > 0); rows also carry the FUSION internal ids `invoiceId`/`poHeaderId`/`prHeaderId` for the drawer deep-links (v1.20.0) (`07_gl_budget_util_ords.sql`, ADDITIVE — re-run after any 05 re-run) |
| GET | `/butil/override/lines` | **Budget Change KPI drawer** (v1.44.0, reworked v1.66.0, `15_gl_butil_override_ords.sql`, ADDITIVE — re-run after any 05 re-run): every signed budget change (`DCT_PROJECT_BUDGET_USER`, db/v2/106 v2 — entered via the Excel VB workbook or this drawer), scoped by the full butil filter set (`?year(req)=&period=&projecttype=&sector=&chapter=&bu=&appropriation=&program=&costcenter=&project=&task=&etype=&search=`) via the same kys key-set CTE → `{total, totals{change,fusionAnnual,fusionYtd}, reasons[{code,name,nameAr}] (the XL_OVERRIDE_REASON lookup — v1.45.0), items[{id, projectNumber/Name, taskNumber/Name, expenditureType, accountingPeriod, fusionAnnual, fusionYtd, changeAmount, lineChangeTotal, adjustedAnnual, adjustedYtd, reasonCategory, comments, updatedBy, updatedAt}]}`; `id` = the opaque xl row key. **The join to the budget table is per LINE and OUTER** (un-phased budget: a change usually has no period row of its own); the year of a change comes from its own MM-YYYY period; fusion totals are de-duplicated per line via `rn_line`. |
| POST | `/butil/override` | Edit a change **from the drawer** — body `{id, budget_change\|null, reason_category?, comments?}` (**null or 0 clears the row**; reason/comments are PARTIAL — only keys present are applied; reason validated against lookup `XL_OVERRIDE_REASON` → 400); same core save as the Excel PUT (`prod.dct_xl_pkg.save_item` — stamps user + Dubai time) → the updated line JSON. A body still carrying the legacy `budget_user` key → **400**. Gate `GL_EDIT_BUDGET_OVERRIDE` (has_priv_or_role, GL style) |
| POST | `/butil/xlsx` | **Budget Utilization Excel register bridge** (v1.30.0) — same body contract as `/butil/book` → enqueues Reporting-Platform `BUDGET_UTIL_REGISTER` (XLSX, reporting/db/25 — 6 sheets: utilization lines / direct AP / GRN receipts / open PO / open PR / pending PR-PO queue) as the calling GL user → `{runId}`; **since v1.96.0 also takes `sheetcols`** (ordered sheet-1 column list from the Manage-Columns view → run param `sheet_cols_bu_lines` → runner `_apply_sheet_cols`) (`11_gl_butil_book_ords.sql`) |
| GET | `/butil/xlsx/:id` | run status (BUDGET_UTIL_REGISTER runs only) → `{runId, status, rowCount, error, startedAt, finishedAt, hasFile}` |
| GET | `/butil/xlsx/:id/file` | authed XLSX download of a finished register run |
| POST | `/butil/book` | **Briefing Book bridge** — body `{year(req), period?, sector?, chapter?, projecttype?, costcenter?, project?, task?, etype?, search?}` (the full butil page filter set since 2026-07-14; `period` = MM-YYYY within the year, validated like `/butil` → 400 otherwise; `chapter`/`costcenter`/`project` accept the pipe-delimited MULTI lists — the book seed's predicates are kept in lock-step, reporting/db/21) → enqueues Reporting-Platform `BUDGET_UTIL_BOOK` (PDF) via `dct_rpt_pkg.enqueue` as the calling GL user → `{runId}` (`11_gl_butil_book_ords.sql`, ADDITIVE — re-run after any 05 re-run; the /rpt/ admin endpoints stay SYS_ADMIN-only) |
| POST | `/butil/ppt` | **PowerPoint deck bridge** (v1.32.1) — same body contract as `/butil/book` → enqueues `BUDGET_UTIL_BOOK` with `formats='PPTX'` (executive slide deck via reporting/runner/render_pptx.py) → `{runId}` (`11_gl_butil_book_ords.sql`) |
| GET | `/butil/ppt/:id` | PPTX run status (BUDGET_UTIL_BOOK runs) → `{runId, status, rowCount, error, startedAt, finishedAt, hasFile}` |
| GET | `/butil/ppt/:id/file` | authed PPTX download of a finished deck run (Content-Disposition attachment) |
| GET | `/butil/book/:id` | run status (BUDGET_UTIL_BOOK runs only) → `{runId, status, rowCount, error, startedAt, finishedAt, hasPdf}` |
| GET | `/butil/book/:id/pdf` | authed PDF download of a finished briefing-book run (Content-Disposition attachment) |
| POST | `/butil/fbpxlsx` | **FBP - Projects Budget Utilization bridge** (v1.120.0) — same body contract as `/butil/xlsx` minus `sheetcols` (the FBP sheet-1 layout is FIXED in the definition) → enqueues Reporting-Platform `FBP_BUTIL_REGISTER` (XLSX, reporting/db/45 — the register copy with the 21-column FBP sheet 1, Task Name instead of Task Number) as the calling GL user → `{runId}` (`52_gl_fbp_register_ords.sql`, ADDITIVE — re-run after any 05 re-run) |
| GET | `/butil/fbpxlsx/:id` | run status (FBP_BUTIL_REGISTER runs only) → `{runId, status, rowCount, error, startedAt, finishedAt, hasFile}` |
| GET | `/butil/fbpxlsx/:id/file` | authed XLSX download of a finished FBP run (Content-Disposition attachment) |
| POST | `/butil/mssxlsx` | **MSS - Projects Budget Utilization bridge** (v1.122.0) — same body contract as `/butil/xlsx` minus `sheetcols` (the MSS layout is FIXED in the definition) → enqueues Reporting-Platform `MSS_BUTIL_REGISTER` (XLSX, reporting/db/46 — 26-col sheet 1, Requester on sheets 2–6, Task Name beside every Task Number on all sheets) as the calling GL user → `{runId}` (`53_gl_mss_register_ords.sql`, ADDITIVE — re-run after any 05 re-run) |
| GET | `/butil/mssxlsx/:id` | run status (MSS_BUTIL_REGISTER runs only) → `{runId, status, rowCount, error, startedAt, finishedAt, hasFile}` |
| GET | `/butil/mssxlsx/:id/file` | authed XLSX download of a finished MSS run (Content-Disposition attachment) |
| POST | `/butil/sectorbook` | **Sector Performance Report bridge** (v1.115.0) — same body contract as `/butil/book` → enqueues Reporting-Platform `SECTOR_PERF_BOOK` (PDF, reporting/db/42 — the Briefing Book pack with the distribution cover: DCT logo + copyright on every page) as the calling GL user → `{runId}` (`50_gl_sector_book_ords.sql`, ADDITIVE — re-run after any 05 re-run) |
| GET | `/butil/sectorbook/:id` | run status (SECTOR_PERF_BOOK runs only) → `{runId, status, rowCount, error, startedAt, finishedAt, hasPdf}` |
| GET | `/butil/sectorbook/:id/pdf` | authed PDF download of a finished Sector Performance Report run (Content-Disposition attachment) |
| GET | `/encumbrances` | **Open Projects Encumbrance Follow-up** — `?year(req)=&period=&projecttype=&sector=&chapter=&costcenter=&project=&task=&etype=&search=&limit=` (same butil filter set + semantics; key-set CTE lifted from `/butil/lines`) → `{year, columns[{key,label,type}], items[], count, total, truncated, maxRows, totals{openAed}}`. UNION of Open Commitment PR (reserved) + Open Obligation PO (GRN-netted) lines, joined to `DCT_GL_COA_SNAP` (via `dct_cc_canon(charge_account)`) for all 10 segment codes+names + sector/chapter; open amounts reconcile to the butil Total Encumbrance; zero lines excluded; capped 10 000 by open amount. (`12_gl_encumbrances_ords.sql`, ADDITIVE — re-run after any 05 re-run; the frontend passes `limit=10000` since ORDS injects its default page size into `:limit` when absent.) |
| GET | `/pending` | **Encumbrances – Pending Approval** — `?year(req)=&period=&projecttype=&sector=&chapter=&costcenter=&project=&task=&etype=&search=&source=&bu=&limit=` (same butil filter set + semantics; `source=PR\|PO` scopes the register AND the aggregates, anything else 400; `bu=` pipe-delimited EXACT any-of Business Unit list — also scopes the `unmatched` coverage KPI; `businessUnits[]` in the response = the snapshot's BU LOV; period-YTD via `set_butil_end`, ALWAYS cleared; items carry `fusionHeaderId` — the FUSION internal pr/po header id for the deep-link builders) → `{year, period?, asOf, kpis{docs,docsPr,docsPo,lines,amtTotal,amtPr,amtPo,reservedAmt,unreservedAmt,encOpenAmt,avgDays,maxDays,over30Docs,over30Amt}, aging[{bucket,docs,amt}×4], approvers[{name,docs,amt,maxDays}≤8], unmatched{docs,prDocs,poDocs}, columns[], items[], count, truncated, maxRows, totals{lineAed,encOpenAed}}`. Rows from `DCT_PR_PO_PENDING_V` (db/v2/52) × the butil key-set CTE, joined to `DCT_GL_COA_SNAP`; ONE scan streams the register (cap 10 000, oldest first) and accumulates all aggregates in the same loop (per-document dedupe via assoc array), so the KPIs cover the full filtered set even when the echoed rows are capped. **Zero-value lines excluded** (`ABS(NVL(line_aed,0)) > 0.005`, v1.30.1 — register, KPIs, aging, approvers and the client drill slices all inherit it; the `unmatched` coverage KPI is computed separately and is unaffected). `asOf` = snapshot load time (dct_to_local). (`13_gl_pending_ords.sql`, ADDITIVE — re-run after any 05 re-run; pass `limit=` explicitly, ORDS injects its page-size default into `:limit` when absent.) |
| POST | `/pending/book` | **Pending-approval Briefing Book bridge** — same body contract as `/butil/book` → enqueues Reporting-Platform `ENC_PENDING_BOOK` (PDF, reporting/db/23) as the calling GL user → `{runId}` (`13_gl_pending_ords.sql`) |
| GET | `/pending/book/:id` | run status (ENC_PENDING_BOOK runs only) → `{runId, status, rowCount, error, startedAt, finishedAt, hasPdf}` |
| GET | `/pending/book/:id/pdf` | authed PDF download of a finished pending-approval briefing-book run |
| POST | `/pending/xlsx` | **Excel register bridge** (v1.29.0) — same body contract as `/pending/book` + `source` (PR\|PO) → enqueues Reporting-Platform `ENC_PENDING_REGISTER` (XLSX, reporting/db/24) as the calling GL user → `{runId}` (`13_gl_pending_ords.sql`) |
| GET | `/pending/xlsx/:id` | run status (ENC_PENDING_REGISTER runs only) → `{runId, status, rowCount, error, startedAt, finishedAt, hasFile}` |
| GET | `/pending/xlsx/:id/file` | authed XLSX download of a finished register run (Content-Disposition attachment) |
| GET | `/projects/filters` | Portfolio filter LOVs (years, types, sectors, chapters, BUs, appropriations, programs, statuses, bands) — `db/22` |
| GET | `/projects/lov?year=` | Portfolio type-ahead lists (projects, cost centres, managers) — `db/22` |
| GET | `/projects?year=&period=&…&band=&status=&manager=&sort=` | Portfolio register + KPI aggregates + bands/flags/dataGaps; **totals reconcile exactly to `/butil`** — `db/22` |
| GET | `/projects/:num?year=&period=&ovr=` | Project 360 header, year figures, funnel, health, AP, revenue, pending, schedule — `db/22` |
| GET | `/projects/:num/tasks?year=&period=` | Task register with planned dates and per-task money — `db/22` |
| GET | `/projects/:num/pipeline?year=&period=` | Open PR / open PO / uninvoiced GRN / pending, one call — `db/22` |
| GET | `/projects/:num/invoices?year=&status=` | AP invoices with the derived paid ratio — `db/22` |
| GET | `/projects/:num/revenue?year=` | AR billed revenue + the receipts DATA_GAP marker — `db/22` |
| GET | `/projects/:num/trx?year=` | Budget transactions touching the project — `db/22` |
| GET | `/projects/:num/cashflow?year=` | Uploaded cashflow plan by period (`sparse` flag) — `db/22` |
| GET | `/sectorperf/filters?year=` | Sector Performance criteria LOVs + sample-data state — `db/24` |
| GET | `/sectorperf?year=&period=&sector=&costcenter=&kind=&projecttype=&bu=&plantype=` | Page 1+2 header: kind matrix, revenue, revenue-to-opex, KPI band, gauges, data quality — `db/24` |
| GET | `/sectorperf/sectors?…&level=sector\|project` | Page 3 sector table with its reconciling total row; `level=project` expands — `db/24` |
| GET | `/sectorperf/departments?…` | Page 2 department series (budget / actual / plan per department) — `db/24` |
| GET | `/sectorperf/trend?…` | Cumulative monthly budget / actual / plan (`budgetBasis` states the budget is flat) — `db/24` |
| GET | `/sectorperf/revenue?…` | Page 4: streams, categories, MTD trend — `db/24` |
| GET | `/sectorperf/sample?year=` | Sample-plan status (rows, batches, canPurge) — `db/24` |
| DELETE | `/sectorperf/sample?year=` | Purge the sample plan (`GL_MANAGE_PLAN_SAMPLE`) — `db/24` |

## Data layer (PROD)
- Tables: `DCT_GL_CLASS_TYPE` → `DCT_GL_CLASS_VALUE` → `DCT_GL_SEG_CLASS_MAP`.
- Package: `DCT_GL_CLASS_PKG` (`norm`, `set_asof`/`clear_asof`/`get_asof`, `resolve_value_id`, `validate_map`); context `GL_CTX`.
- Views: `DCT_GL_COA_V` (+`GL_COA_V`), `DCT_GL_BALANCES_V`.
- Actuals reporting (db/v2/32–36): base pass-through views (`AP_*`/`PO_*`/**`PR_*`**/`GRN_ALL_V2`/`GL_BALANCES`), **`GL_BALANCES_CC`** (the ONE canonical remap of the reloaded feed's re-ordered `CONCATENATED_SEGMENTS` — every gl_balances↔COA join goes through its `cc_string`), `DCT_ACTUAL_V`, `DCT_BUDGET_ACTUAL_V`, **`DCT_PR_COMMITMENT_PERIOD_V`** (`db/v2/36`, over `ATD_PR_*`, AED via `DCT_CURRENCY_CODES` snapshot: `pr_total_ytd`=Reserved+Liquidated / `pr_open_commitment_ytd`=Reserved / `pr_pipeline_ytd`=Not-reserved + `pr_count`), and `DCT_BUDGET_ACTUAL_PERIOD_V` (per-combination × period YTD; +appropriation; joins the PR view for Commitment; PO 3 buckets from `po_distributions` de-duped by `po_distribution_id` — `total_po_ytd` [ex Failed/Passed] / `open_obligation_ytd` [(Reserved+Partial) **GRN-netted** via `grn_all_v2.po_distribution_id`] / `po_pipeline_ytd` [Failed/Passed]; `open_encumbrance_ytd`; `funds_available_calc_ytd` = Budget−OpenPO−OpenPR−GRN−APDirect; `funds_available_ytd` = GL balance; `po_count`/`pr_count`; **`ap_direct_actual_ytd` = AP lines with `po_number IS NULL`**); indexed snapshot `DCT_GL_COA_SNAP` + `prod.dct_actuals_refresh` (hourly job `DCT_ACTUALS_REFRESH_JOB`).
- Bridge synonyms: `GL_SRC_*` → `ATD_GL_*` (rename-insulation).
- Sector Performance (db/v2/123): `DCT_SECTOR_PERF_V` (butil + plan + expenditure kind — the report fact), `DCT_SECTOR_PLAN_V` (expenditure plan aggregated at the butil grain, BUTIL_END-aware) + `DCT_SECTOR_PLAN_ORPHAN_V` (plan rows with no budget line, surfaced not dropped), `DCT_SECTOR_ACTUAL_MONTH_V` (AP+GRN by month, no period cut — `SUM(months <= cut) == the butil actual at that cut`), `DCT_GL_REVENUE_FACT_V` (revenue ACTUAL from `ATD_AR_INVOICE_DISTRIBUTION`), tables `DCT_GL_REVENUE_CATEGORY` / `DCT_GL_REVENUE_CAT_MAP` / `DCT_GL_REVENUE_PLAN`, and `DCT_GL_PLAN_SAMPLE_PKG` (`generate_all` / **`purge`** — sample plan data, removable in one call).

## Security Info drawer (2026-07-19 — Security Console adoption, SYS_ADMIN only)
- `isSysAdmin` (from session rolesCsv) shows the pnav-end **Security Info** `<button>` (never an `<a>` — the pending smoke asserts 9 pnav anchors).
- `openSecurityInfo` — fetches `GET /dct/sec/pageinfo?module=GL&page=<active tab>` and renders the `.dw-*` drawer: page view privilege, registered artifacts (buttons/endpoints/actions) and the duty/job roles granting each privilege. `closeSecurityInfo`.
- Server side: every 07/09/11/12/13 handler now checks `dct_sec.has_priv_or_role(user,'<GL_PRIV>',NULL,'GL')` (grandfathered while `FEATURE_SEC_ENFORCE_GL`=N) and /gl/butil + /gl/encumbrances + /gl/pending apply the SECTOR security-profile scope (`DCT_SEC_DATA` + `V_DCT_SEC_USER_SCOPE`).

<!-- 2026-08-13 additions -->

## Generate and Send (butil page-head, v1.84.0) — scoped report emails to To/Cc/Bcc lists
- `toggleGsMenu`/`closeGsMenu` — the **Generate and Send ▾** split button (Sector / Department / Project level).
- `openGs(level)` — opens the `.dw-gs` drawer, loads the level tree (`GET /butil/dist/meta/tree`) into the SHARED `<tree-select>` component (tri-state checkboxes, filter, CSS-drawn icons).
- `gsAdd`/`gsRemove` — move ticked leaves to the right-hand "Selected for sending" list.
- `gsNext` — recipient preview (`POST /butil/dist/meta/preview`): To/Cc verbatim + Bcc count per node, TEST-MODE / email-off banners, no-list warnings; `gsRunsPlanned`.
- `gsSendGo` — `POST /butil/dist/meta/send` (page criteria + node scope; formats PDF book / XLSX register) → step-3 progress polling `GET /butil/dist/batch/:bid` every 5s (`gsAllDone`, `gsRunTone`).

## Email Logs (`view()==='emaillog'`, v1.84.0) — Generate-and-Send history
- `elRun`/`elClear` — search: date range, level, sector/CC/project, recipient-contains, run status, email status, test Y/N, report, batch (`GET /butil/emails`).
- `elDrill(row)` — `.dw-el` drawer over `GET /butil/emails/:rid`: per-address disposition + outcome, TEST badge, criteria echo; `elDownload(file)` re-downloads the run's PDF/XLSX via the existing book/xlsx file routes.

## Report Recipients (`view()==='recipients'`, Settings group, v1.84.0) — the To/Cc/Bcc lists
- `rlLoad` — register (`GET /butil/dist`; counts per disposition). Row click = `rlOpen` (edit drawer), `rlNew`, `rlSave` (POST/PUT), `rlDelete` (409 while email history exists — disable instead), `rlAddRecip`/`rlDelRecip`.
- Excel import: `rlImpOpen`/`rlImpPick`/`rlImpFile` (SheetJS parse), sheet auto-detect (Departments→COSTCENTER / Sectors→SECTOR), email columns by @-density with To/Cc defaults (`rlImpCols`), Active-only filter, `rlImpRun` (`POST /butil/dist/meta/import`, ≤500-row chunks, replaces matched rows' recipients).

### API Endpoints (ORDS) — `db/27_gl_report_dist_ords.sql` (additive; re-run after any 05 re-run)
| Method | Path | Purpose |
|---|---|---|
| GET/POST | `/gl/butil/dist` | recipient-list register / create (create = GL_MANAGE_REPORT_DIST) |
| GET/PUT/DELETE | `/gl/butil/dist/:id` | one list + recipients / partial update / delete (409 w/ history) |
| GET | `/gl/butil/dist/meta/tree?level=&year=` | Sector→CC→Project selection tree (butil key cache) |
| POST | `/gl/butil/dist/meta/preview` | To/Cc/Bcc preview per node + testMode/emailEnabled flags |
| POST | `/gl/butil/dist/meta/import` | Excel-sourced chunked upsert (≤500 rows) |
| POST | `/gl/butil/dist/meta/send` | batch send: one run + ONE email per node × format (GL_RUN_BRIEFING_BOOK) |
| GET | `/gl/butil/dist/batch/:bid` | batch poll (runs + sent/failed/skipped counts) |
| GET | `/gl/butil/emails` | email history register (filters incl. test Y/N) |
| GET | `/gl/butil/emails/:rid` | one email drill (recipients w/ dispositions, files, criteria) |

## Terms and Key definitions (`view()==='terms'`, Settings group, v1.118.0) — report intro content
- `tkLoad` — register of rich-text definition documents (`GET /terms` + `GET /terms/meta/lookups` in parallel; canManage echo). Row click = `tkOpen` (detail drawer incl. CLOB `contentHtml`), `tkNew`, `tkSave` (POST/PUT — content read from the Quill editor via `getSemanticHTML()`), `tkDelete`.
- **Quill 2.0.3 rich-text editor** (vendored `Jet/lib/quill/`, UMD loaded BEFORE require.js in the boot chain): bold/italic/underline/strike, headers, color/background, sizes, lists, indent, align, RTL direction. `tkQuillOk` falls back to a raw-HTML textarea if the script failed to load. Saved HTML prints as **content entry 01 of the Sector Performance Report** with identical formatting (the ACTIVE document whose start/end window covers the report period end).
- Settings tab is capability-hidden: `termsCaps` loaded at boot from `/terms/meta/caps` (GL_MANAGE_TERMS/SYS_ADMIN).

### API Endpoints (ORDS) — `db/51_gl_terms_ords.sql` (additive; re-run after any 05 re-run — GL post-05 re-run list is now 07..51)
| Method | Path | Purpose |
|---|---|---|
| GET/POST | `/gl/terms` | register (+canManage echo) / create (GL_MANAGE_TERMS; validates lookups + dates; HTML sanitised) |
| GET/PUT/DELETE | `/gl/terms/:id` | detail incl. CLOB contentHtml / partial update (full-CLOB body parse — never 32K-capped) / delete |
| GET | `/gl/terms/meta/caps` | `{canManage}` — drives the Settings tab visibility |
| GET | `/gl/terms/meta/lookups` | applied-to (`GL_TERMS_APPLIED_TO`) + status (`GL_TERMS_STATUS`) LOVs EN+AR |

### Cost adjustments in the drill drawers (v1.86.0)
- `drillWithCostAdj(params, metric, wantAdj)` — fetches `/butil/lines` + `/butil/lines/costadj` in parallel and merges rows/total/count; used by `openBuDrill` (gate: metric ap/budget/budgetannual + `buCadjOn()` echo + `row.hasAdj==='Y'`) and `openBuAgg` (echo only).
- ORDS: `GET /gl/butil/lines/costadj` (`db/29_gl_costadj_drill_ords.sql`, additive) — APPROVED `DCT_PA_COST_ADJ` rows for the drill scope, drill-shaped per metric; YTD rule = DCT_PA_COST_ADJ_BUTIL_V. Since 2026-08-27 the ap rows ARE the referenced invoice distribution (number + Fusion link via invoiceId, line, date, vendor, currency, header amount, REAL validation/payment statuses, the distribution's own description via AP_INVOICE_DISTRIBUTIONS_V) with the ADJUSTMENT amount in the money column and fromAdj/adjRef driving the drawer's (**) source marker + footnote; no reference = PCA-shaped row.

### Expenditure Plan balances (v1.87.0, 2026-08-26)
- Butil results table: **Plan Annual / Plan YTD** columns after YTD Budget (v1.87.1: headers carry no "Approved" prefix; the Revised pair keeps its prefix) (Revised pair auto-appears via `buPlanRevOn` only when any revised plan exists); rows/totals fed by `/gl/butil`'s `planApprovedAnnual/Ytd`, `planRevisedAnnual/Ytd`, `hasPlan`; totals also ship `planUnmatched` (plan on lines outside the butil year — advisory note under the table). CSV gains the four columns.
- KPI band: 6th tile **Expenditure Plan** (`.bk-plan`, hero = Approved YTD + `buPlanYtdPct()` of annual; drillable Annual/YTD rows via `openBuPlanAgg(type, annual)`).
- `openBuPlanDrill(row, type, annual)` / `openBuPlanAgg(type, annual)` — monthly plan rows in the shared drawer (columns client-defined: period/amount/loadedBy/loaded/file, + project/task/etype in aggregate mode); totals reconcile to the cell/tile.
- Portfolio register (shared IR): `planA`/`planY` money columns (+ `planRevA`/`planRevY` when revised exists; `stateRev` 2) + a plan KPI tile; Project 360 `year` block + plan KPI tile.
- ORDS: `GET /gl/butil/lines/plan` (`db/30_gl_plan_drill_ords.sql`, additive) — `DCT_PROJECT_CASHFLOW` monthly rows for the drill scope; `type=APPROVED|REVISED`, YTD cut via `period` (omit = annual); row + aggregate (kys CTE) modes; **`extra=Y` (v1.92.0)** also includes the plan rows whose key has NO budget line, attributed through `dct_butil_key_cache` like db/33's unmatched leg (the Department/Sector tab drills send it; default N keeps the Plan KPI tile drill matched-only); source view `DCT_PROJECT_CF_BUTIL_V` (db/v2/126). `/gl/projects` + `/gl/projects/:num` (db/22) carry the same four plan keys.

### Fund Movement columns (v1.94.0, 2026-08-30)
- Butil results tables (ALL three tabs — Budget Line / Department / Sector): **Fund Movement Count** + **Fund Movement Amount** columns right after Fund Available. Count = Additional Fund transfer LINES on the row's key/group in the budget year up to the selected period (all statuses — register-sheet parity; zero-amount lines excluded so the count always equals the drill row count); Amount = NET signed (additions − deductions), green `.fm-pos` / red `.fm-neg`. Header hints `chFmCnt`/`chFmAmt`.
- `runBuFm(level)` — fetches `GET /butil/fundmove` per level into `buFmLineMap` (key `project|task|etype`; params = year+period only, so filter changes never refetch it) / `buFmAggMap` (key = CC or sector name; params = the full agg filter set); called from `runButil` + `runBuAgg`. `fmOfRow(row)` / `fmOfGrp(row)` resolve a row's stats; `fmTxt`/`fmCls` render the cells.
- `fmHover(fm, e)` / `fmOut` — `.fm-tip` popover on cell hover: ▲ n additions +amt (green) / ▼ n deductions −amt (red) / Net.
- `openBuFmDrill(row)` (line tab, exact key) / `openBuAggFmDrill(row)` (group scope via `buAggGrpScope`; keyless group → `openBuMissCc`) — shared drill drawer with the register Fund Movement sheet's **28 columns** (`fmDrillCols`, Amount tinted via the drill `pn` column flag) + an FM-only **sort switch** (`.fm-sortbar`, `fmDrillSort`/`setFmDrillSort`: default = Project·Task·Type·Date, date = Date·Project·Task·Type; `fmDrillOn` reset in `fillDrill`).
- `buFmAggExtra` computed — transfers attributed to a CC/sector with no group row → `fmExtraNote` under the agg table (never dropped). Both page CSV exports (`buExportCsv`/`buAggExportCsv`) carry the two columns.
- ORDS (`db/34_butil_fundmove_ords.sql`, additive — **GL post-05 re-run list = 07..34**): `GET /gl/butil/fundmove` (`level=line|dept|sector` keyed aggregates {cnt, amt, posCnt/posAmt, negCnt/negAmt} + totals; line level ignores page filters, dept/sector bind the own-attribute filter set incl. SECTOR data scope) + `GET /gl/butil/lines/fundmove` (drill rows, `sort=default|date`, row + group modes, cap 1000). Source = `pa_additional_fund_lines` ⋈ `pa_budget_trx_headers` type 'Additional' ⋈ aggregated `pa_budget_trx_approvals` — lifted from reporting/db/25's Fund Movement sheet SQL so page and register always tie. Smokes `tests/butil_fundmove_api_smoke.py` 50/50 + `tests/fm_browser_smoke.py` 33/33 EN+AR.
- **v1.95.0 (2026-08-30)**: the drill's Code Combination column shows the styled 10-segment popover (drill response ships a `combos` side-object — distinct combinations resolved against `DCT_GL_COA_SNAP`; drawer hover delegate `drillComboRow` = `acRowMap || fmComboMap`), and the 5 status columns (Transaction/Line/Baseline/Journal/Approval State) render as tinted icon pills via `btStClass` + a drill column `type:'status'` branch (`.dw-drawer .st` vocabulary in app.css).

### API Endpoints (ORDS) — `db/37_gl_fmr_ords.sql` (additive; re-run after any 05 re-run — GL post-05 re-run list is now 07..37)
| Method | Path | Notes |
|---|---|---|
| GET | `/gl/fmr/entity?period=` | Budget/Actual/Plan `overall{}` + per-entity `entities[DCT\|ALC\|MUSEUMS\|MASTERPIECES\|UNCLASSIFIED]` + per-type `trend[PAYROLL\|OPEX\|CAPEX\|CWIP\|OTHER\|UNCLASSIFIED]`. Read = any valid session. |
| GET | `/gl/fmr/sector?period=&entity=` | per-sector Budget/Actual/Plan, `entity=` optional pipe-delimited any-of over the entity codes above. |

### API Endpoints (ORDS) — `db/42_gl_fd_ords.sql` + `db/43_gl_fd_drill_ords.sql` (additive; GL post-05 re-run list = 07..43)
| Method | Path | Purpose |
|---|---|---|
| POST | `/gl/fd/report` | **Live (GL/db/49).** Queue `GL_BUDGET_STATUS` PDF/PPTX with period, entity, sectors, departments, presentation, units, language, sorts and department search. Requires `GL_RUN_BRIEFING_BOOK`; reporting/db/41 definition. |
| GET | `/gl/fd/report/:id` | **Live.** Report status; report privilege and requesting-user ownership required. |
| GET | `/gl/fd/report/:id/file` | **Live.** Authenticated PDF/PPTX download; same report privilege and ownership. |
| GET | `/gl/fd/status?period=` | Budget Status cube: `rows[]` at (sector × cost centre × entity × chapter) grain + `costCenters[]` LOV (v1.104.0) with budget/actual/encumbrance/fundsAvailable/combinations + `sectors[]` LOV + `chapters[]` + `entities[]` + `excluded{}` (Budget-Group-1 rows outside Chapters 1–3) + `totals{}` + `thresholds{near,over}` (v1.105.0: GL settings BUD_UTIL_NEAR_PCT / BUD_UTIL_OVER_PCT, defaults 90/100 — the page's state pills + map heat). **v1.107.0 (approach A): ONE scan for the whole year** — + `series[{s,c,e,ch,m{MM:[budget,actual,encumbrance,fund,combinations]}}]` for every loaded period of the year, `periods[]` (MM-YYYY asc), `excludedByPeriod{MM}`; the anchor period's rows/LOVs/excluded/totals are collected from the same pass (3.0 s / 221 KB). Same view + rules as `/fmr/entity` (reconciles to the cent). Read = any valid session. | **GL/db/47 (2026-09-06)**: `entity` per row = the data-driven ENTITY classification (`entities[]` from the values table + Unclassified when present). |
| GET | `/gl/fd/lines?period=&metric=[&chapter=][&entity=][&sector=][&costcenter=]` | (GL/db/43) Figure drill: one row per 10-segment combination with all four measures (drilled one = `total`, zero rows suppressed), sorted chapter → account → cost centre, cap 5,000, + `combos{}` segment side-map for the drawer popover. Same predicates as `/fd/status` — reconciles to the band figure. Read = any valid session. |
| GET | `/gl/fmr/notes?period=` | saved Current/Expected Variance notes for the period. |
| PUT | `/gl/fmr/notes` | upsert one note `{period, noteType: CURRENT_VARIANCE\|EXPECTED_VARIANCE, text}` (empty text deletes); `GL_MANAGE_DOF_NOTES` (SYS_ADMIN). |
| GET | `/gl/fmr/trend/lines?period=&metric=&chapter=&entity=` | KPI/trend drill: supporting GL combinations for `metric` budget\|actual\|plan\|fundsavailable, `chapter`/`entity` both optional (omitted = all chapters / whole entity level), sorted chapter then GL account (generic drill shape + `combos` side-map). |

### API Endpoints (ORDS) — `db/41_gl_fmr_report_ords.sql` (additive; part of the GL post-05 re-run list, now 07..41)

| Method | Path | Purpose |
|---|---|---|
| POST | `/gl/fmr/report` | run the Reporting-Platform definition `GL_FMR_REPORT` (reporting/db/40) — body `{period MM-YYYY req, format PDF\|XLSX\|PPTX (default PDF), entity optional}` → `{runId}`; gate `GL_RUN_BRIEFING_BOOK`. |
| GET | `/gl/fmr/report/:id` | run status `{status, rowCount, error, format, hasFile}`. |
| GET | `/gl/fmr/report/:id/file` | authed download of the finished run's file (whichever single format the run rendered). |

New table `dct_gl_fmr_note` (year+period+type, unique, upsert). No new comment infrastructure — Sector-level "Comments" reuses the existing `SECTOR` level of Budget Utilization Comments (`db/v2/125` + `GL/db/26`). The page's **Generate Report ▾** dropdown (`fmrGenOpen`/`runFmrReport(fmt)`) polls the bridge and auto-downloads; PDF = landscape book `gl_fmr_book.html.j2`, XLSX = sheet per section, PPTX = `render_pptx.py` `_fmr_deck` (dispatches on `report_code`).
