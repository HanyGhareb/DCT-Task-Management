# Financial Planning and Budgeting — FP (App 210) — Functions List

> **2026-07-22 (v1.42.0) renames:** the app is now **Financial Planning and Budgeting** (`appName`, sub "FP"); the **Actuals** tab is now **General Ledger** (`navActuals`, `view()==='actuals'` unchanged); **Budget Utilization** is now **Project Budget Utilization** (`navButil`/`buTitle`); **Overview** and **Explorer** are merged into one **Chart of Accounts** page — see below.


User-facing functions by area. Each area = a `view()` section in `Jet/index.html`; each bullet is a
public method on the single KO viewModel in `Jet/js/app.js`. Mandatory maintenance artifact —
update on any view/method/endpoint change.

## Chart of Accounts (`view()==='overview'`) — the ONE CoA hub: overview + values + mapping + explorer (v1.42.1)
Four collapsible `.bu-sec` regions; `loadCoa()` loads all three data sets once on landing / `go('overview')` (`coaLoaded` guard). `toggleCoa('ov'|'cls'|'map'|'exp')` toggles each (`coaOvOpen`/`coaClsOpen`/`coaMapOpen`/`coaExpOpen`; overview + explorer open by default, values + mapping collapsed). **`classifications`, `mapping` and `explorer` view ids + nav entries are removed — `go()` redirects them to `overview`.**
- **Region 1 — Classification overview**: coverage stats `combinationCount`, `pctClassified`, per-dimension `valueCount`/`mappedSegments` (from `/boot`) + "How it works".
- **Region 2 — Classification values** (was the Classifications page): `clsType` dimension selector + `addValue`/`editValue`/`saveValue`/`deleteValue` + row-click `openClsDrill` assignments drawer. Own loader flag `clsLoading` (was the shared `loading`, which now only drives the Explorer table). `clsType.subscribe` reloads while on `overview`.
- **Region 3 — Segment mapping** (was the Segment Mapping page): `mapType`/`segSearch`/`segOptions`/`mapSegment` picker + effective-history timeline + `addMapping`/`editMapping`/`deleteMapping`. `mapType.subscribe` reloads while on `overview`.
- **Region 4 — Combinations explorer** (`.bu-results`, **maximizable** via `toggleCoaMax()`/`coaMax`, Esc restores): filters `expSearch`/`fSector`/`fChapter`/`asOf` → `loadCombos(offset)`, `comboRange`, `exportCsv()` (≤500 rows).

## Classifications — MERGED into Chart of Accounts Region 2 (v1.42.1)
- See the Chart of Accounts section; `view()==='classifications'` removed, `go()` redirects to `overview`. All value-management methods unchanged.

## Segment Mapping — MERGED into Chart of Accounts Region 3 (v1.42.1)
- See the Chart of Accounts section; `view()==='mapping'` removed, `go()` redirects to `overview`. All mapping methods unchanged.

## Explorer — MERGED into Chart of Accounts (v1.42.0)
- The combinations explorer is now Region 2 of the Chart of Accounts page (see above); `view()==='explorer'` and its nav entry are removed. `loadCombos`/`exportCsv`/`comboRange` and the combo observables are unchanged.

## General Ledger (`view()==='actuals'`) — Budget vs Actual report (YTD per GL combination)
- full-viewport width (the `.wrap.wide` modifier is applied on Actuals + Dashboard).
- search criteria: `acPeriod` (**mandatory**, defaults to current period), `acSector`, `acChapter`, `acProgram`, `acAppr`, `acAccount`, `acCostCenter`, `acSource` (transaction-source — keep rows whose measure is non-zero; reactive list `acSources` = budget/commitment/obligation/**openCommitment**/**openObligation**/glActual/grn/apDirect), `acSearch` — populated by `loadAcFilters()` (`/actuals/filters`, now also returns `accounts` + `costCenters` LOVs).
- `runActuals()` — **one-shot loader (v1.57.0)**: page-merges `/actuals` (server clamp 1,000 rows/request) up to 10,000 rows client-side for the IR grid (`AC_MAX`/`AC_PAGE`, `acTruncated` note; the pager is gone); `acReset()` — clear filters, keep current period; a `.bu-load-ov` **oj-progress-circle busy overlay** covers the `.ac-body` wrapper (KPI band + register) on load and every search.
- **Report-parameters drawer (v1.23.0)** — the filter card moved OFF the page into a right-edge slide-in drawer (`.dw-filter`, min(720px, 94vw)): `openAcFilters()`/`closeAcFilters()`/`applyAcFilters()` (Apply = search + close; Reset re-runs and keeps the drawer open; Esc closes), page-head **Filters** button with an active-criteria count badge (`acActiveCount`, `.fbadge`), and an **applied-criteria chip bar** on the page (`.ac-chipbar` — period chip + one labelled chip per active filter via `acFilterChips`; click anywhere on it to open the drawer). **v1.39.0** — Sector/Chapter/DCT-Program/Appropriation/Account/Cost-Centre are **multi-select** (pick→chip, `ac*Sel` arrays + `acAddSel`/`acChipRemove`, pipe-delimited any-of request), plus a MANDATORY **Account Type** multi-select filter (`acAccTypes` 1=Assets…5=Owner's Equity; defaults to Expense, cannot be emptied, required marker on the label) and an **Account Type** column in the results table + CSV. Selected values render as a removable-chip tray in the drawer. **v1.57.0** — the six big LOVs (Sector/Chapter/DCT Program/Appropriation/Account/Cost Center) are **type-ahead `<input list>` datalist fields** (value = code, label = name; `acLovCommit` matches exact code → `code · name` → exact name → unique contains, then chips the pick; Enter or change commits); Account Type / Transaction Source / Period remain selects. **v1.59.0** — a **"Figures in" display-unit select** (the SHARED `buUnit`/`buUnitOpts` attribute from Budget Utilization, persisted in `gl_bu_ui.unit`): KPI band formats via `buNum`, the IR register scales money columns by the unit divisor + `(B/M/K)` label suffix in `acIr` (Auto/Exact = full numbers; drills/popover keep unscaled rows); display-only, instant.
- **Grouped KPI band (v1.23.0)** — the 14 flat `.kstat` tiles became **6 measure-group cards** (`.kgrid`/`.kg-*`): Budget (+ `acUtilTxt`/`acPctW` utilization bar), Commitment (PR) = Total + Open/Pipeline rows, Obligation (PO) = Total + Open/Pipeline rows, Open Encumbrance = total + Open PR/Open PO rows, GL Actual = total + GRN/AP Direct/SLA rows, Funds Available (GL) = total + Calc row + `acFundsTxt` remaining-% (red `.neg` when negative). Each card: accent spine + icon chip + Fraunces value + sub-rows.
- business-question answer cards (14): `tot(k)` reads the `totals` summary — budget, **Total PR** (`prTotal`), **Open Commitment** (`openCommitment`), **Commitment Pipeline** (`commitmentPipeline`), **Total PO** (`totalPo`), **Open Obligation** (`openObligation`), **PO Pipeline** (`poPipeline`), **Open Encumbrance** (`openEncumbrance`), GL actual, GRN actual, AP direct, SLA actual, **Funds Available (GL)** (`fundsAvailable`), **Funds Available (calc)** (`fundsAvailableCalc`).
- result register — **SHARED `<interactive-report>` since v1.57.0** (`reportCode GL_BUDGET_ACTUAL_IR`, section `ac`; envelope `acIr` pureComputed): flat columns replacing the old stacked `.grp-cell` table — frozen **Combination** (mono) + **Cost centre** columns, grouped header bands **Commitment (PR)** = Total/Open/Pipeline/PR-count, **Obligation (PO)** = Total/Open/Pipeline/PO-count, **GL Actual** = Actual/GRN/AP direct/SLA, **Funds available** = GL/Calc, per-group tints (`.acg-*`/`.acc-*`) + per-column ⓘ hints, zebra; the IR's own search / column show-hide / filters / multi-sort / CSV+XLSX / saved layouts all apply. Figure semantics unchanged: Commitment from `ATD_PR_*` (**Total** = Reserved+Liquidated / **Open** = Reserved / **Pipeline** = Not-reserved); Obligation from `po_distributions` (**Total** = all except Failed/Passed / **Open** = Reserved+Partially-Liquidated **GRN-netted** / **Pipeline** = Failed/Passed); **Open Encumbrance** = Open PR + Open PO; **AP Direct** = AP lines with `po_number IS NULL`; **SLA** = GRN + AP direct. Delegated wrapper handlers (`acGridOver`/`acGridMove`/`acGridClick`, `ko.contextFor`): the Combination cell shows the 10-segment popover (side map `acRowMap` recovers the full source row) and **every figure cell drills** (`AC_METRIC` column→metric map → `openDrill`).
- **KPI-card aggregate drill (v1.40.0)**: `openAcAgg(metric)` opens the shared `.dw-*` drill drawer with the supporting lines across the WHOLE filtered set (`GET /actuals/lines` with the filter set and no `cc` = aggregate mode; leads with Cost-centre/Account/Combination columns, capped 500 w/ `count` "top N of M", total reconciles to the KPI). Every KPI-band figure is drillable (`.kg-drill`). **v1.58.0** — `fillAcAgg` post-processor: Cost-centre column = `code - name` (resolved from the `/actuals/filters` LOV), rows guaranteed amount-descending + a **sort-criteria pill** above the table (`drillSortNote`/`.dw-sort`, cleared on close), and Combination cells show the 10-segment popover (delegated `drillGridOver/drillGridMove` on the drawer table — plain-KO cell context `$data`=col/`$parent`=row, source row via `acRowMap`).
- drill-down (per cell): `openDrill(row, metric)` (`/actuals/lines`, metric ∈ budget|commitment|opencommitment|commitmentpipeline|obligation|openobligation|popipeline|glactual|grn|apdirect) → wide modal; `closeDrill()`. **commitment/openCommitment/commitmentPipeline** → real PR lines (PR #, description, requester, funds status); **obligation/poPipeline** → PO lines; **openObligation** → PO lines with Ordered / Received (GRN) / Open columns (the netting made visible). Modal header carries an **Export CSV** button (`drillExportCsv()`, shown once lines are loaded).
- `acExportCsv()` — CSV of the full filtered set (≤5000 rows, incl. all 3-figure Commitment/Obligation + Open Encumbrance + both Funds Available); `acRange` — "loaded of total". The IR toolbar adds its own CSV/XLSX of the visible grid.

## Budget Utilization (`view()==='butil'`) — project budget vs actual (per task & expenditure type)
- full-viewport width (`.wrap.wide` also applied on this view). One row per **Budget Year × Project × Task × Expenditure Type**, budget > 0 lines only.
- page layout (v1.14.0) = three **brand-headed regions** (`.bu-sec`): **Search** (filters, collapsible), **Overview** (KPI band, collapsible), **Results** (table + pager, maximizable). `toggleBuSec(k)` collapses with a rotating chevron; state persists in `localStorage('gl_bu_ui')`; collapsed headers show live summaries (`buSearchSummary` = year + active-filter count via `buActiveFilters`; `buKpiSummary` = compact Budget · Fund). `toggleBuMax()`/`buMax` maximizes the Results region to a full-screen surface (z 55, below the drill drawer/modal/toast so drills open above it; sticky thead + pinned pager; body scroll locked; **Esc restores**, guarded while a drawer/modal is open).
- search criteria: `buYear` (**mandatory**, defaults to latest year), `buPeriod` (**Accounting period, v1.16.0** — Full year + the 12 MM-YYYY months of the selected year via `buPeriodOpts`; **YTD semantics** with a ⓘ hint; defaults to the **current period** when the year is current — first load, Reset and year-change), **Project Type = MULTI-SELECT since v1.60.0** (`buTypeSel` chips + `buTypePick`/`buTypeAdd`; `buType` is a computed returning the pipe-joined any-of list so all senders — butil/encumbrances/pending/report bridges — are unchanged; **defaults to the single `DCT OPEX Project Type` chip** on first load + Reset; server = INSTR any-of in GL/db/07/12/13/15 + the 4 report definitions), `buSector`, plus four **autocomplete LOV filters** (v1.12.0, native `<datalist>`): `buCc` (Cost Center — option label = department), `buProject` (Project — option value = number, label = name; contains-match on number OR name), `buTask` (Task), `buEtype` (Expenditure Type), and free-text `buSearch`. Select LOVs from `loadBuFilters()` (`/butil/filters`); autocomplete lists from `loadBuLovs()` (`/butil/lov?year=`, reloaded when `buYear` changes).
- **Business Unit multi-select** (`buBus`/`buBuSel`/`buBuAdd`, v1.34.0) — every budget line takes its PROJECT's Business Unit (projects master; real names since the 2026-07-18 extract fix); picks become chips on the shared mchips bar, sent as `bu=` pipe-delimited EXACT any-of on `/butil`, `/butil/lines`, `/encumbrances` and all three report bridges (Reset clears). Shared with the Projects Encumbrances page; the Pending Approval page keeps its own snapshot-document BU filter.
- `runButil(offset)` — run the report (`/butil`, server-paginated); `buReset()` (clears filters + all multi-select chips, restores the DCT OPEX default type); `btnSearch`/`btnReset` + Enter-to-search on every filter input; `yearRequired` toast when no year.
- **Multi-select Chapter / Cost centre / Project (v1.23.0)** — every Chapter pick (`buChapterPick`/`buChapterAdd`) and every exact Cost-centre/Project LOV pick (`buCcCommit`/`buProjCommit` on the datalist inputs' change/Enter) becomes a **chip** in a labelled bar under the filter grid (`.mchips-bar`/`.mchip`, × removes via `buChipRemove`); `buChapterSel`/`buCcSel`/`buProjSel` observableArrays. Requests send the chips **pipe-delimited** (`buChapterParam()`/`buCcParam()`/`buProjParam()` — a pipe list = exact any-of match server-side; a single free-typed pipe-less value keeps the original contains-match). Flows into `/butil`, the pager, CSV export, both drill modes AND the Briefing Book.
- **Period-aware Budget — Annual + YTD columns (v1.37.0)** — `ATD_PROJECTS_BUDGET` carries `ACCOUNTING_PERIOD` (MM-YYYY), so the view's `budget` = **YTD** (periods ≤ the selected Accounting Period; = annual on Full year) and new `budget_annual` = all periods. Results table shows BOTH columns, each drillable (`openBuDrill(d,'budgetannual'|'budget')` → drawer of the line's period records: period/amount/updated by/on); Budget KPI tile = duo tile (hero = YTD; rows **Annual Budget** / **YTD Budget** with YTD-of-annual % via `buYtdOfAnnual()`, aggregate drills `openBuAgg('budgetannual'|'budget')`; seg bar = YTD share of annual); CSV export gains Annual Budget. Fund Available and Utilization % are computed on the YTD budget. Server: `/butil` emits `budgetAnnual` (rows+totals); `/butil/lines` metrics `budget`/`budgetannual`.
- **Calculation Logic region (v1.36.0)** — fourth collapsible `.bu-sec` at the BOTTOM of the page (`buSecCalcOpen`, **collapsed by default**, state in `localStorage('gl_bu_ui').calc`, toggled via `toggleBuSec('calc')`): explains how every figure is computed from the search criteria — colour-coded formula pills (`.calc-formula`/`.cf-*`, palette matches the KPI band: Fund = Budget − (Actual AP + Actual GRN + Commitment PR + Obligation PO)), a 3-column per-figure table (what it counts / Accounting-Period YTD effect — annual budget NOT period-filtered; AP by invoice accounting date, GRN by accounted-else-transaction date, PR/PO by budget date, PO GRN-netted) and Budget-Year / YTD / Utilization-% notes. EN/AR, static content (no fetch).
- **"Figures in" display-unit selector (v1.24.0)** — Search-region field (`buUnit`: Auto B/M/K | Billions | Millions | Thousands | Exact number, i18n `fUnitL`/`unit*`) driving the unit-aware formatter `buNum(n)` used by the 4 KPI tiles, the duo breakdown rows, the collapsed-band summary (`buKpiSummary`) and all 6 results-table money columns. Display-only (instant re-render, no re-query); persisted in `localStorage('gl_bu_ui')`; exact tooltips + raw CSV unchanged.
- loading states (v1.15.0): while `/butil` runs the results tbody shows **8 shimmer skeleton rows** (`skRows`/`skCols`, `.sk-row`/`.sk`), an indeterminate `.ldbar` sweep under the Results header, and the Search button goes `.busy`; every other load slot app-wide (Actuals/Dashboard/Classifications/Explorer/drill drawers/modal) uses the **branded `.ldr` loader** (GL diamond in twin counter-rotating arcs + i18n `loadingData` text).
- totals **answer band** (**4 tiles since v1.23.0** — `.bu-kpis`/`.bk-*`, scoped to this view) via `buTot(k)`: **Budget** | **Total Actual** (`buActualTot` = actualAp + actualGrn) | **Total Encumbrance** (`buEncumbTot` = commitmentPr [open PR] + obligationPo [open PO, GRN-netted]) | **Fund Available** (= Budget − AP − GRN − Open PR − Open PO). The two composite tiles (`.bk-duo`) show a **stacked composition bar** (`.bk-seg`, `buSegW(part,total)`) + a `buTotPctTxt` %-of-budget line + two **drillable breakdown rows** (`.bk-duo-row`: AP/GRN and PR/PO with colour dots and per-component amounts + a bracketed **%-of-budget suffix** per row via `buPartPct(v)` [v1.23.1] — click drills that component via `openBuAgg`). Each tile = per-measure gradient tint + accent spine + icon chip + Fraunces value + **%-of-budget context line** + **mini utilization bar** (`buPct(k)`/`buBarW(k)`/`buPctTxt(k)`; Budget tile shows the filtered row count, Fund tile uses `buFundSub` — "% of budget remaining", or red `.neg` "over budget" when negative). The 4 measure tiles (`.bk-click`) drill to all supporting lines; responsive 6→3→2→1 columns.
- result table (17 cols): Type / Sector / Department / Cost centre / Project (number + name) / Task / GL Account / Appropriation / Chapter / Program / Expenditure type + the 6 measures. Attributes resolve task-first (task `COST_CENTER`→SECTOR map, task `APPROPRIATION`→CHAPTER map, task `PROGRAM`), then transactions, then project.
- `buExportCsv()` — CSV of the full filtered set (≤5000 rows); `buRange` — “a–b of total”.
- **Missing-Cost-Centre red alert band (v1.43.0)** — `.bu-alert` under the page head, shown ONLY when the current filtered set has budget lines (annual budget ≠ 0) with **no cost centre** (`buMissCc`/`buMissCcBudget` from `/butil`'s `missingCc`/`missingCcBudget` aggregates, recomputed on every search); message = count + annual-budget sum + the fix (maintain the Cost Center task attribute in Fusion); the whole band click-drills via `openBuMissCc()` → shared drawer over `/butil?nocc=Y` (same filters, ≤5000 rows: Project/Name/Task/Etype/Department/Appropriation/Annual Budget; total reconciles to `missingCcBudget`, CSV export included). Browser smoke `tests/butil_misscc_browser.py` 14/14 EN+AR.
- drill-down (two entry points, one **right-edge slide-in drawer** — `.dw-*`, mirrors the platform edit-drawer; brand-themed header + eyebrow + context; `closeDrawer()`):
  - `openBuDrill(row, metric)` — the four money **table cells** (`.money-cell`) drill that single budget line (`/butil/lines?year=&project=&task=&etype=&metric=`); eyebrow = project·name, context = task·expenditure type.
  - `openBuAgg(metric)` — the four **KPI cards** (`.kstat-click`: Actual AP / Actual GRN / Commitment PR / Obligation PO) drill **all** supporting lines across the current filter set (`/butil/lines?year=&metric=&projecttype=&sector=&search=&costcenter=&fproject=&ftask=&fetype=` — the LOV filters travel f-prefixed so they don't collide with the row-drill key params); eyebrow = "All budget lines · <year>", context = active filters, adds Project/Task columns, and a `drillCapNote` "showing top N of M" when capped at 1000.
  - metric ∈ ap|grn|pr|po; totals always reconcile: **ap**→AP invoices (invoice #, date, vendor, currency, invoice amount, distribution AED, validation, payment, description), **grn**→GRN receipts (receipt #, date, currency, rate, amount AED; rows sorted by GRN # then line — cap selection stays top-by-amount; **v1.46.0 register parity**: + Project name/Expenditure type (aggregate mode) and per-PO-distribution Invoiced (AED) / Uninvoiced (AED) / **Related Invoices** comma-separated list — same columns as the BUDGET_UTIL_REGISTER GRN sheet; Amount (AED) stays last so the total footer reconciles; **v1.47.0**: every related-invoice number is its own Fusion deep-link via `relatedInvLinks(row)` + the hidden `relatedInvPairs` number~id field), **pr**→open-commitment PR lines (PR #, description, budget date, currency, line amount AED), **po**→open-obligation PO lines GRN-netted (PO #, line, budget date, vendor, funds status, PO amount, line amount AED, Open AED). Reuses the shared drill observables (`drillCols`/`drillRows`/`drillTotalV`/`drillCount`/`cell`).
  - `drillExportCsv()` (v1.11.0) — **Export CSV** button in the drawer header (and the Actuals drill-modal header): serializes the loaded `drillCols`/`drillRows` (server-driven columns, raw values, quote-escaped) + a Total reconciliation footer row; UTF-8 BOM for Excel-safe Arabic; filename `gl_drill_<title>_<sub>.csv`; hidden while loading / when no lines.
  - `drillLink(row, col)` (v1.20.0) — **Fusion deep-links** in the drawer: Invoice/PO/PR numbers render as new-tab anchors (`.fus-link`, ↗) via the SHARED `shared/js/fusionLinks.js` (UMD → `window.FusionLinks`; loaded by index.html before app.js) when the row carries the FUSION internal id — `invoiceId` (ap), `poHeaderId` (grn/po), `prHeaderId` (pr) from `/butil/lines`; plain text otherwise. CSV export keeps numbers only.
- **Generate Report dropdown (v1.32.1)** — the Excel + Briefing Book page-head buttons are consolidated into ONE **"Generate Report ▾"** split button (`genOpen`/`toggleGen`/`closeGen`, `.gen`/`.gen-menu`/`.gen-item` in app.css; click-outside `.gen-back` closes) offering **Briefing Book (PDF)** → `runBuBook`, **Excel Register (XLSX)** → `runBuXlsx`, **PowerPoint (PPTX)** → `runBuPpt`. The button shows a "Generating…" busy label while any of the three runs (`buGenBusy` computed). **Export CSV** (client-side grid export) / Rebuild views / Refresh actuals stay separate.
- `runBuXlsx()` — **Excel Register** (dropdown, v1.30.0, internal analysis): generates the Reporting-Platform `BUDGET_UTIL_REGISTER` workbook (reporting/db/25 — 6 worksheets: utilization lines, direct AP invoices, GRN receipts, open purchase orders, open requisitions, pending-approval PR/PO queue) with the page's full filter set; `POST /butil/xlsx` → 5-s poll → auto-download `Budget_Utilization_Register_<year>.xlsx`.
- `runBuPpt()` — **PowerPoint** (dropdown, v1.32.1): generates an executive PowerPoint deck of the same data as the Briefing Book — enqueues `BUDGET_UTIL_BOOK` with `formats='PPTX'` (reporting/runner/render_pptx.py: cover + KPI overview + utilization-by-sector chart+table + budget composition + lines under pressure + actuals & supplier concentration + open obligations/commitments + observations & insights + methodology, native editable slides). `POST /butil/ppt` → 6-s poll (`GET /butil/ppt/:id`) → authed download `Budget_Utilization_Briefing_<year>.pptx`; `buPptBusy` drives the busy state.
- `runBuBook()` — **Briefing Book** button (page head; **v1.22.0: sends ALL page filters**; **v1.30.0: the book gained Part 5 "Pending Approvals — PR & PO Queue"** + an Approval-queue insight, Observations renumbered Part 6): generates the Reporting-Platform `BUDGET_UTIL_BOOK` PDF for the page's **full filter set** — Year, Period (YTD), Project type, Sector, Chapter, Cost center, Project, Task, Expenditure type, Search — with the page's exact predicate semantics, so the book scope always matches the page (the cover prints every applied filter). Flow: `POST /butil/book` (GL-gated bridge, `11_gl_butil_book_ords.sql`) → toast "queued — run #N" → 6s polling of `GET /butil/book/:id` (≤ ~6 min) → authed blob download of `GET /butil/book/:id/pdf` as `Budget_Utilization_Briefing_Book_<year>.pdf`; `buBookBusy` drives the "Preparing book…" label. The run is rendered by the normal reporting worker fleet (also runnable from the BI app).

## Projects Encumbrances (`view()==='encumbrances'`) — open PO/PR encumbrance lines, full GL combination (v1.25.0)
- **"Open Projects Encumbrance Follow-up"** — every OPEN encumbrance line (Open Commitment PR + Open Obligation PO, GRN-netted) for the budget lines matching the Budget Utilization scope, exploded to the FULL GL combination (all ten segments, **code + name**) plus sector/chapter names. Rendered in the **SHARED `<interactive-report>` component** (the same APEX-IR grid AP/BI use): column show/hide/reorder/rename, filter chips, multi-sort, calculated columns, aggregates, control breaks, highlight rules, CSV + SheetJS XLSX export, and per-user localStorage layouts (`reportCode:'GL_PROJ_ENCUMBRANCES'`, `layoutsApi:null`).
- **Search criteria are the SAME as Budget Utilization** — the tab reuses the `bu*` filter observables (`buParams`) and the shared `bu-*` autocomplete datalists verbatim (Year req, Period YTD, Project type, Sector, Chapter[multi], Cost centre[multi], Project[multi], Task, Expenditure type, Search); the Search button runs this tab.
- `runEncumbrances()` — one-shot capped fetch (`GET /encumbrances`, limit 10 000) → builds the IR envelope (`enData`) from the server's `columns[]`+`items[]`. `enCount` / `enOpenTotal` (+ `enOpenTotalTxt`) / `enTruncated` drive the summary line; the **Open total reconciles to the butil Total Encumbrance** (Commitment PR + Obligation PO). Auto-runs on first open (via `go('encumbrances')`), re-runs on Search.
- `enIrError` — set from `window._irLoadError` if the shared component failed to load (index.html requirejs boot); the tab then shows a graceful notice instead of the grid.
- IR enablement: GL (a portal-style app) bootstraps requirejs in `index.html` (shim `knockout`→the existing global, `shared`/`text`/`xlsx` paths), requires `shared/components/interactiveReport` (+`shared/editDrawer`, `shared/i18n`) so the `<interactive-report>`/`<edit-drawer>` components register on the app's KO before `app.js` boots; `toggleLang` also flips `shared/i18n` so the grid re-renders in AR. `platform.css` is linked **before** `app.css` (portal styles still win) to supply the IR base classes; the IR var bridge in `app.css` themes the grid green.

## Encumbrances – Pending Approval (`view()==='pending'`) — PR/PO documents awaiting approval (v1.27.0)
- **"Encumbrances – Pending Approval Follow-up"** — every PR / PO document currently PENDING APPROVAL in Fusion (daily BIP snapshot `atd_pr_po_pending_approval`, otbi-atd/db/47) joined to the open-encumbrance line detail (project / task / expenditure type, budget date, AED amounts, full 10-segment GL combination code+name) PLUS the approval trail (preparer/buyer, submitted date, pending-with approver, days pending, business unit, funds status). Data = `PROD.DCT_PR_PO_PENDING_V` (db/v2/52). **Zero-value lines are excluded (v1.30.1)** — the page follows budget utilization; a zero line never reserves funds (the reserved/not-reserved split stays visible; book + Excel register are additionally reserved-only).
- **Search criteria are the SAME as Budget Utilization** — reuses the `bu*` filter observables + shared `bu-*` datalists verbatim; the Search/Reset buttons run this tab (`runPending()`).
- **Monitoring KPI band** (server-aggregated over the FULL filtered set, correct even when the register is capped): Pending documents (PR/PO split + seg bar) · Pending amount AED (PR/PO split) · **Funds reserved vs not-reserved** (reserved = already inside the butil open-encumbrance figures; not-reserved = would hit Fund Available on approval) · **Approval aging** (avg days, oldest, over-30-days docs+amount, red flavour when any are stalled). **Every tile breakdown row DRILLS (v1.30.0)** — `openPnDrill('pr'|'po'|'reserved'|'unreserved'|'over30'|'within30')` opens the SHARED right-edge drill drawer with the matching pending lines (client-side slice of `pnItems`, cap 1,000 w/ top-N note, reconciling total, CSV export; Document # cells deep-link to Fusion via the generic `drillLink` docNumber rule).
- **Aging analysis + Top pending approvers mini tables** (`pnAging`/`pnApprovers` — buckets 0-7/8-15/16-30/31+ at document grain; top 8 approvers by value held w/ doc count + max days — the follow-up chase list). Rows drill too (`openPnBucket`/`openPnApprover`, v1.30.0); headers are **#79C5AC region-header bands** (user-specified).
- **Busy overlay** (v1.30.0) — the butil oj-progress-circle replica over the KPI band + Results while `/gl/pending` runs (`pnLoading` on the `.bu-body` wrapper).
- **Extract-coverage note** (`pnUnmatchedTxt`) — snapshot documents with no matched project budget line (`in_extract='N'`) are counted, never silently dropped; the Briefing Book annex lists them.
- Register in the **SHARED `<interactive-report>`** (`reportCode:'GL_ENC_PENDING'`, `section:'pend'`, cap 10 000, default sort days-pending desc) with the same GL-combination hover popover as the encumbrances tab; `pnIrError` fallback notice.
- **Source filter** (`pnSource`, v1.28.0) — All / PR / PO dropdown in the Search region (page-local, cleared by Reset; `source=` on `/pending`) + **"Showing figures in"** (the shared `buUnit` selector — KPI tiles and mini-table amounts format via `buNum`).
- **Business Unit multi-select** (`pnBus`/`pnBuSel`/`pnBuAdd`, v1.31.0) — dropdown fed by the `businessUnits[]` LOV in the `/pending` response; each pick becomes a chip on the shared mchips bar (Reset clears). Sends `bu=` pipe-delimited EXACT any-of list, scoping the register, every KPI/aging/approver aggregate AND the unmatched extract-coverage KPI; forwarded to both the Briefing Book and the Excel register. The BIP snapshot is the platform's only cross-BU source (the OTBI extracts are DCT-scoped), which is why the filter lives on this page.
- **Fusion deep-links on Document #** (`pnGridOver`/`pnGridClick`, v1.28.0) — hovering a docNumber cell shows the link affordance (`.pn-doclink` + tooltip), clicking opens the Fusion PR/PO deep link in a new tab via the shared `fusionLinks.js` builders over `fusionHeaderId` (kept in a VM side-map keyed source|doc# — the shared IR grid drops undeclared row fields). Same delegated ko.contextFor pattern as the combination popover; no shared-component change.
- `runPnBook()` — **Briefing Book** button → `POST /pending/book` with the full page filter set, 6-s poll (`GET /pending/book/:id`), auto-download of the PDF (`ENC_PENDING_BOOK`, reporting/db/23 — **funds-reserved non-zero lines only**, registers carry Sector/Cost centre/Appropriation code+name, full untruncated text).
- `runPnXlsx()` — **Export Excel** button (v1.29.0, internal analysis) → `POST /pending/xlsx` with the full page filter set + source, 4-s poll (`GET /pending/xlsx/:id`), auto-download of `Encumbrances_Pending_Approval_Register_<year>.xlsx` (`ENC_PENDING_REGISTER`, reporting/db/24 — sheet 1 = the 29-column flat pending PR/PO register on the book scope rule; sheet 2 = the extract-coverage annex).

## Dashboard (`view()==='dashboard'`) — executive analytics
- `dashPeriod` selector → `loadDashboard()` (`/dashboard`); KPI strip via `kpis()` (budget, actual, funds, encumbrance, PO total & count, utilisation/commitment %).
- `gauge()` — utilisation radial gauge (SVG); `trend()` — period-over-period actual SVG area+line; `sectorBars`/`programBars`/`apprBars` — horizontal bar charts (actual / PO commitments); `insights()` — auto-generated executive sentences. All charts are hand-built SVG/CSS (no chart library).

## Refresh (Overview + Actuals + Dashboard) & Rebuild (Overview + Budget Utilization)
- `refreshActuals()` — `POST /actuals/refresh` safely rebuilds `DCT_GL_COA_SNAP` through validated staging so the report reflects the latest GL/ATD data + mapping edits; concurrent runs are refused, the prior committed snapshot survives any load/validation failure, and each run records counts/duration/error in `DCT_ACTUALS_REFRESH_LOG` (90-day retention). Also runs hourly (`DCT_ACTUALS_REFRESH_JOB`, db/v2/35 + resilient procedure db/v2/118) and is mirrored as a button in the ATD app (`POST /atd/actuals/refresh`).
- `rebuildViews()` — **Rebuild views** button (Overview + Budget Utilization page heads, tooltip hint + confirm): `POST /actuals/rebuild` → `prod.dct_views_rebuild` (db/v2/38). For **structural** reloads (new/renamed columns): re-creates the 16 `SELECT *` base pass-through views over the `ATD_*` tables, recompiles INVALIDs, refreshes the snapshot; toast reports any views still invalid (those need a script edit). For a plain data reload, Refresh actuals is enough.

## Shell
- `t()` / `toggleLang()` (EN/AR + RTL), `go(view)`, `signOut()` (→ Admin), `refreshFilters()`.
- formatters: `money(n)` (full AED with separators), `compact(n)` (1.71B / 868.0M / 12K), `fmt(n)`.

## Reconciliation (`view()==='recon'`) — Actuals ↔ Budget Utilization data-integrity (v1.41.0)
- **Purpose** — verifies data integrity between the Actuals and Budget Utilization dashboards. Consumption (AP/GRN/PR/PO) is the SAME source both sides, so it reconciles to residual 0 via named leakage buckets; budget = two different Fusion ledgers (GL `GL_BALANCES` Expense scoped vs PPM `PROJECTS_BUDGET`) shown side by side. Data = `DCT_GL_RECON_FACT_V` + `DCT_GL_RECON_GLBUDGET_V` (db/v2/105).
- **Filters** — Budget Year (req) + Accounting Period (YTD, sets `BUTIL_END` so both sides align) + multi-select Sector / Chapter / GL Account / Cost Centre / Appropriation / DCT Program (`rcSector/rcChap/rcAcct/rcCc/rcAppr/rcProg`, each a `rcMulti()` picklist→chips) + **GL budget chapters** (`rcBChap`, Expense scope, default CH2–CH5). `runRecon()` fetches summary + rows; `rcReset()`.
- **Status band** — 5 KPI tiles: Coverage % (butil/actuals) + total consumption both sides · **Non-project (Actuals only)** = spend on combinations with no project/task/etype (can't enter Budget Utilization) · **Orphan consumption** = spend on a project line with no budget (the actionable flag) · GL vs Project budget · Fund available each side. Charts (hand-built SVG/CSS): `rcDonut` composition donut (Matched / No-project / Not-validated / Orphan via conic-gradient) + `rcMeasures` per-measure Actuals-vs-Butil bars with status dots. Every tile figure has an ⓘ info hint (`hRc*` keys) and is a **drill link** (v1.42.4).
- **KPI drill-down (v1.42.4)** — every KPI tile figure opens the SHARED drill drawer with the full supporting detail:
  - Coverage / Non-project / Orphan → `rcKpiDrill(bucket,titleKey)` calls `/recon/drill?measure=all` (cross-measure AP+GRN+PR+PO source documents, with a **Source** column; `bucket`=matched/no_project/no_budget_line). Reconciles to the tile total to the riyal.
  - GL / Project **Budget** and GL / Project **Fund** sub-values → `rcBudgetDrill(side,focus,titleKey)` calls `/recon/budget?side=gl|ppm` (budget-line detail = Budget · Consumed · Fund per project-line [ppm] or per GL combination [gl]; `focus`=budget|fund picks the reconciling drawer total). GL side FULL-OUTER-JOINs consumption so un-budgeted-combination spend surfaces as negative-fund rows and GL Fund reconciles to the tile.
- **Register** — measure tabs (`rcSetMeasure` AP/GRN/PR/PO) + grain segmented control (`rcSetGrain`, 8 grains); table shows per-row Actuals/Butil/Diff + Non-project/Validation(AP)/No-budget-line, every leakage cell a drill link. Column headers carry ⓘ hints. `rcExportCsv()`.
- **Difference drill** — `rcCellDrill(row,bucket)` → `rcDrill(measure,bucket,row)` calls `/recon/drill` and opens the SHARED drill drawer (`drillCols/drillRows/drillTotalV/drillCount`) with the source documents (doc #/line, project/task/etype, amount, hasProject/validation/onBudgetLine). Verified: every drill total reconciles to its summary bucket.

## Legacy (EBS) (`view()==='legacy'`) — Fusion/EBS COA mapping + historical balances (v1.48.0)
- `runEbsMap()` — loads the COA mapping register (GET /coamap; segment/status/search filters) into the SHARED `<interactive-report>` (client-built columns)
- `xmGridClick(d,e)` — delegated row click → edit drawer (admins; `ko.contextFor(td)` + segment|ebsValue side-map)
- `openXmNew()` / `openXmEdit(row)` / `closeXmDrawer()` / `saveXm()` — add/edit drawer (POST /coamap, partial PUT /coamap/:id; deactivate instead of delete)
- `uploadEbs()` / `ebFileChosen(d,e)` — EBS balance Excel upload: SheetJS client parse (synonym-priority header map w/ column claiming — accepts the full EBS "GL Period Balances" export layout incl. CC_ID/descriptions/Account Type; bare `Budget` = amount when `Budget Group Code` present), chunks of 500 → POST /ebs-balances with per-chunk progress
- `ebTemplate()` — downloads the EBS balances upload template workbook (SheetJS)
- `loadEbsSummary()` — per-year coverage tiles + top-unmapped lists (GET /ebs-balances/summary)

### Balances YoY (`#pg-yoy`, 2026-08-02; interactive report since v1.55.0)
- `runYoy()` — loads the year-over-year comparison (GET /ebs-balances/yoy) for the selected year chips (max 6) + Balance-as-of month
- `yoToggleYear(y)` / `yoMonthLabel(o)` — criteria helpers (year chips, Full year / month YTD labels)
- `yoView` / `yoPivot` — client-side pivot (one row per Fusion account, values per year) + live search/type filtering
- `yoIr` — SHARED `<interactive-report>` envelope (`GL_BAL_YOY`): year columns ASCENDING, dynamic "Change YY-YY" / "Change % YY-YY" headers for the two latest selected years, per-column ⓘ hints, trailing **Chart** column (shared IR `spark` type — inline sparkline, hover = cross-year trend chart popover); rebuilds on run/measure/search/type; **v1.56.0 layout round**: alternating per-year column tints (`yoc-a`/`yoc-b`), Change/Change % on a gold tint (`yoc-chg`) with ▲ green / ▼ red delta arrows (shared IR `column.delta`), zebra rows (envelope `zebra:true`)
- `yoMeasure` + `yoVal/yoValTxt/yoChange/yoChangeTxt/yoChangePct/yoTotal` — measure toggle (Actual/Budget/Encumbrance) + delta vs previous year + totals
- `yoCsv()` — CSV export of the visible pivot (ascending years, selected measure, totals row)
- `runYoyXlsx()` — Export Excel Register: enqueue EBS_GL_YOY_REGISTER via POST /ebs-balances/yoy/xlsx, poll, auto-download
- `runEbsRegister()` — enqueues EBS_GL_BALANCE_REGISTER via the bridge, polls, auto-downloads the XLSX

## Cashflow (`view()==='cashflow'`) — budget cashflow plan uploads (v1.49.0)
- `loadCfSummary()` — GL + Projects per-year/type coverage tables + top unmapped appropriations (GET /cashflow/summary)
- `uploadCfGl()` / `cfGlChosen(d,e)` — GL cashflow Excel upload (10 segments + period + CF type + amount): SheetJS client parse (fuzzy header map), chunks of 500 → POST /cashflow with per-chunk progress
- `uploadCfPj()` / `cfPjChosen(d,e)` — Projects cashflow Excel upload (project/task/etype grain) → POST /cashflow/projects
- `cfGlTemplate()` / `cfPjTemplate()` — download the upload template workbooks (SheetJS)

## DOF Submissions (`view()==='dof'`) — YoY / Budget Utilization / Quarterly (v1.49.0, reworked v1.52.0)
- `runDof()` — loads the selected dataset (GET /dof/yoy | /dof/butil | /dof/quarterly; Year **2016..current+1** + optional Period YTD-end) into the SHARED `<interactive-report>`; YoY includes chapter + grand total rows; cashflow-missing hint when the plan is not loaded (suppressed for EBS-era years < 2026, which show the era note instead); **column headers carry the run year** ("Revised Budget 2026", "Actual FY 2025"…) via `dofColsYoy/Butil/Quarterly(yr)` + `{y}`-substituted i18n keys, and the figure columns declare **`hint`** (ⓘ hover popover, v1.53.0 — generic `column.hint` support added to the shared IR component) explaining FY-vs-YTD prior actuals, the variance formula and cashflow/utilization semantics EN+AR
- **v1.56.0 layout round** — columns ordered in YEAR BLOCKS (frozen Chapter/Appr/Account code columns via shared-IR `column.sticky` → current-year block → prior-year block → Variance → Reasons) under **grouped FY header bands** (`column.group`; butil = Budget/Performance bands, quarterly = Budget + Quarter 1–4 bands); current-year columns brand-tinted / prior-year grey / quarters alternating (`dofc-*` colClass in app.css); Variance columns carry ▲ green / ▼ red delta arrows; long texts one-line ellipsis w/ hover full text; zebra rows
- `dofEmit()` + `dofUnit` / `dofZero` — display-only Search parameters re-projecting the CACHED run (no re-query): **"Showing figures in"** (AED exact / Thousands / Millions / Billions — money values scaled in the envelope) and **"Near-zero display"** (Dimmed 0.00 / Dash / Blank — shared-IR `column.nearZero`, threshold = rounds-to-zero at the current unit); both persisted in `gl_dof_ui`
- **v1.56.1** — identity columns MERGED (Appropriation / Account / Entity = "code — name"; `apprFull`/`accountFull`/`entityFull` computed on ingest, side-map + drawer keyed on them); envelopes carry `stateRev: 2` (shared-IR hook — stale saved column order yields to the year-block default)
- `dofChSel` / `dofApSel` + `dofChAdd` / `dofApAdd` / `dofApprOpts` / `dofApLabel` — **multi-select Chapter + Appropriation criteria** (butil chips pattern, any-of; Appropriation LOV scoped to picked chapters; applied client-side in `dofEmit` — whole-chapter picks keep server CHTOTAL rows, appropriation picks get `dofGrandOf()` recomputed grand w/ ratios recomputed)
- `toggleDofSec(k)` / `dofSearchSummary()` / `dofResSummary()` / `dofReset()` — butil-pattern collapsible Search + Results regions (state in `localStorage('gl_dof_ui')`), collapsed-state summaries, criteria reset
- `dofGridClick(d,e)` — delegated row click → Reasons/Remarks drawer (admins; `ko.contextFor(td)` + apprCode[|accountCode] side-map; YoY totals not clickable)
- `openDofNote(row)` / `closeDofDrawer()` / `saveDofNote()` — notes drawer (yoy/butil = one Reason; quarterly = 4 quarter Reasons + Remark; PUT /dof/notes per field, empty text deletes; saved notes pre-fill every future run); since v1.56.0 the drawer opens with a **Record details grid** (`dofNInfo` — every dataset column of the clicked row) above the note fields; v1.56.2 (2026-08-04 (3)): the grid mirrors the TABLE format — unit scaling, 2-decimal money, near-zero mode, ▲/▼ colored variance
- `runDofRegister()` — Generate Workbook (XLSX): enqueues DOF_YOY_PERF (yoy + butil — one workbook, two sheets) or DOF_QUARTERLY_PERF via POST /dof/register, polls, auto-downloads

## API Endpoints (ORDS) — `db/05_gl_ords.sql`, base `/ords/admin/gl/`
| Method | Path | Purpose |
|---|---|---|
| GET | `/recon/filters` | Reconciliation LOVs — years, periods, budgetChapters default (CH2..CH5) + code+label dimension lists (db/14) |
| GET | `/recon/summary` | per-measure AP/GRN/PR/PO {actuals, butil, diff, noProject, apValidation, noBudgetLine} + derived totals + GL vs PPM budget + fund available each side (db/14) |
| GET | `/recon/rows` | reconciliation register at `?grain=` (account/sector/chapter/costcenter/appropriation/program/combination/budgetline) + optional `?measure=`; one row per dimension value with both sides + leakage buckets (db/14) |
| GET | `/recon/drill` | source records behind a difference cell — `?measure=ap\|grn\|pr\|po\|all&bucket=&grain=&key=` (bucket ∈ all/no_project/ap_validation/no_budget_line/matched; `measure=all` = cross-measure KPI drill, adds a Source column); generic doc rows + hasProject/validation/onBudgetLine flags (db/14) |
| GET | `/recon/budget` | budget-line detail behind the Budget & Fund KPI tiles — `?side=gl\|ppm`; ppm = PROJECTS_BUDGET project-lines, gl = GL_BALANCES per-combination (FULL-OUTER-JOINed to consumption); returns Budget/Consumed/Fund + `{total,consumedTotal,fundTotal,count}` (db/14) |
| GET | `/coamap` | EBS mapping register (`?segment=&search=&active=`, cap 5000) + segments LOV (GL/db/16) |
| POST | `/coamap` | create mapping row (GL_MANAGE_EBS_MAPPING / SYS_ADMIN; dup → 400) (GL/db/16) |
| PUT | `/coamap/:id` | partial update / deactivate mapping row (GL/db/16) |
| POST | `/ebs-balances` | bulk upsert EBS balance rows (≤500/req, per-row results, upsert on 7 segments + period; measures ptd/budget/encumbrance all optional →0; optional ccId/accountType/per-segment *Desc; adjustment period `13-YYYY` accepted, dated 31-Dec) (GL/db/16) |
| GET | `/ebs-balances/summary` | per-year coverage (rows/periods/combos/actual+budget+encumbrance totals/mapped) + top unmapped EBS accounts + Future2 values (GL/db/16) |
| POST | `/ebs-balances/register` | enqueue EBS_GL_BALANCE_REGISTER (year req) (GL/db/16) |
| GET | `/ebs-balances/register/:id` | poll register run status (GL/db/16) |
| GET | `/ebs-balances/register/:id/file` | download the register XLSX (GL/db/16) |
| GET | `/ebs-balances/yoy` | year-over-year YTD comparison on the Fusion account basis (years= pipe list max 6, month=0 FY or 1-12 YTD cutoff, search/atype/chapter; EBS years = stored YTD slices via the account map, 2026+ = Fusion fact view) (GL/db/18) |
| POST | `/ebs-balances/yoy/xlsx` | enqueue EBS_GL_YOY_REGISTER with the same params (GL/db/18) |
| GET | `/ebs-balances/yoy/xlsx/:id` | poll YoY register run status (GL/db/18) |
| GET | `/ebs-balances/yoy/xlsx/:id/file` | download the YoY register XLSX (GL/db/18) |
| POST | `/cashflow` | bulk upsert GL budget-cashflow rows (≤500/req, per-row results; segments zero-padded to canonical widths; GL_MANAGE_CASHFLOW / SYS_ADMIN) (GL/db/17) |
| POST | `/cashflow/projects` | bulk upsert Projects cashflow rows (project/task/etype grain) (GL/db/17) |
| GET | `/cashflow/summary` | GL + Projects per-year/type coverage + top cashflow appropriations without a chapter (GL/db/17) |
| GET | `/dof/yoy` | DOF YoY dataset — `?year=(req)&period=` (MM-YYYY YTD end, default latest); account grain (4xxxxx only) + EBS codes; **any loaded fiscal year 2016+** — current AND prior legs = Fusion ∪ legacy-EBS (bg-1, STORED YTD slices) + chapter/grand totals + reasons (GL/db/17) |
| GET | `/dof/butil` | DOF Budget Utilization dataset — appropriation grain: initial/revised budget FY, APPROVED/REVISED cashflow YTD, actual YTD, variance, utilization %, reasons; EBS-era years: Initial = Revised (one EBS budget measure); chapter/grand total rows via GROUPING SETS (`rowType` DETAIL/CHTOTAL/GRAND) (GL/db/17) |
| GET | `/dof/quarterly` | DOF Quarterly dataset — appropriation grain: approved budget, revised budget Q1/Q2, per-quarter approved/revised CF vs actuals + variance/% + quarterly reasons + remarks; EBS-era years via stored-YTD quarter slices; chapter/grand total rows (`rowType`) (GL/db/17) |
| GET | `/dof/notes` | saved Reasons/Remarks notes for a year (GL/db/17) |
| PUT | `/dof/notes` | upsert one note by natural key (year/entity/appropriation[/account][/quarter]/noteType; empty text = delete; GL_MANAGE_DOF_NOTES / SYS_ADMIN) (GL/db/17) |
| POST | `/dof/register` | enqueue DOF_YOY_PERF / DOF_QUARTERLY_PERF (`{report:YOY\|QUARTERLY, year, period?}`) (GL/db/17) |
| GET | `/dof/register/:id` | poll DOF report run status (GL/db/17) |
| GET | `/dof/register/:id/file` | download the DOF workbook XLSX (GL/db/17) |
| GET | `/boot` | dimensions catalog + combination/classified counts |
| GET | `/class-types` | dimensions |
| GET/POST | `/class-values` | list (by `?type=`) / create classification value |
| PUT/DELETE | `/class-values/:id` | update / delete value |
| GET | `/segments/:key/values` | distinct segment values + description (picker; `?search=&limit=`) |
| GET/POST | `/mappings` | list (`?type=&segment=&valueid=`; each row carries `segmentDesc` from the GL_SRC_* dimension) / create assignment (overlap-validated). GET upgraded by `09_gl_class_drill_ords.sql` (ADDITIVE, DEFINE_HANDLER-only; source synced into 05 so an 05 re-run already carries it) |
| PUT/DELETE | `/mappings/:id` | update / delete assignment |
| GET | `/combinations` | unified COA view, paginated, `?search=&sector=&chapter=&program=&asof=` |
| GET | `/combinations/:ccId` | single combination |
| GET | `/balances` | budget balances enriched with Sector, `?asof=&sector=` |
| GET | `/actuals/filters` | report LOVs in one call: periods (+default), sectors, chapters, programs, appropriations, **accounts**, **costCenters** |
| GET | `/actuals` | Budget-vs-Actual per combination for one period (YTD), `?period(req)=&sector=&chapter=&program=&appropriation=&account=&costcenter=&accounttype=&source=&search=&limit=&offset=`; **sector/chapter/program/appropriation/account/costcenter/accounttype accept pipe-delimited any-of lists** (multi-select, v1.39.0); `accounttype` filters on the leading account digit (1=Assets…5=Owner's Equity); each item carries `accountTypeCode`; totals + items include `prTotal`, `openCommitment`, `commitmentPipeline`, `totalPo`, `openObligation` (GRN-netted), `poPipeline`, `openEncumbrance`, `fundsAvailable` (GL), `fundsAvailableCalc`, `slaActual`, `poCount`/`prCount` |
| GET | `/actuals/lines` | drill-down lines behind a figure, `?period=&cc=&metric=` (budget\|commitment\|opencommitment\|commitmentpipeline\|obligation\|openobligation\|popipeline\|glActual\|grn\|apDirect); commitment/openCommitment/commitmentPipeline→real PR lines (`pr_*`), obligation/poPipeline→PO lines, openObligation→PO lines GRN-netted (Ordered/Received/Open); **apDirect** shows the real AP Invoice # + Line (not `invoice_id`), zero-amount lines suppressed; **grn** shows GRN # + Line (`10_gl_actuals_lines_docnum.sql`, ADDITIVE DEFINE_HANDLER-only, source synced into 05) |
| GET | `/dashboard` | executive analytics for a period: KPIs + by sector/program/appropriation + trend, `?period=` |
| POST | `/actuals/refresh` | rebuild `DCT_GL_COA_SNAP` + recompile INVALID PROD views (manual; same proc as the hourly job) |
| POST | `/actuals/rebuild` | structural-reload recovery: re-create the 16 base pass-through views + recompile + refresh snapshot via `prod.dct_views_rebuild` (db/v2/38) → `{rebuilt, invalidRemaining, refreshedAt}` (`08_gl_views_rebuild_ords.sql`, ADDITIVE — re-run 07 AND 08 after any 05 re-run) |
| GET | `/butil/filters` | Budget Utilization LOVs: years (+`defaultYear`), projectTypes, sectors, chapters, **businessUnits (v1.34.0)** (`07_gl_budget_util_ords.sql`, ADDITIVE — re-run after any 05 re-run) |
| GET | `/butil/lov` | Budget Utilization **autocomplete LOVs** (v1.12.0), `?year=` (optional → all years) → `{costCenters[{cc,dept}], projects[{p,n}], tasks[], etypes[]}` — distinct values of `DCT_BUDGET_UTILIZATION_V` feeding the filter-bar datalists (`07_gl_budget_util_ords.sql`) |
| GET | `/butil` | Budget Utilization report, `?year(req)=&period=&projecttype=&sector=&chapter=&costcenter=&project=&task=&etype=&search=&limit=&offset=&nocc=` → totals + items over `DCT_BUDGET_UTILIZATION_V` (db/v2/37); `costcenter`/`project`/`task`/`etype` are contains-match LOV filters (project matches number OR name); **`chapter`/`costcenter`/`project` also accept a pipe-delimited MULTI list (v1.23.0, `a\|b\|c`) matched EXACTLY (chapter text / CC code / project number) — a single pipe-less value keeps the original semantics**; **`bu=` (v1.34.0) pipe-delimited EXACT any-of Business-Unit list (project attribution via the projects master; also on `/butil/lines` + `/encumbrances` + every report bridge)**; **`period=MM-YYYY` (v1.16.0, must belong to `year`, else 400) = YTD through that period** — sets `GL_CTX.BUTIL_END` via `DCT_GL_CLASS_PKG.set_butil_end` before the queries and always clears it after (+ on error) **v1.43.0:** response always carries `missingCc`/`missingCcBudget` (count / annual-budget sum of filtered rows with annual budget ≠ 0 and NO cost centre — drives the red alert band); `nocc=Y` filters the register to exactly those rows (the band's drill drawer). **v1.44.0:** `ovr=Y` = **Consider Override Budget** — sets `GL_CTX.BUTIL_OVR` so the view takes `NVL(budget_user, budget)` per period row (the end-user Excel override, db/v2/106); every budget-derived figure (budget/budgetAnnual/fundAvailable/utilization) then reflects the override; always cleared after (+ on error). Response echoes `considerOverride` and totals + rows gain `overrideBudget`/`overrideBudgetAnnual`/`overrideLines` (ALWAYS emitted, flag-independent — feeds the separate Override Budget KPI). |
| GET | `/butil/lines` | Budget Utilization figure drill-down, `?year(req)=&metric(req)=` (ap\|grn\|pr\|po) `[&period=MM-YYYY]` (YTD — inline date bounds on all four metric queries incl. the PO GRN-netting, so drill totals reconcile to the period-filtered figures; GRN dates = `NVL(accounted_date, transaction_date)`) + EITHER `&project=&task=&etype=` (row drill) OR `&projecttype=&sector=&chapter=&search=&costcenter=&fproject=&ftask=&fetype=` (KPI-card aggregate; the LOV filters are f-prefixed to avoid the row-drill key params; `chapter`/`costcenter`/`fproject` accept the same pipe-delimited MULTI lists as `/butil`, v1.23.0) → `{metric, aggregate, total, count, columns[], rows[]}`; one `kys` key-set CTE drives both modes; filters/amounts mirror `DCT_BUDGET_UTILIZATION_V` so totals reconcile to the on-screen figure; rows capped 1000 (window `total`/`count` are full). PO/PR header+line joins de-duped to avoid fan-out. **Every drawer shows document # + line** (ap = Invoice #/Line via `line_number`, grn = GRN #/Line via `receipt_line_number` **+ PO #/PO Line/Supplier** via de-duped `po_headers`/`po_lines` joins, pr = PR #/Line via `pr_lines.pr_line`, po = PO #/Line) and **zero-amount lines are suppressed** (ap/grn/pr amount ≠ 0, po open > 0); rows also carry the FUSION internal ids `invoiceId`/`poHeaderId`/`prHeaderId` for the drawer deep-links (v1.20.0) (`07_gl_budget_util_ords.sql`, ADDITIVE — re-run after any 05 re-run) |
| GET | `/butil/override/lines` | **Override Budget KPI drawer** (v1.44.0, `15_gl_butil_override_ords.sql`, ADDITIVE — re-run after any 05 re-run): every project-budget period row carrying an end-user `BUDGET_USER` override (`DCT_PROJECT_BUDGET_USER`, db/v2/106 — entered via the Excel VB template), scoped by the full butil filter set (`?year(req)=&period=&projecttype=&sector=&chapter=&bu=&appropriation=&program=&costcenter=&project=&task=&etype=&search=`) via the same kys key-set CTE → `{total, totals{override,fusion}, reasons[{code,name,nameAr}] (the XL_OVERRIDE_REASON lookup — v1.45.0), items[{id, projectNumber/Name, taskNumber/Name, expenditureType, accountingPeriod, fusionBudget, overrideBudget, reasonCategory, comments, updatedBy, updatedAt}]}`; `id` = the opaque xl row key |
| POST | `/butil/override` | Edit an override **from the drawer** — body `{id, budget_user\|null, reason_category?, comments?}` (null budget clears the row; reason/comments are PARTIAL — only keys present are applied; reason validated against lookup `XL_OVERRIDE_REASON` → 400); same core save as the Excel PUT (`prod.dct_xl_pkg.save_item` — stamps user + Dubai time) → the updated row JSON. Gate `GL_EDIT_BUDGET_OVERRIDE` (has_priv_or_role, GL style) |
| POST | `/butil/xlsx` | **Budget Utilization Excel register bridge** (v1.30.0) — same body contract as `/butil/book` → enqueues Reporting-Platform `BUDGET_UTIL_REGISTER` (XLSX, reporting/db/25 — 6 sheets: utilization lines / direct AP / GRN receipts / open PO / open PR / pending PR-PO queue) as the calling GL user → `{runId}` (`11_gl_butil_book_ords.sql`) |
| GET | `/butil/xlsx/:id` | run status (BUDGET_UTIL_REGISTER runs only) → `{runId, status, rowCount, error, startedAt, finishedAt, hasFile}` |
| GET | `/butil/xlsx/:id/file` | authed XLSX download of a finished register run |
| POST | `/butil/book` | **Briefing Book bridge** — body `{year(req), period?, sector?, chapter?, projecttype?, costcenter?, project?, task?, etype?, search?}` (the full butil page filter set since 2026-07-14; `period` = MM-YYYY within the year, validated like `/butil` → 400 otherwise; `chapter`/`costcenter`/`project` accept the pipe-delimited MULTI lists — the book seed's predicates are kept in lock-step, reporting/db/21) → enqueues Reporting-Platform `BUDGET_UTIL_BOOK` (PDF) via `dct_rpt_pkg.enqueue` as the calling GL user → `{runId}` (`11_gl_butil_book_ords.sql`, ADDITIVE — re-run after any 05 re-run; the /rpt/ admin endpoints stay SYS_ADMIN-only) |
| POST | `/butil/ppt` | **PowerPoint deck bridge** (v1.32.1) — same body contract as `/butil/book` → enqueues `BUDGET_UTIL_BOOK` with `formats='PPTX'` (executive slide deck via reporting/runner/render_pptx.py) → `{runId}` (`11_gl_butil_book_ords.sql`) |
| GET | `/butil/ppt/:id` | PPTX run status (BUDGET_UTIL_BOOK runs) → `{runId, status, rowCount, error, startedAt, finishedAt, hasFile}` |
| GET | `/butil/ppt/:id/file` | authed PPTX download of a finished deck run (Content-Disposition attachment) |
| GET | `/butil/book/:id` | run status (BUDGET_UTIL_BOOK runs only) → `{runId, status, rowCount, error, startedAt, finishedAt, hasPdf}` |
| GET | `/butil/book/:id/pdf` | authed PDF download of a finished briefing-book run (Content-Disposition attachment) |
| GET | `/encumbrances` | **Open Projects Encumbrance Follow-up** — `?year(req)=&period=&projecttype=&sector=&chapter=&costcenter=&project=&task=&etype=&search=&limit=` (same butil filter set + semantics; key-set CTE lifted from `/butil/lines`) → `{year, columns[{key,label,type}], items[], count, total, truncated, maxRows, totals{openAed}}`. UNION of Open Commitment PR (reserved) + Open Obligation PO (GRN-netted) lines, joined to `DCT_GL_COA_SNAP` (via `dct_cc_canon(charge_account)`) for all 10 segment codes+names + sector/chapter; open amounts reconcile to the butil Total Encumbrance; zero lines excluded; capped 10 000 by open amount. (`12_gl_encumbrances_ords.sql`, ADDITIVE — re-run after any 05 re-run; the frontend passes `limit=10000` since ORDS injects its default page size into `:limit` when absent.) |
| GET | `/pending` | **Encumbrances – Pending Approval** — `?year(req)=&period=&projecttype=&sector=&chapter=&costcenter=&project=&task=&etype=&search=&source=&bu=&limit=` (same butil filter set + semantics; `source=PR\|PO` scopes the register AND the aggregates, anything else 400; `bu=` pipe-delimited EXACT any-of Business Unit list — also scopes the `unmatched` coverage KPI; `businessUnits[]` in the response = the snapshot's BU LOV; period-YTD via `set_butil_end`, ALWAYS cleared; items carry `fusionHeaderId` — the FUSION internal pr/po header id for the deep-link builders) → `{year, period?, asOf, kpis{docs,docsPr,docsPo,lines,amtTotal,amtPr,amtPo,reservedAmt,unreservedAmt,encOpenAmt,avgDays,maxDays,over30Docs,over30Amt}, aging[{bucket,docs,amt}×4], approvers[{name,docs,amt,maxDays}≤8], unmatched{docs,prDocs,poDocs}, columns[], items[], count, truncated, maxRows, totals{lineAed,encOpenAed}}`. Rows from `DCT_PR_PO_PENDING_V` (db/v2/52) × the butil key-set CTE, joined to `DCT_GL_COA_SNAP`; ONE scan streams the register (cap 10 000, oldest first) and accumulates all aggregates in the same loop (per-document dedupe via assoc array), so the KPIs cover the full filtered set even when the echoed rows are capped. **Zero-value lines excluded** (`ABS(NVL(line_aed,0)) > 0.005`, v1.30.1 — register, KPIs, aging, approvers and the client drill slices all inherit it; the `unmatched` coverage KPI is computed separately and is unaffected). `asOf` = snapshot load time (dct_to_local). (`13_gl_pending_ords.sql`, ADDITIVE — re-run after any 05 re-run; pass `limit=` explicitly, ORDS injects its page-size default into `:limit` when absent.) |
| POST | `/pending/book` | **Pending-approval Briefing Book bridge** — same body contract as `/butil/book` → enqueues Reporting-Platform `ENC_PENDING_BOOK` (PDF, reporting/db/23) as the calling GL user → `{runId}` (`13_gl_pending_ords.sql`) |
| GET | `/pending/book/:id` | run status (ENC_PENDING_BOOK runs only) → `{runId, status, rowCount, error, startedAt, finishedAt, hasPdf}` |
| GET | `/pending/book/:id/pdf` | authed PDF download of a finished pending-approval briefing-book run |
| POST | `/pending/xlsx` | **Excel register bridge** (v1.29.0) — same body contract as `/pending/book` + `source` (PR\|PO) → enqueues Reporting-Platform `ENC_PENDING_REGISTER` (XLSX, reporting/db/24) as the calling GL user → `{runId}` (`13_gl_pending_ords.sql`) |
| GET | `/pending/xlsx/:id` | run status (ENC_PENDING_REGISTER runs only) → `{runId, status, rowCount, error, startedAt, finishedAt, hasFile}` |
| GET | `/pending/xlsx/:id/file` | authed XLSX download of a finished register run (Content-Disposition attachment) |

## Data layer (PROD)
- Tables: `DCT_GL_CLASS_TYPE` → `DCT_GL_CLASS_VALUE` → `DCT_GL_SEG_CLASS_MAP`.
- Package: `DCT_GL_CLASS_PKG` (`norm`, `set_asof`/`clear_asof`/`get_asof`, `resolve_value_id`, `validate_map`); context `GL_CTX`.
- Views: `DCT_GL_COA_V` (+`GL_COA_V`), `DCT_GL_BALANCES_V`.
- Actuals reporting (db/v2/32–36): base pass-through views (`AP_*`/`PO_*`/**`PR_*`**/`GRN_ALL_V2`/`GL_BALANCES`), **`GL_BALANCES_CC`** (the ONE canonical remap of the reloaded feed's re-ordered `CONCATENATED_SEGMENTS` — every gl_balances↔COA join goes through its `cc_string`), `DCT_ACTUAL_V`, `DCT_BUDGET_ACTUAL_V`, **`DCT_PR_COMMITMENT_PERIOD_V`** (`db/v2/36`, over `ATD_PR_*`, AED via `DCT_CURRENCY_CODES` snapshot: `pr_total_ytd`=Reserved+Liquidated / `pr_open_commitment_ytd`=Reserved / `pr_pipeline_ytd`=Not-reserved + `pr_count`), and `DCT_BUDGET_ACTUAL_PERIOD_V` (per-combination × period YTD; +appropriation; joins the PR view for Commitment; PO 3 buckets from `po_distributions` de-duped by `po_distribution_id` — `total_po_ytd` [ex Failed/Passed] / `open_obligation_ytd` [(Reserved+Partial) **GRN-netted** via `grn_all_v2.po_distribution_id`] / `po_pipeline_ytd` [Failed/Passed]; `open_encumbrance_ytd`; `funds_available_calc_ytd` = Budget−OpenPO−OpenPR−GRN−APDirect; `funds_available_ytd` = GL balance; `po_count`/`pr_count`; **`ap_direct_actual_ytd` = AP lines with `po_number IS NULL`**); indexed snapshot `DCT_GL_COA_SNAP` + `prod.dct_actuals_refresh` (hourly job `DCT_ACTUALS_REFRESH_JOB`).
- Bridge synonyms: `GL_SRC_*` → `ATD_GL_*` (rename-insulation).

## Security Info drawer (2026-07-19 — Security Console adoption, SYS_ADMIN only)
- `isSysAdmin` (from session rolesCsv) shows the pnav-end **Security Info** `<button>` (never an `<a>` — the pending smoke asserts 9 pnav anchors).
- `openSecurityInfo` — fetches `GET /dct/sec/pageinfo?module=GL&page=<active tab>` and renders the `.dw-*` drawer: page view privilege, registered artifacts (buttons/endpoints/actions) and the duty/job roles granting each privilege. `closeSecurityInfo`.
- Server side: every 07/09/11/12/13 handler now checks `dct_sec.has_priv_or_role(user,'<GL_PRIV>',NULL,'GL')` (grandfathered while `FEATURE_SEC_ENFORCE_GL`=N) and /gl/butil + /gl/encumbrances + /gl/pending apply the SECTOR security-profile scope (`DCT_SEC_DATA` + `V_DCT_SEC_USER_SCOPE`).
