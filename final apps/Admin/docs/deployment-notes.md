# Admin (App 200) — Deployment Notes

> Central identity provider — all module apps authenticate through Admin. A bad Admin deploy breaks login for every app, so deploy Admin first and smoke-test login before touching module apps.
>
> **Keep this file current: every deployment (frontend, DB, ORDS) must be recorded here.**

- **Module code:** ADMIN · **ORDS base path:** `/ords/admin/dct/` · **Brand:** `#C74634` (Oracle Red, App 200 only)
- **DB source of truth:** `db/v2/` at repo root (NOT under `final apps/Admin/` — Admin owns the platform schema)

---

## 1. Frontend (JET SPA) deployment

| Step | Detail |
|---|---|
| Bump `window.APP_VERSION` | In `Jet/index.html` (currently `4.4.0`). Drives the requirejs `urlArgs` + i18n cache key. Localhost auto-busts; deployed browsers serve stale JS/HTML forever if you forget. |
| Deploy `final apps/shared/` | The shared layer (`platform.css`, `shell.js`, `i18n.js`, `chartLoader.js`, components) is referenced at `../shared/` by ALL apps. Any change to it requires an APP_VERSION bump in **all 7 apps'** index.html (precedent: the 4.1.1 wave bump). |
| Check `js/services/config.js` | `apiBase: '/ords/admin/dct'` (live). Never ship `apiBase: null` (mock) to production. |
| Chart.js | Never `<script>`-tag it — loaded via requirejs `chartjs` path, created via `shared/chartLoader.makeChart`. |
| Update `docs/functions_list.md` | If this deploy added/removed/renamed any view, viewModel method, service, or ORDS endpoint, reflect it in the functional inventory (see root `CLAUDE.md` → "Functions List"). |

## 2. Database deployment (PROD schema, via SQLcl)

Platform-wide SQLcl rules (apply to every script):

- Run via SQLcl only: `sql -name prod_mcp` — **never** the MCP `run-sql` tool (silently swallows errors).
- `prod_mcp` connects as ADMIN — prefix all objects: `CREATE OR REPLACE PACKAGE prod.x …`.
- `SET DEFINE OFF` at the top of every script (`&` in literals hangs SQLcl).
- Deploy scripts must be **CRLF** — SQLcl 26.1 silently skips ALL statements in LF-only files run via `@`.
- **Any script containing Arabic (or non-ASCII) literals MUST be run with `JAVA_TOOL_OPTIONS=-Dfile.encoding=UTF-8`** (PowerShell: `$env:JAVA_TOOL_OPTIONS="-Dfile.encoding=UTF-8"`). Without it SQLcl reads the file as the Windows code page and the Arabic is **silently destroyed** before it reaches the AL32UTF8 column — either as literal `?` or as lossy mojibake. The DB/ORDS/JET write paths are all UTF-8-clean; this is purely a SQLcl file-read issue. Diagnose with `SELECT DUMP(col,1016)` (clean Arabic = `d8`/`d9` lead bytes; corruption = `3f` repeated, or Latin-1 bytes). This destroyed 31 reference values once — see `db/v2/25`.
- No SQL keywords (MERGE/DECLARE/CREATE PROCEDURE) in leading `--` banner comments — SQLcl silently skips the following statement. Diagnose with `SET ECHO ON`.
- `INSERT ALL` + IDENTITY columns → ORA-00001. Use one `INSERT INTO` per row.
- Never close date ranges with `TRUNC(SYSDATE)` when `start_date` defaults to SYSDATE-with-time (ORA-02290) — use `GREATEST(SYSDATE, start_date)`.
- `q'[...]'` literals break on `]'` sequences — use `q'!...!'` there, but `ESCAPE '!'` inside `q'!...!'` terminates the literal — use `ESCAPE '\'`.

Script inventory (`db/v2/`, run order = `install.sql` 01→12, then numbered patches):

| Script | Deployed | Contents |
|---|---|---|
| `01`–`12` (install.sql) | ✅ | 24 DCT_* tables, DCT_AUTH/DCT_REST/DCT_NOTIFY, ORDS module (11), master data (12) |
| `14` approval sweep | ✅ | `DCT_APPROVAL_PKG` escalation/auto-approve scheduler job, daily **07:10** — verify the job is still enabled after redeploys |
| `15`/`16` Phase 2 | ✅ 2026-06-11 | Natural-key masters, unified documents/coding-lines/status-history, DCT_LOOKUP_PKG lookup-first, 22 FKs |
| `17` user photo | ✅ | DCT_USERS photo_blob (base64 ≤ 30k via `/dct/users/:id/photo`) |
| `18` user PUT date fix | ✅ | GREATEST(SYSDATE, start_date) fix for role end-dating |
| `19` wave enhancements | ✅ 2026-06-13 | Template draft lifecycle, `/dct/boot`, `/dct/notifications/count`, `/dct/audit/:id` snapshots, FEATURE_*/LANDING_* seeds, validate_session inactivity timeout |
| `20` enhancements 2 | ✅ 2026-06-13 | Settings PUT upsert, audit fromdt/todt + `/dct/audit/export` CSV, `/dct/sessions/` + revoke, approval-templates restore, INTEGRATION_API_KEY secret seed |
| `21` UAT cleanup | rerunnable | Parks `uat.auto.*` users, closes their sessions, drops `UAT_WAVE_FLOW~V%` archives |
| `22` region theme | ✅ 2026-06-13 | Five `THEME_REGION_*` settings (header fill/font + border color/width/style): system rows (APPEARANCE), override rows in ALL `dct_modules` (26 × 5, NULL = inherit), `chk_dct_set_type` widened (+COLOR/SELECT), `/dct/boot` whitelist + `THEME_REGION_%`. Rerunnable. |
| `24` audit snapshots | ✅ 2026-06-13 | `DCT_AUDIT_PKG` (`snapshots_on` ×2 / `snap` / `log`) + ADMIN synonym + `FEATURE_AUDIT_SNAPSHOTS` (global default) **and 7 per-module switches** `FEATURE_AUDIT_SNAPSHOTS_<ADMIN\|PC\|DT\|HR\|FL\|CC\|AR>` (all BOOLEAN, default **N**). Powers the audit-diff modal. Rerunnable; run in its own fresh session. After deploy, **re-run every module's ORDS script** (11 + PC/DT/HR/FL/CC/AR) so their write handlers capture before/after snapshots. |
| `25` Arabic encoding repair | ✅ 2026-06-13 | One-time data repair: re-supplies correct Arabic for 31 values destroyed by earlier seed runs (SQLcl read the .sql as the Windows code page, not UTF-8). Fixed: `DCT_USERS.display_name_ar` (ADMIN, was literal `?`), `DCT_ROLES.role_name_ar` ×6, `DCT_LOOKUP_CATEGORIES.category_name_ar` ×4, `DCT_LOOKUP_VALUES.value_name_ar` ×20 (all FL). The stored corruption was **unrecoverable** (mojibake collapsed every Arabic letter whose UTF-8 trailing byte was 0x80–0x9F to one char), so values were re-keyed by business code. Idempotent/rerunnable. **MUST run with `JAVA_TOOL_OPTIONS=-Dfile.encoding=UTF-8`** or it re-corrupts. |
| `26` focus highlight | ✅ 2026-06-15 | Configurable field-focus highlight (all apps): `THEME_FOCUS_COLOR` (COLOR, default `#F0883E`) + `FEATURE_FOCUS_HIGHLIGHT` (BOOLEAN, default `Y`) + `THEME_FOCUS_FILL_LEVEL` (NUMBER 0–100 fill intensity, default `15`) system rows (APPEARANCE), `/dct/boot` whitelist + `THEME_FOCUS_COLOR`/`THEME_FOCUS_FILL_LEVEL` (the flag rides the existing `FEATURE_%` clause). Applied at boot by `shared/js/shell.js` (`applyRegionTheme`) as CSS vars `--focus-color`/`--focus-ring`/`--focus-fill` in `platform.css` (fill = tint mixed `1 - level/100` toward white; field fill + border + ring); flag `N` → neutral border, no ring/fill. Admin-only (global, no per-module rows). Edited in System Settings → Field Focus Highlight (shared `regionPicker`: native color picker + on/off switch + intensity slider). Rerunnable; run in a fresh session (redefines the `/dct/boot` ORDS handler). |
| `49` module access | ✅ 2026-07-13 | **DCT_MODULE_ROLES goes live** (App-Launcher visibility): wipes the dormant 04 seed rows (49 rows, behavior-preserving), gap-fills missing `dct_modules` rows for **ATD + GL**, adds ADMIN synonym `dct_module_roles`, and defines 3 additive handlers — `GET /dct/my/modules` (denied module codes for the session user; fail-open: zero rows = everyone, SYS_ADMIN never denied), `GET/PUT /dct/modules/:id/roles` (SYS_ADMIN grant-set editor; empty `roleIds` = unrestricted; FK violation → 400). Shared shell (`initModuleAccess`) hides denied switcher entries; Admin → Modules gains a per-app Access modal. Security boundary stays the per-handler ORDS role checks — this is launcher visibility only. Rerunnable; fresh session; **re-run after any 11 re-run**. Run with `JAVA_TOOL_OPTIONS=-Dfile.encoding=UTF-8` (Arabic literals in the gap-fill). |
| `50` module access enforcement | ✅ 2026-07-13 | **Server-side gate** in `DCT_REST.VALIDATE_SESSION` (body replace; source synced into 11): derives the target ORDS module from CGI env **`X-APEX-PATH`** (first segment) and raises a clean **403 `{"error": "Module access denied"}`** when the module is restricted in `DCT_MODULE_ROLES` and the session user holds no mapped role (SYS_ADMIN always allowed; `dct` shared platform module EXEMPT; fail-open on any gate error). One-shot `g_deny_sent` flag makes the handler's follow-up `err(401)` a no-op so the body stays single-JSON (reset at every validate_session entry — pooled DB sessions carry package state). **A NEW module app must add its ORDS segment to the CASE map** (pc/dt/hr/fl/cc/ar/tm/atd/gl/rpt/ap today). Blocks direct-URL/API access — completes 49's launcher-visibility layer. |
| `31` automation registry | ✅ 2026-06-28 | `DCT_RUNNERS` table (+ `ux_dct_runners_code` function-based unique index on `UPPER(runner_code)`) + ADMIN synonym + 28-row seed (MERGE WHEN NOT MATCHED — never clobbers edits) + ORDS `/dct/runners*` (8 handlers: list/meta/create/detail/update/delete + raw-binary `PUT/GET runners/:id/file` with `MAX_UPLOAD_MB` guard + media download). Powers Admin → System → **Automation Registry** (`runners` view; shared `<edit-drawer>` + `<doc-upload>`, registered in Admin `main.js`). APP_VERSION **4.5.9**. **SQLcl gotcha:** `EXECUTE IMMEDIATE q'[CREATE TABLE…]'` and the function-based index silently failed under SQLcl here — created table + index directly, then re-ran the script (idempotent: table → ORA-00955 guarded, seed MERGE no-op, ORDS redefined). Rerunnable; fresh session. |

### Audit snapshots (db/v2/24) — how it works
- **Per-module control.** Each module has its own switch `FEATURE_AUDIT_SNAPSHOTS_<CODE>` (CODE = the audit module code ADMIN/PC/DT/HR/FL/CC/AR), default **N**, on System Settings → FEATURES. `FEATURE_AUDIT_SNAPSHOTS` (no suffix) is the **default applied to any module without its own switch** (none today — it's a safety net for future modules). Resolution: module switch (Y/N) wins; else the global default; else off.
- `snapshots_on(module_code)` does that resolution (used by `log` to decide whether to store). The no-arg `snapshots_on` is TRUE if **any** `FEATURE_AUDIT_SNAPSHOTS%` key is `Y` (used by `snap` to skip work when snapshots are off everywhere). All keys match `FEATURE\_%` so they flow to `GET /dct/boot` automatically.
- Write handlers call `dct_audit_pkg.snap(table, pk_col, pk_val)` before/after the write and `dct_audit_pkg.log(user, action, type, id, module_code, p_old=>, p_new=>)`. `log` is autonomous + swallows exceptions — auditing can never fail a business write. `snap` excludes BLOB/CLOB/LONG/RAW columns and `PASSWORD_HASH`; settings PUT additionally skips snapshots for `%API_KEY%/%SECRET%/%PASSWORD%/%TOKEN%` keys.
- **Coverage = core entity create/update per module** (Admin users/roles/settings/modules/orgs/lookups/delegations/announcements; PC pc/reimb/clearing; DT requests/settlements; HR employees/contracts/salary; FL registrations/contracts/vouchers; CC cards/requests/replenishments; AR events). Pure workflow actions (approve/submit/etc.) and reference-data sub-endpoints are not yet instrumented — they have their own status-history trails and can adopt the helper incrementally.

## 3. ORDS deployment

- Module lives under **ADMIN** schema at `/ords/admin/dct/` — `ORDS.ENABLE_SCHEMA('PROD')` does not make `/ords/prod/...` routable on ADB.
- Every PROD object referenced in handler PL/SQL needs an ADMIN synonym (`CREATE OR REPLACE SYNONYM dct_rest FOR prod.dct_rest;`).
- **Run ORDS/synonym scripts in their own fresh SQLcl session** — if an earlier script set `CURRENT_SCHEMA = PROD`, synonyms become self-referencing (ORA-01471).
- Bearer token arrives in CGI env `'AUTHORIZATION'` (no `HTTP_` prefix) on ADB — `DCT_REST.VALIDATE_SESSION` handles the fallback; do not regress it.
- `GET /dct/branding` is the ONLY unauthenticated handler — exposes just APP_NAME/APP_NAME_AR/APP_TAGLINE/APP_TAGLINE_AR. Never add secrets to it.
- ORDS masks any `%API_KEY%` setting value in GET responses; api keys are write-only.
- ORDS base URL comes from OCI Console — never guess from tnsnames (JDBC ≠ HTTPS hostname).

## 4. Post-deploy smoke test

1. Login at the deployed Admin URL (200 from `POST /dct/auth/login`).
2. Module switcher lists all live apps; brand colors load from each module's `THEME_BRAND_COLOR`.
3. `GET /dct/boot` returns feature flags; notifications badge populates.
4. Verify `DCT_APPROVAL_PKG` sweep job exists and is `ENABLED`.

## 5. Deployment history

- **2026-07-21 (latest) — System Settings action hints (Admin 4.7.8; frontend only):** every Data Maintenance action button (Save Changes, Run cleanup, Recompile invalid objects, Run health check, Run integrity check, Refresh performance, Refresh lock status) gained a well-formatted **ⓘ hint popover** — a small info badge left of the button that reveals a dark, titled card (what it does · what it touches · when to use) on hover/focus. Reusable `.ss-hint` / `.ss-hint-pop` / `.ss-act` CSS in Admin `app.css` (Admin-only, RTL-aware via logical props); EN+AR `sys.*.hintTitle`/`hintBody` keys. No DB/ORDS/shared change.
- **2026-07-21 — Front-end "Recompile invalid objects" action (db/v2/104; Admin 4.7.7; no shared change):** the Data Maintenance → **Database object health** card gained a SYS_ADMIN **Recompile invalid objects** button beside "Run health check now" (disabled when 0 invalid). DB: `prod.dct_db_recompile` (AUTHID DEFINER, runs as PROD) = `prod.dct_views_rebuild` (recreates the 15 base pass-through views as `SELECT *`, self-healing an ATD reload that dropped/renamed a column — the usual root of a cascade) → `DBMS_UTILITY.COMPILE_SCHEMA('PROD',FALSE)` (recompiles the dependent views + package bodies in order) → `dct_db_health_pkg.run_health_check` (refreshes the card state); returns before/after/cleared/viewsRebuilt/stillInvalid. ORDS: `POST /dct/maintenance/db-health/recompile` on `dct.admin` (SYS_ADMIN-gated). **Origin incident:** an `ATD_PROJECTS` reload (833→946 rows) dropped `PROJECT_TYPE_DESCRIPTION`, invalidating `PROD.PROJECTS` (explicit-column pass-through) and cascading to 21 invalid objects (`DCT_BUDGET_UTILIZATION_V`/`DCT_ACTUAL_V`/`DCT_BUTIL_SCOPE_V`/AP+PO `_V` views/WF obj views/`DCT_AP_PKG` body…), taking GL Budget Utilization down — the button clears all of it in one click. Validated live against the real 21: 15 views rebuilt, after=0, 0 still invalid, Budget Utilization restored. **Gotcha:** `LISTAGG` `ON OVERFLOW TRUNCATE` goes INSIDE the `LISTAGG(...)` parens (after the delimiter), not after `WITHIN GROUP` (ORA-00923 otherwise). Additive; re-run after any db/v2/79 re-run.
- **2026-07-19 — "My Delegations" self-service vacation page (Admin 4.6.31; webtier release `20260719001043` LIVE; no DB change):** new Workspace-nav page `myDelegations`, visible to EVERY user — the discoverable front door to the vacation rule. Outgoing ("I delegated") / incoming ("delegated to me") split, create in a shared `<edit-drawer>` (delegate + scope All-roles/One-module + date window + reason), cancel own ACTIVE rows. Purely a UI addition: `GET /dct/delegations/?mine=Y` already scopes non-admins to their own rows and `POST /dct/delegations/` already 403s creating on someone else's behalf without SYS_ADMIN — the rights existed, only the page was missing (creation previously hid inside Profile; the System → Delegations page stays the admin oversight view and its nav group is admin-only). Route added to `KNOWN_ROUTES` so `LANDING_<ROLE>` can target it. Browser acceptance `my_delegations_browser.py` **11/11** — run AS A NON-ADMIN manager end-to-end (nav visible, System group hidden, create → ACTIVE, the delegate's own session sees it incoming, cancel → CANCELLED, AR/RTL), self-cleaning.
- **2026-07-18 — Role/Object management drawers + DWP escalation/expiration/vacation completion (db/v2/98 + 95/63 re-runs; Admin 4.6.30 + bumps in all shared-consumer apps; webtier release `20260718235226` LIVE):** the Role Assignments page gains three right-edge drawers (shared `<edit-drawer>`): **Policies** (converted from a modal), **Manage Roles** (WF_ADMIN — create/rename/deactivate DATA assignment roles + cardinality from the UI; deactivation warns with the active-assignment count and existing rows stay in history), **Manage Objects** (SYS_ADMIN — object-type registry CRUD where the LOV view and EVERY column are chosen from data-dictionary dropdowns; `DCT_WF_ASSIGN.save_object_type` re-validates with DBMS_ASSERT + all_tab_columns before anything is stored, so nothing free-typed reaches the registry; a brand-new type still needs its CREATE VIEW from a developer when no suitable view exists). DB: `95` re-run adds `save_data_role` / `save_object_type` (both return the active-assignment count on deactivation; a role_code that exists as a SECURITY role is refused, never silently taken over); `98` = 6 new self-gated wf.rest routes — `GET/POST assign/manage/roles`, `GET/POST assign/manage/object-types`, `GET assign/dict` (PROD views search / columns of one view), `POST tasks/:id/reassign` (engine `reassign_task` finally exposed — the leaver/vacation REPLACE case, WF_ADMIN). **DWP timer rules completed:** the designer step editor now exposes the full **Timers** section (reminder offsets, escalate-to-role + hours, auto-action outcome + hours — backend db/v2/68/69 already carried the fields; UI-only change to `<wf-designer>`), and the engine sweep (63 re-run) gained the **late-delegation leg**: a `dct_delegations` row created AFTER tasks exist now attaches the delegate to every open task of the delegator on the next 15-min sweep (same ALL_ROLES/MODULE/SPECIFIC_ROLE scope semantics as resolve-time; idempotent NOT EXISTS; hist TASK_DELEGATED + notify) — before, a vacation delegation only covered future tasks. Tests: API smoke `98t` **23/23**; browser `wf_manage_drawers_browser.py` **17/17** EN + AR/RTL; sweep manual run clean; 0 INVALID; 0 test residue. Gotchas: validate a `:bind` param **BEFORE** `dct_rest.json_header` — once output starts, `err(400)` rides inside a 200 body (the dict route hit this); a raw `;` in a query string never reaches the handler (parameter separator) — the injection test must URL-encode `%3B`; MERGE inside a package body would be silently swallowed by Linux SQLcl — `save_data_role` uses UPDATE-then-INSERT instead.
- **2026-07-18 — DWP Dynamic Role Assignments (db/v2/94–96 + reporting/db/27; Admin 4.6.27 + bumps in all shared-consumer apps; webtier release `20260718215055` LIVE):** date-tracked assignment of users to workflow DATA roles (WF_APPROVER / WF_FBP / WF_PBP / WF_PLANNER / WF_FYI_GROUP) against business objects (Sector, Department, HR Org, Cost Center, Project, Task, Appropriation, PO, GL Account, Entity — extensible via the `DCT_WF_OBJECT_TYPE` registry, adding a type is a data row + at most one small LOV view). DB: `94` registry + `DCT_WF_ROLE_ASSIGNMENT` (immutable identity, end/replace/void only — a BEFORE UPDATE trigger enforces it) + `DCT_WF_ROLE_POLICY` (per-role single-assignee; the policy row doubles as the overlap-check lock) + 10 LOV views + participant-rule columns + `ASSIGNED_ROLE` in the resolver CHECK; `63` re-run — new `assigned_holders` (as-of-SUBMISSION-date lookup; ORG kinds climb nearest-mapped-ancestor; `scoped_holders` now delegates here with type DEPARTMENT, absorbing the empty `dct_wf_approver_map` and its SYSDATE gap), `resolve_participants` gained trailing `p_asof` (activate_step passes `started_at`; simulate = as-of-now); `95` `DCT_WF_ASSIGN` (CRUD/LOV/preview; 95t **26/26** incl. the 5/5 hierarchy-cascade E2E: FBP falls Task→Project→Cost Center→Sector via a FIRST_MATCH rule cascade — no engine hierarchy code, the transaction's facts carry the dimension keys and facts snapshot at submission so as-of is automatic); `68`+`69` re-run (designer carries objectTypeCode/key2FactPath; validate() flags incomplete ASSIGNED_ROLE rules and the ANY_ROLE_HOLDER fallback trap); `96` = 9 self-gated `/wf/assign/*` routes + `v_dct_wf_assign_audit` (96t **23/23**; `/wf/` is gate-exempt so EVERY route checks WF_ADMIN/SYS_ADMIN itself); reporting/db/27 registers IR `WF_ROLE_ASSIGN_AUDIT` (BI viewer, BI_USER-gated). Frontend: Admin → System → **Role Assignments** (`roleAssignments`, tabs Assignments + Audit w/ CSV export; type-driven object picker incl. dependent project→task and indented org tree; live "currently holds" preview; End/Replace/Void dialogs; per-object Timeline); designer participant editor offers **ASSIGNED_ROLE**; `wfService` gained the `assign*` client; `api.fetchBlobUrl` honors `{base:'wf'}`. Browser acceptance **13/13**. Gotchas: `[COLON]` placeholders need the `REPLACE(...,CHR(58))` wrapper on BOTH ends of the q'!!' literal or the handler 555s; a `value:`-bound input filled by a test then saved via JS click never fires `change` — bind programmatic fields with `textInput:`; ending an assignment TODAY keeps it ACTIVE through end of day (inclusive end date — by design); an org-hierarchy LOV must offer the FULL tree (a parent-level assignment must be creatable in the same namespace the walk climbs).
- **2026-07-13 — Module access ENFORCEMENT + webtier release (db/v2/50; APP_VERSION 4.6.5 Admin + bumps in all 11 shared-consumer apps; webtier release `20260713231051` LIVE):** the 49 launcher-visibility layer now has teeth. Server: `DCT_REST.VALIDATE_SESSION` gate (see §2 table row 50) — restricted module + no mapped role = HTTP 403 on EVERY protected handler of that module's ORDS API, so URL override/curl is blocked (`/dct/` exempt, SYS_ADMIN exempt, fail-open). Client: `shell.initModuleAccess` additionally redirects out of a denied app at boot (`moduleFromPath` prefix match → first allowed module; inert on dev-proxy roots). Live-tested: restricted TASK_MGMT → plain user `GET /tm/teams` = clean single-JSON 403, admin 200, `/dct/` 200, restore 200, bogus-token 401 unaffected (deny-suppression flag verified one-shot). Gotchas: ORDS template delete = `ORDS.DELETE_TEMPLATE(p_module_name, p_uri_template)` (NOT p_pattern); the handler's follow-up `err(401)` after a gate 403 previously appended stray text to the body — hence `g_deny_sent`.
- **2026-07-13 — Module access control (db/v2/49; APP_VERSION 4.6.3 Admin + bumps in all 11 shared-consumer apps):** `DCT_MODULE_ROLES` wired into the platform. DB/ORDS: dormant 04 seed wiped (go-live changes nothing), ATD + GL registry rows gap-filled (they had NO `dct_modules` row — unrestrictable forever), `GET /dct/my/modules` + `GET/PUT /dct/modules/:id/roles` (see §2 table). Frontend: `shared/js/shell.js` `initModuleAccess` (rides `initRegionTheme` in module apps; Admin calls it from `_bootstrap`) hides denied `.modsw-item`s by registry index — fail-open on any error; Admin → Modules cards gain a 🔑 **Access** button + modal (role checkboxes, "Available to everyone" vs "Restricted to roles (n)" badge, Clear all; i18n EN/AR). Semantics: zero rows = everyone; any rows = only mapped roles; SYS_ADMIN always sees all (lockout guard). Verified: API smoke 12/12 (restrict/deny/restore + 401/403/404/400) and Playwright E2E 7/7 (restrict ATD via UI → plain user's switcher hides exactly Analytics Loader → restore). **Gotchas:** `dct_module_roles` needed its first ADMIN synonym; `'roleIds[%d]'` inside a `q'[...]'` literal terminates it at `]'` — use `q'!...!'`; the Admin dashboard "modules directory" cards are NOT filtered (switcher only) — follow-up if wanted.
- **2026-07-07 — APEX App 211 (BI) onboarded to platform auth + SSO mapping.** 211 had the default "Oracle APEX Accounts" scheme (headless-shell origin) → subscribed to App 200's `DCT Custom Auth` in Builder; first login then threw **ERR-1002 "Unable to find item USER_ID"** — `dct_auth.post_login` populates the 14 standard `USER_*` app items which 211 lacked → created by script **`db/v2/42_apex_211_dct_items.sql`** (mirrors 05b's `wwv_flow_imp_shared.create_flow_item` pattern; reusable for any future module APEX app). SSO wiring on 211's page 9999 done in Builder (item + Unrestricted + `SSO_AUTO_LOGIN`), `APEX_SSO_APPS` → `200,211`. E2E PASS: BI JET code → landed authenticated on `/ords/r/prod/bi-reporting/home`.
- **2026-07-06 (latest) — SSO app mapping, JET module → its own APEX app (db/v2/41c; APP_VERSION 4.6.1 Admin + bumps in all 10 shared-consumer apps):** the shared-shell APEX button now sends the calling module's app number (`shell.moduleAppFromPath()`, derived from the URL path) in `POST /dct/sso/code` body `{app}`; the handler targets that app's login page when the id is on the new **`APEX_SSO_APPS`** CSV allowlist (seeded `200`), else falls back to App 200. `APEX_SSO_URL` became a template (`f?p=%APP%:9999:…`). Onboarding a module's APEX app (201, 204, …) = wire its login page 9999 like App 200's + append the id to `APEX_SSO_APPS` — no code deploy (runbook § App mapping; checklist item added to SHARED_APEX_ARCHITECTURE). Handler-only replace on the existing `sso/code` template (DEFINE_HANDLER, never DEFINE_TEMPLATE); source synced into 11. Live smoke 6/6 incl. temporary allowlist of 201 → URL flipped to `f?p=201:9999`. Also: PROD DB password confirmed set (ORA-28007 on re-set = already that value).
- **2026-07-06 (later) — SSO Builder steps COMPLETED + both directions live-verified.** APEX side done in Builder (9999 hidden item + `SSO_AUTO_LOGIN`; page 9996 `GO_JET` + nav-bar entry), `FEATURE_SSO_HANDOFF`=**Y**. Live E2E: JET→APEX (audit `SSO_ISSUE`/`SSO_REDEEM`/`LOGIN` SUCCESS) and APEX→JET **cold-start** (fresh browser + `#sso=` → signed-in dashboard). Extra gotchas fixed en route: page 9999 **Page Access Protection must be Unrestricted** (checksum mode → "Session state protection violation" on the hand-off URL); **never wrap `apex_util.redirect_url` in `WHEN OTHERS`** (it catches the internal stop-engine *success* signal and a fallback redirect replaces the good one → 404; also `apex_application.g_stop_apex_engine` fails validation with PLS-00302 on this instance — use the flag-and-branch pattern in `apex-sso-setup.md`); **`JET_SSO_URL` must point directly at `…/Admin/Jet/index.html`** (module-app landers redirect and DROP the `#sso` fragment); APEX Builder access itself = unified ADB sign-in with **PROD DB credentials** (password reset + APEX workspace account unlocked after failed-attempt lockout, 2026-07-06).
- **2026-07-06 — Cross-UI SSO hand-off, JET ⇄ APEX App 200 (db/v2/41 + 41b; APP_VERSION 4.6.0 Admin + bumps in all 10 shared-consumer apps):** one-time, single-use, SHA-256-hashed hand-off codes over the DCT_AUTH/DCT_SESSIONS backbone, gated by `FEATURE_SSO_HANDOFF` (default **N**). DB: `DCT_SSO_CODES` + `DCT_SSO_PKG` (issue_code/peek_username/redeem_code, TTL `SSO_CODE_TTL_SECS`=60s bounded 10–600), `dct_auth.authenticate` gains the `SSO$<code>` sentinel branch (03 synced), settings `APEX_SSO_URL`/`JET_SSO_URL` (category SECURITY), lookup `SSO_DIRECTION`. ORDS (41b, fresh session): `POST /dct/sso/code` (bearer; returns `{code,expiresInSecs,apexUrl}`) + `POST /dct/auth/sso` (public; redeems APEX2JET code → same payload as `auth/login`, `auth_method='SSO'`); both mirrored into 11. Frontend: `shared/js/shell.js` injects an **APEX** topbar button (`.tb-apex`, vanilla-DOM banner pattern; module apps get it via `initRegionTheme`'s existing boot fetch, Admin via `_bootstrap` → `shell.injectApexLink`; popup-safe: tab opened synchronously, URL set after the fetch); Admin `authService.ssoExchange(code)` + `appController` `#sso=<code>` boot hook (scrubs the code via `history.replaceState`, bad code falls to the login form). APEX Builder side is manual — runbook `docs/apex-sso-setup.md` (9999 item+process, page 9996 Go-JET, nav-bar entry). Flip the gate to Y only after the Builder steps + smoke. **Gotchas hit:** package-private `hash_code` in SQL → PLS-00231 (assign to a local first); `dct_system_settings.value_type` allows STRING/NUMBER/BOOLEAN/JSON/DATE only (no TEXT).
- **2026-07-05 — Freelancer Portal link on the login page (APP_VERSION 4.5.12→4.5.13, Admin-only):** added a line at the bottom of the Admin login card telling external freelancers this portal is for DCT staff and linking them to the public **Freelancer Portal** (`/FL/Portal/index.html`, root-absolute so the dev-proxy / any host serves it). New i18n keys `login.freelancerNote` / `login.freelancerLink` (EN + AR); RTL verified. Frontend only — no DB/ORDS change.
- **2026-06-15 — Region header flush + missing region headers (APP_VERSION 4.5.3→4.5.4 in all 8 apps; shared/ change):** `final apps/shared/css/platform.css` now pulls a card's **first-child** `.section-heading`/`.section-heading-row` flush to the top/left/right border — negative margin (`-20px -20px 16px`) cancelling the card's 20px padding + top-only `border-radius`, set `!important` so it overrides the legacy inline `margin-top:0` some first headings carry (which otherwise left them flush only on the sides). Only the first heading flushes; mid-card sub-section headings keep normal spacing. Per-app: added themed headers to previously headerless main regions (Admin profile → Profile Information added, Change Password flushed, My Delegations normalized into `.section-heading-row`; FL/CC/DT/HR detail+form views — see their notes). Frontend/CSS only — no DB/ORDS change.
- 2026-06-13: **Audit before/after snapshots** deployed (db/v2/24 + all 7 ORDS scripts re-run). New `DCT_AUDIT_PKG` captures JSON old/new of changed rows into `dct_audit_log`, **gated per module** by `FEATURE_AUDIT_SNAPSHOTS_<CODE>` switches (+ `FEATURE_AUDIT_SNAPSHOTS` global default), all default OFF. No frontend change — the audit-diff modal already reads `/dct/audit/:id`; the 8 switches render in System Settings → FEATURES automatically. Verified in PROD: snap NULL when OFF / full JSON when ON, `password_hash`+`photo_blob` excluded, `snap` resolves for all 23 instrumented tables, and per-module resolution (enable PC only → PC captures, DT does not; explicit N overrides global Y).
- 2026-06-13: **Region appearance theme** deployed (db/v2/22 + shared layer). Region/modal/table headers + card borders themed via `THEME_REGION_*` (module → system → CSS fallback); System Settings page gained the Region Appearance palette picker (live preview, AA-contrast badge); module apps boot via `shell.initRegionTheme` (HR has no settings endpoint — system default only). APP_VERSION **4.3.0** in all 7 apps. Verified: `/dct/boot` returns the 5 keys; Playwright on Admin systemSettings/users + PC dashboard.

## 6. Known gaps / cautions

- Audit snapshots (`dct_audit_log.old_values/new_values`) are populated by `DCT_AUDIT_PKG` (db/v2/24) **per module** via the `FEATURE_AUDIT_SNAPSHOTS_<CODE>` switches — all default OFF, so the diff modal shows "no snapshot" until an admin enables a module. Coverage is core entity create/update per module (see §2); workflow actions and reference-data writes are not yet instrumented.
- ADB ORDS returns a 400 HTML error page for **body-less requests carrying `Content-Type: application/json`** (e.g. DELETE). Fixed in `shared/js/api.js` (Content-Type only when a body exists) — don't regress.
- Rotate any leaked keys immediately (an ANTHROPIC_API_KEY leak was rotated during Phase 1).
- **2026-06-13 — Settings palette picker extracted to shared (APP_VERSION 4.4.0):** `Admin/Jet/js/viewModels/systemSettings.js` refactored to consume the new `shared/js/regionPicker.js` (PALETTE/contrast/pickers/live-preview) — no behaviour change. Same helper now powers the Region Appearance panel on every module's settings page. Shared/ change → APP_VERSION bumped to 4.4.0 in all 7 apps.

- **2026-07-19 — User Management detail tabs as interactive-report grids (Admin 4.7.5 + patch bumps in all 12 apps; no DB change):** the four tabs under the user detail (Role Assignments / Security Profile / Exclusions / Effective Privileges) replaced their hand-rolled `rm-table` markup (and the effective chip groups) with the SHARED `<interactive-report>` component — report codes `SEC_USER_ROLES`/`SEC_USER_PROFILES`/`SEC_USER_EXCLUSIONS`/`SEC_USER_EFFECTIVE`, formatted region-themed grid + search/columns/filter/highlight/CSV/XLSX for free. End/Remove row actions = a declared `action` column + delegated click on the `.rm-ir-embed` wrapper (`td[data-key="action"]`; row identity from a side-map keyed on the visible `code|startDate|status` cells — the IR drops undeclared row fields, GL pending-page pattern; new `.rm-ir-embed` + red action-cell styles in vault.css). Inline assign forms stay below each grid. Empty tabs still mount the IR (toolbar + its own empty state — tests must assert `.ir-toolbar`, not `.ir-table`). `security_console_browser.py` extended → **29/29** EN + AR/RTL.
- **2026-07-19 — Page Security callout highlight (Admin 4.7.4 + patch bumps in all 12 apps):** the page-name + view-privilege header in the Page Security drawer is now a filled brand-tinted callout (`.si-head` in platform.css — brand-soft fill, accent inline-start spine, white code pill; GL portal twin mirrors it in green inline styles) so the view privilege reads first, per user feedback.
- **2026-07-19 — Security Console review round (user comments 1–6; Admin 4.7.2 + patch bumps in all 12 apps; db/v2/103 + 101 re-run; webtier release `20260719180138` LIVE):**
  ① **Descriptions everywhere** — `db/v2/103_dct_sec_descriptions.sql` (109 rows, EN + AR, NVL-guarded/rerunnable, deploy with `-Dfile.encoding=UTF-8`): every privilege (81 were missing — AR/CC/DT/GL/PC/TM/platform), every role (28 incl. the GL duties/jobs, WF data roles, SYS_ADMIN…) and both privilege groups now carry end-user descriptions; FL_FREELANCER mojibake repaired; verification = 0 missing on all three tables. The console's create/edit forms now REQUIRE a description client-side.
  ② **Who columns** — 101 re-run: `createdBy/createdAt/updatedBy/updatedAt` (12-h Dubai local via `dct_to_local`) on the privileges/groups/roles/profiles list GETs; 101t **44/44**. Also FIXED the test suite's silent self-cleanup failure: its temp SQL was LF-only (this Linux SQLcl swallows LF-only blocks — cleanup now writes CRLF and surfaces ORA- lines) **and** it never deleted `dct_user_roles` rows for the test roles (FK_DCT_UR_ROLE blocked the role delete). All `TSEC101_*` residue purged from PROD; suite verified residue-free end-to-end.
  ③ **Interactive-report catalogs** — Privileges / Privilege Groups (card grid replaced) / Abstract–Duty–Job Roles / Security Profiles all render in the SHARED `<interactive-report>` (report codes `SEC_*`; who columns visible; row click opens the editor). Admin main.js now registers `shared/components/interactiveReport` + the `xlsx` requirejs path.
  ④ **Drawer editors** — every create/edit form moved into the shared `<edit-drawer>` (privilege 640px, group 760px, profile 800px, role **920px** — the FULL role editor with its 4 tabs lives in the drawer and the separate `secRoleEdit` route/view/VM was DELETED; copy 480px; New User 640px). Deactivate/Copy are quiet links at the drawer bottom; confirm dialogs stay modal.
  ⑤ **User Management alignment** (frontend-design pass) — strict `.rm-form-grid` 2-column grid (labels above fields, uniform widths, `.rm-fg-span` for full rows), Save Profile top-right in the panel header, status badge in the detail header, **New User in a drawer** that lands on Role Assignments after create.
  ⑥ **Page Security drawer = interactive report** — `<security-info>` (shared) renders the artifact register as an embedded `<interactive-report>` (`SEC_PAGEINFO`, drawer widened to 820px; 7 new `si.col.*` i18n keys EN+AR); the GL portal twin does the same (`GL_SEC_PAGEINFO`, `.dw-wide`). vault.css additions: `.rm-ir-panel`, `.rm-form-grid`, `.rm-danger-link`.
  Tests: rewritten `security_console_browser.py` **26/26** EN + AR/RTL (gotchas: IR grid headers are CSS-uppercased — compare lower-case; an embedded IR carries its own nested calc-editor `<edit-drawer>` — scope drawer locators with `.first` / `:not(.si-drawer)`); GL drawer IR smoke clean.
- **2026-07-19 — Vault security pages restyled LIGHT (Admin 4.7.1 + patch bumps in all 12 apps; shared/css/vault.css only; webtier release `20260719171327` LIVE):** user feedback — the security section rendered dark ("why the pages in dark mode?") while every other page is light-content. `vault.css` is now THEME-AWARE: light GitHub-light palette by default (white surfaces, `#D96C1E` amber accent, same `rm-*`/`re-*` classes untouched) so Roles / Permissions / Role Edit and all 7 Security Console pages match the light platform themes; the original GitHub-dark Vault palette applies ONLY under `body[data-theme="vault"]`/`"midnight"` (the Appearance-page dark themes stay coherent). New tokens `--vault-purple/-field/-thead/-on-accent/-modal-shadow`; every hard-coded dark hex moved onto vars. NO markup/VM/i18n change. Verified: `security_console_browser.py` 19/19 re-run + light screenshots of Roles/Permission-Matrix/Privileges. Also purged crashed-test residue from PROD (4 `TSEC101_*` roles + 2 privileges + dependents, `refresh_flat` re-run — they were visible in the Roles UI). Remember: the top bar + side nav are the corporate theme's dark CHROME (platform-wide signature, all 12 apps) — a fully-light shell = the Oracle Redwood theme in System Settings → Appearance.
- **2026-07-19 — Security Console (Fusion-style RBAC) Phase 0+1 LIVE (APP_VERSION 4.7.0; webtier release `20260719165634` LIVE):**
  db/v2/99 (DDL: `role_category` ABSTRACT/DUTY/JOB on DCT_ROLES [40 legacy roles → JOB],
  `verb` on DCT_PERMISSIONS, 12 new DCT_SEC_* tables — priv groups/items, role hierarchy,
  exclusions ROLE+USER, profiles+scopes+user assignments, materialized closure
  DCT_SEC_ROLE_PRIV_FLAT, version row, page+artifact registry; DUTY-assignment BEFORE
  INSERT trigger on DCT_USER_ROLES; abstract-role seeds EMPLOYEE/MANAGER/SUPPLIER/FREELANCER;
  DCT_WF_OBJECT_TYPE gains use_in_security/use_in_wf + BUSINESS_UNIT/DCT_PROGRAM/CHAPTER dims,
  hierarchy_kind now allows CLASS), db/v2/100 (V_DCT_USER_PRIVS_EFF + V_DCT_SEC_USER_SCOPE
  [ORG + CLASS descendants pre-expanded] + DCT_SEC pkg [has_priv w/ per-session version cache,
  has_priv_or_role grandfather helper — NULL legacy role = any-valid-session until
  FEATURE_SEC_ENFORCE_<MOD>=Y, refresh_flat full closure rebuild, copy_role, add/remove_hierarchy
  w/ cycle+depth≤5 guards] + DCT_SEC_DATA.is_unrestricted fail-open gate), db/v2/101
  (30 handlers under /dct/sec/ — **RE-RUN 101 AFTER ANY 11 RE-RUN**), db/v2/102 (GL+ADMIN seeds).
  Tests: 100t 32/32 (incl. the equivalence invariant: v_dct_user_privs_eff ≡ v_dct_user_permissions
  for every user), 101t API 44/44, security_console_browser.py 19/19 EN+AR/RTL.
  Admin JET: new nav group **Security Console** (privileges / privilegeGroups / abstractRoles /
  dutyRoles / jobRoles / secProfiles / userManagement + secRoleEdit), `services/secService.js`,
  Vault-styled (`rm-sc-*` additions in shared vault.css). Shared: `<security-info>` component
  (`shared/js/components/securityInfo.js`, `.si-*` in platform.css, `si.*` keys in common i18n)
  — SYS_ADMIN-only page-security drawer; registered in Admin main.js. Shared change ⇒ APP_VERSION
  bumped in ALL apps. Legacy users/userEdit/roles/permissions pages UNCHANGED and still routable —
  they retire only after User Management passes UAT. Gotchas: `POST /dct/users` requires `roles: []`
  (FOR 1..get_count(NULL) = ORA-06502); test-user profile end-dating must backdate start_date
  (chk_sec_up_dates); exclusions are branch-scoped (role A's exclusion never kills role B's grant).
