# Task Management (App 207) — Deployment Notes

Module `TASK_MGMT` · ORDS `/ords/admin/tm/` (`tm.rest`) · JET `final apps/TM/Jet/` · brand teal `#0E8A8A`.
Canonical platform-wide SQLcl/ORDS rules live in **Admin → docs/deployment-notes.md §2**; this file is TM-specific.

## 0. Deployment history (most recent first)

- **2026-07-18 — Vacations profile photos + module rename "Task Management → Team Management" (APP_VERSION TM→4.9.12, FLEET-WIDE bump).** (a) Vacations member avatars (team-grid/week/year/day-agenda + detail modal) now render the profile photo via the shared Admin `/dct/users/:id/photo` **media** handler (no auth header needed), keyed on `userId`; `photoErr` drops the `<img>` on 404 so initials show through (`selVacPhoto`/`selVacInitials` computeds for the modal). (b) **Module renamed to "Team Management"** — `mod.tm` in `shared/i18n/common.{en,ar}.json` (EN "Team Management" / AR "إدارة الفريق") + TM `tm.dashboard.title`. **This is a shared/ change:** `i18n.js` fetches `common.<lang>.json?v=<APP_VERSION>` and nginx caches JSON 7d, so EVERY app's cached browsers keep the old label until that app's APP_VERSION bumps → **bumped all 12 apps' APP_VERSION** (Admin 4.7.6, PC 4.5.31, CC 4.5.32, FL 4.18.15, DT 4.5.31, HR 4.6.27, AR 4.7.15, TM 4.9.12, ATD 1.23.13, GL 1.34.10, BI 1.10.20, AP 1.13.5) and redeployed the whole fleet. Verified live: side-nav + switcher read "Team Management"; a member with a profile photo renders it, others fall back to initials; 0 console errors.

- **2026-07-18 — Team Vacations: Day/Week/Month/Year time scales (DRAFT, frontend-only; APP_VERSION 4.9.0→4.9.1).** Added a scale switcher (top-right) driving a purpose-built layout per scale: **Day** = agenda of who's out (type/status chips + day X/Y); **Week** = member×7-day grid; **Month** = the existing APEX calendar with its Calendar/Team-grid sub-toggle (shown only at Month scale); **Year** = member×12-month leave-day heat matrix (single-hue teal magnitude ramp) + team-total footer, current month underlined, **cell → drill into that Month**. KPI band is now scale-aware (away today/this week/this month/this year; leave-days per scope; peak over the visible range). Context-preserving scale switch (lands on today if today is in the viewed period). `vacations.js` rewritten around a `scale` observable (`normalizeAnchor`/`step`/`matrixRows` helpers); +20 i18n keys both langs (removed `kpi.outMonth`; added `kpi.away`, `scale.*`, `scope.*`, `scopeShort.*`, `noneRange`, `teamTotal`, `dayWord`); `.vac-agenda`/`.vac-wd`/`.vac-yr*` in `css/app.css`. Only TM changed (no `shared/`). Verified live 4/4 scales EN+AR/RTL, year→month drill, 0 console errors. Deployed via `webtier/deploy_frontend.sh` (SSH_USER=opc).

- **2026-07-17 — Team Vacations page (DRAFT, frontend-only; APP_VERSION 4.8.14→4.9.0, webtier release 20260717221915).** New **Vacations** view under Work in the side-nav (🌴): APEX-Calendar-style month grid with lane-laid multi-day leave bars + a per-member **Team grid** matrix toggle, KPI band (on-leave-today / away-this-month / leave-days / peak-overlap), 5 leave types on a dataviz-validated CVD-safe palette (Annual teal / Sick orange / Mission blue / Training olive / Other magenta; approved solid, **pending dashed+striped**), click-to-open detail + plan/edit modals with member picker/type/dates/notes/validation. EN/AR + RTL, ~40 i18n keys both files. Files: `js/views/vacations.html`, `js/viewModels/vacations.js`, **`js/services/vacationService.js`** (new), `.vac-*` in `css/app.css`, nav entry in `appController.js`. NO DB/ORDS layer yet. **⚠ Runs on a DETERMINISTIC SAMPLE OVERLAY draped over the real team roster** (yellow "Sample data — draft" badge shown) — `vacationService.getVacations` calls future `GET /tm/vacations` first and falls back to generated sample when it 404s, so the page goes live unchanged once the endpoint ships; the fabricated leave sits against real member names, so treat as a preview only. Deployed via `webtier/deploy_frontend.sh` (SSH_USER=opc). Smoke: three new files 200 over HTTPS, root 302, branding 200. **Rollback target: `20260717144738`.** Only TM changed (no `shared/` touch → no other app bumped). Uncommitted on branch `wf-worklist-ui`.

- **2026-07-14 — Notifications page KO binding fix (APP_VERSION 4.8.11→4.8.12, webtier release 20260714201814):** `notifications.html` bound the type badge as bare `notifType || notif_type`, but the notifications handler emits the key `type` — KO threw `notifType is not defined` and aborted the list foreach. Fixed to `$data.notifType || $data.type` (same fix in FL/HR/CC/TM; see FL deployment-notes 2026-07-14 hotfix round). Frontend only.

- **2026-06-20 — Reporting-Cycle engine + Executive Visibility + Roles (Phase A). APP_VERSION → 4.7.0.**
  - **Business need:** management visibility across all DCT departments/sectors; per-team reporting cadence with pre-deadline reminders + executive consolidation; strict per-team isolation with explicit cross-team exceptions; admin-definable roles; team parent-child hierarchy.
  - **New DB scripts (run in order, after the base install):**
    - `09_tm_cycle_ddl.sql` — schema type `DCT_TM_ID_TAB`; `DCT_TM_TEAMS.parent_team_id` (self-ref hierarchy) + `DCT_TM_TEAM_RELATIONS`; cycle engine tables `DCT_TM_CYCLE_CONFIG` / `DCT_TM_CYCLE_SUBMITTERS` / `DCT_TM_PERIODS` (`snapshot_json` CLOB, `IS JSON`) / `DCT_TM_SUBMISSIONS` / `DCT_TM_PERIOD_SIGNOFF`; `DCT_TM_VISIBILITY_GRANT` (one structural shape CHECK).
    - `10_tm_cycle_seed.sql` — lookups `TM_CADENCE` / `TM_SUBMITTER_SCOPE` / `TM_PERIOD_STATUS` / `TM_SUBMISSION_STATUS` / `TM_VIS_SCOPE` / `TM_VIS_LEVEL` / `TM_GRANT_STATUS` / `TM_TEAM_RELATION`; 3 new artifact types `CYCLE`/`PERIOD`/`SUBMISSION` + default CRUD matrix on the 6 roles; settings `CYCLE_ENABLED`, `CYCLE_HORIZON_DAYS`.
    - `11_tm_vis_pkg.sql` — `DCT_TM_VIS_PKG`: pipelined `visible_teams(uid)` (membership + leadership + TEAM/TEAM_TREE/ORG grants + org-head + scope=ALL/admin), `can_view_team`, `can_view_all`, `create_grant`/`revoke_grant`, `set_team_parent` (cycle-guarded).
    - `12_tm_cycle_pkg.sql` — `DCT_TM_CYCLE_PKG`: `save_cycle_config`, `generate_periods` (idempotent), `open_period`+`build_submission_skeleton` (auto-pulls task/objective/deliverable/RAID metrics), `save_submission`/`submit_submission` (DRAFT→SUBMITTED/LATE), `close_period` (writes immutable `snapshot_json` via APEX_JSON), `lock_period`, `leader_signoff`, `run_cycle_sweep`/`run_job`.
    - `13_tm_cycle_views.sql` — `dct_tm_team_tree_v`, `dct_tm_cycle_config_v`, `dct_tm_period_v`, `dct_tm_submission_v`, `dct_tm_visibility_grant_v`, `dct_tm_exec_team_v` (per-team scorecard: open/done/overdue, high risks, KPI %, on-time %).
    - `14_tm_cycle_ords.sql` — **adds** routes to `tm.rest` (does NOT delete the module): `cycle-config`, `periods` (+`/generate|/close|/lock|/signoff`, `/:id/snapshot`), `period-status`, `my-submissions`, `submissions` (+`/save|/submit`), `visibility-grants` (+`/revoke`), `my-visibility`, `teams/parent`, `team-tree`, `exec/teams`, `roles` (+`/retire`), `role-template-perm`.
    - `15_tm_cycle_jobs.sql` — `DCT_TM_CYCLE_JOB` daily **06:30 Asia/Dubai** (before the 07:00 reminder job): generate → open → remind non-submitters on `reminder_days_before` offsets → mark DUE/overdue → escalate to leader → auto-close + snapshot after a 7-day grace.
  - **Edited base scripts (redeploy both):** `02_tm_views.sql` (`dct_tm_team_v` gains `parent_team_id`/`parent_team_name`); `04_tm_pkg.sql` (`is_tm_admin` now accepts **SYS_ADMIN** as well as TM_ADMIN); `06_tm_ords.sql` — **row-level visibility retrofit**: every team-keyed GET list handler (`teams`, `teams/:id`, `members`, `objectives`, `tasks`, `deliverables`, `raid`, `milestones`, `meetings`) + the dashboard aggregates now filter `team_id IN (SELECT column_value FROM TABLE(dct_tm_vis_pkg.visible_teams(l_uid)))` (closes a pre-existing leak where any authed user could read any team). boot roles now return `tmRoleId`/`isSystem`/`descriptionEn`; teams list returns `orgId`/`parentTeamId`.
  - **★ Install-order gotcha (cost a redeploy):** `06_tm_ords.sql` begins with `ORDS.DELETE_MODULE('tm.rest')`, so **re-running 06 wipes 14's handlers**. Order is always **06 → 14** in the same deploy pass; if you touch 06, you MUST re-run 14 afterward (fresh session). The `install.sql` should list 14 immediately after 06.
  - **Deploy order used:** schema-altering scripts (`09`,`10`,`11`,`12`,`13`,`02`,`04`) in CURRENT_SCHEMA=PROD sessions; then a **fresh session** for `06` then `14`; then `15`. Recompile `DCT_TM_VIS_PKG`/`DCT_TM_CYCLE_PKG` after re-running `04` (they depend on `dct_tm_pkg`).
  - **Pipelined-function gotcha:** `visible_teams` must return a **schema-level** `CREATE TYPE prod.dct_tm_id_tab` (a package type can't be used in a SQL `TABLE()` across the ADMIN→PROD synonym boundary); ADMIN synonym `dct_tm_vis_pkg` required in 06/14.
  - **Frontend (APP_VERSION 4.7.0):** new views `myUpdates` (member check-in), `exec` (executive rollup: RAG donut + on-time leaderboard + grouped scorecard with org/team-tree/flat drill-down), `visibilityGrants` (admin), `teamCycle` (cadence config + periods + roster + close/lock/signoff + parent-team picker); `teamRoles` promoted to full CRUD (create/edit/retire role + clickable template matrix); nav entries added (Work→My Check-ins, Insights→Executive View, Admin→Visibility Grants); ~120 EN+AR i18n keys; new `.sub-stat`/`.pstat`/`.metric*`/`.exec-*`/`.perm-cell` styles in `css/app.css`. Reporting-Cycle button added to `teamDetail` header.
  - **Verified live (ORDS, ADMIN/SYS_ADMIN session):** full lifecycle — save cadence → generate → save+submit submission (SUBMITTED, on-time) → period-status roster → leader signoff → close (snapshot CLOB = valid JSON) → my-submissions; visibility grant create/list/revoke; retrofitted `teams`/`tasks` still 200 and scoped; `team-tree`/`exec/teams`/`my-visibility` 200. Frontend JS/JSON syntax-clean.

- **2026-06-20 — Phase B/C enhancements. APP_VERSION → 4.8.0.**
  - **Phase B (exec polish):** the executive rollup + scorecard + RAG/on-time charts + org/team-tree/flat drill-down shipped in Phase A; added **achievement badges** to the scorecard (`exec.js badges()`: perfect on-time / zero-overdue / all-on-track).
  - **Phase C — AI period summary:** new `16_tm_ai_pkg.sql` (`DCT_TM_AI_PKG.summarize_period`) — calls Anthropic via `DBMS_CLOUD.SEND_REQUEST` (model `claude-sonnet-4-6`), **reuses the active ANTHROPIC key from `DCT_AR_AI_PROVIDERS`** (no new key), gated by the `AI_FEATURES_ENABLED` module setting (default **N** — admin opt-in). ACL for `api.anthropic.com` already granted (idempotent re-grant in the script). Builds a factual context from the period's submissions so the model only re-words supplied facts (no hallucinated data); writes `dct_tm_periods.ai_summary`. ORDS `POST/GET periods/:id/ai-summary` added to `14`; UI button + display in the teamCycle roster modal. **Verified live: produced a genuine board-ready paragraph from real submission data; flag reset to N after test.**
  - **Phase C — weekly digest + mobile push:** `DCT_TM_CYCLE_PKG.run_cycle_sweep` step 7 sends a Sunday leader digest (`TM_WEEKLY_DIGEST`), gated by new setting `CYCLE_WEEKLY_DIGEST` (default Y). **Mobile push needs no new code** — every `DCT_NOTIFY.send` row (TM_CYCLE_OPEN/REMINDER/OVERDUE/ESCALATION/SUBMITTED, TM_PERIOD_CLOSED, TM_WEEKLY_DIGEST) auto-fans to App 209 via the `DCT_NOTIFICATIONS`→`DCT_PUSH_OUTBOX` AFTER-INSERT trigger (db/v2/28). Sweep re-verified clean with a Sunday `as_of`.
  - **Phase C — Gantt/timeline:** new `timeline` view (pure-CSS date-axis bars for tasks/deliverables/milestones), reached from the teamDetail header.
  - **★ Bug fixed (would have broken the daily job):** sweep-driven period transitions (`open_period`, mark-DUE, auto-close) called `log_status` with a NULL actor, but `DCT_REQUEST_STATUS_HISTORY.CHANGED_BY` is NOT NULL → `DCT_TM_CYCLE_PKG.log_status` now skips the history row when actor is NULL (system action). Redeploy: `@10` `@12` (PROD session), `@16` (PROD session), then fresh session `@06` → `@14`.
  - **Pending:** browser Playwright E2E + UAT round for both waves.

## 1. Deploy checklist

**DB (SQLcl `sql -name prod_mcp`, run scripts via PowerShell `& sql.exe -S -name prod_mcp "@file"`):**
1. `@install.sql` — runs `01_tm_ddl` → `03_tm_seed` → `02_tm_views` → `04_tm_pkg` → `05_tm_reminder_pkg` → `07_tm_jobs`.
2. **Fresh session**, then `@06_tm_ords.sql` (ORDS module + ADMIN synonyms — must NOT follow an `ALTER SESSION SET CURRENT_SCHEMA=PROD`, or synonyms self-reference → ORA-01471).
3. Verify: `@test/tm_smoke_test.sql` (16 asserts, rolls back) and `SELECT object_name,status FROM all_objects WHERE object_name LIKE 'DCT_TM%' AND status='INVALID'` → 0 rows.

**Frontend:**
4. `final apps/TM/Jet/index.html` `window.APP_VERSION` is `4.5.0`. Bump on every TM frontend deploy (drives requirejs + i18n cache key).
5. App 207 is registered in `final apps/shared/js/shell.js` MODULES + `shared/i18n/common.*.json` (`mod.tm`). **A change to `final apps/shared/` requires bumping `APP_VERSION` in ALL 8 apps** (done at 4.5.0 for this release).
6. **Update `docs/functions_list.md`** if this deploy added/removed/renamed any view, viewModel method, service, or ORDS endpoint (functional inventory — see root `CLAUDE.md` → "Functions List").

## 2. Hard-won gotchas (this build)

- **`SET SQLBLANKLINES ON` is mandatory** in every TM `.sql`. Without it, SQLcl 26.1 silently swallows any PL/SQL block that contains interior blank lines (no error; prints bogus WHENEVER help on the next statement). Symptom seen: lookup seed block vanished, CATS stayed 0.
- **CRLF line endings** required (SQLcl skips LF-only files). All TM scripts written CRLF, UTF-8 **no BOM**.
- **Keyword-free comment banners** — a leading `--` comment containing a SQL keyword (e.g. `MERGE`) makes SQLcl swallow the next statement.
- **No Oracle filtered indexes** — used function-based unique indexes for "one active membership per (team,user)" and the role-perm template/override key (`NVL(team_id,-1)`).
- **PLS-00231** — package-private functions can't be used in SQL DML; `actor_name`/`gen_code`/`role_id_of` are published in the `DCT_TM_PKG` spec.
- **Arabic** stored correctly (multibyte, verified no mojibake) via UTF-8-no-BOM files + PowerShell SQLcl. Do NOT deploy seeds through the bash `printf | sql` pipe (mangles backslashes/UTF-8).
- **utPLSQL is not installed** on this ADB — unit tests are a standalone transactional assert harness (`test/tm_smoke_test.sql`).

## 3. Permission model

Two layers: system roles `TM_ADMIN`/`TM_USER` (module access, via App 200) + **team-scoped roles** (Leader/Supervisor/Coordinator/Member/Quality/Observer) carrying an artifact CRUD matrix (`DCT_TM_ROLE_PERMS`, template default + per-team override). `DCT_TM_PKG.tm_can()/require_perm()` enforce it; ORDS maps `-20401→401 -20403→403 -20404→404 -20001/-20090→400`.

## 4. Reminders

`DCT_TM_REMINDER_JOB` (DBMS_SCHEDULER, daily 07:00 Asia/Dubai) → `DCT_TM_REMINDER_PKG.run_job`: due-within-lead-days + overdue reminders to assignees (per `DCT_TM_REMINDER_PREFS.lead_days`, fallback module setting `DEFAULT_REMINDER_LEAD_DAYS`) + overdue escalation to team leaders. Run manually: `BEGIN dct_tm_reminder_pkg.run_job; END;`.

## 5. Deployment history

- **2026-06-15 — Region header flush (TM APP_VERSION 4.6.4→4.6.5; shared/ change → all 8 apps bumped):** shared `platform.css` flush rule (see Admin/FL notes) removes the gap between every in-card header and the border. No TM markup change — teamDetail's in-card `.section-heading`/`.section-heading-row` are already first-child (flush automatically); the per-tab section headers sit outside cards and are unaffected. Version bumped for the shared/ change. Frontend/CSS only.
- **2026-06-15 — Team Detail visibility enhancements (E1–E5). APP_VERSION → 4.6.1.**
  - **What:** the team workspace is now readable at a glance (frontend-only — no DB/ORDS change; `GET teams/:id` already returns every summary field used). **E1** a `.tm-hdr-band` summary header (RAG pill, task-completion bar, stat chips for members/open/overdue[danger]/risks[warn]/deliverables, leader avatar, date range) replaces the thin subtitle. **E2** each tab shows a count pill + a red attention dot (Tasks when overdue>0, RAID when openRisks>0). **E3** the Overview is a command panel: semantic `.tm-statcard`s + a "Needs attention" list (overdue tasks + open high/critical RAID) + an "Upcoming" strip (soonest milestones/deliverables/meetings) + a compact Team Info card. **E4** status/severity render as semantic `.badge--ok/warn/danger/info/muted` across all detail tables. **E5** denser cells (deliverables mini progress bar, owner avatars, overdue dates in red).
  - **Frontend (TM):** `css/app.css` gained the additive `.tm-hdr-*`/`.tm-stat-chip`/`.tm-statcard`/`.tm-attn-*`/`.tm-upcoming`/`.tm-tab__count|__dot`/`.badge--*` blocks (built on platform tokens; reuses `.rag`/`.tm-prog`/`.avatar`/`.is-overdue`). `teamDetail.html` header band + tab template + Overview rebuilt; table status badges → `statusBadge`, RAID severity → `sevBadge`. `teamDetail.js` added `taskPct` / `counts`+`tabCount`/`tabAttn` / `loadOverview` (one parallel fetch of tasks/raid/milestones/deliverables/meetings → builds attention + upcoming, also refines tab counts) / `statusBadge`/`sevBadge` + date helpers; `refreshTeam` seeds the count pills from the summary. New i18n `tm.detail.needsAttention/upcoming/taskCompletion/allClear/openLabel/daysLate/daysLeft/dueToday` (EN+AR). `functions_list.md` updated. All additions are new — no existing observable/handler renamed (drawers/kanban unaffected).
  - **Verified** in a KO render harness (real view+VM+`shared/editDrawer`, stubbed services): header band + 62% bar + chips; 7 tab count pills + 2 attention dots; 6 stat-cards + 3 attention rows + 4 upcoming items; Deliverables tab shows color-coded badges + mini bars + bold-red overdue date; no page errors. Built from the approved `mockups/teamDetail-enhanced.html` (since removed). Design via /ui-design-system + /frontend-design + /senior-frontend.

- **2026-06-15 — Per-row EDIT on the Teams list. APP_VERSION → 4.6.0.**
  - **What:** the **Teams** list (`teams`) gained a per-row **Edit** action (last column) that opens the existing create `<edit-drawer>` pre-filled to **update** the team (name EN/AR, type, class, category, status, objective). The status field shows only in edit mode; the drawer title/save-label flip Create↔Edit.
  - **Frontend only — no DB/ORDS change** (`tmService.updateTeam` → `PUT teams/:id` already existed and `DCT_TM_PKG.update_team` is fully NVL-guarded, so the partial body sent here preserves leader/purpose/dates). `teams.js`: `editId` + `fmStatus`, `editTeam(team)` (loads the full record via `getTeam` then opens the drawer), `save` branches create↔update, reactive `formTitle`/`formSaveLabel` computeds, and a `showForm` subscription that clears `editId` on any close (Esc/scrim/cancel). `teams.html`: trailing Edit column (the cell sets `clickBubble:false` so the Edit button doesn't trigger the row's navigate-to-detail) + an edit-only status select in the drawer. Reused existing i18n (`tm.detail.editTeam`, `tm.action.saveChanges`/`edit`) — no new keys. `functions_list.md` updated (`editTeam`).
  - **Pending live smoke test** (Teams list → row Edit → change name/status → save → row refreshes).

- **2026-06-15 — Per-row EDIT for Members + Documents (new update endpoints). APP_VERSION → 4.5.9.**
  - **What:** the **Members** and **Documents** tabs on `teamDetail` gained a per-row **Edit** action opening the shared `<edit-drawer>` pre-filled to update the record — Members edits **role + title**, Documents edits **file name + doc type + notes** (the stored blob is not touched; the file picker is hidden in edit mode). This completes per-row edit across every Team-detail tab (Deliverables/RAID/Milestones/Meetings shipped in 4.5.7; Objectives/Tasks already had Manage/task drawers).
  - **DB (`04_tm_pkg.sql`, re-run session 1):** new `DCT_TM_PKG.update_member(actor,team,user,role_code,title)` (perm `MEMBER/UPDATE`; UPDATEs `tm_role_id`+`title` on the active membership) and `update_document(actor,doc_id,file_name,doc_type_code,notes)` (derives team from `dct_documents.reference_id`, perm `DOCUMENT/UPDATE`, resolves doc-type code→id with NVL-keep fallback).
  - **DB (`02_tm_views.sql`, re-run session 1):** `dct_tm_document_v` now also exposes `dt.doc_type_code` (needed to pre-select the type in the edit drawer).
  - **ORDS (`06_tm_ords.sql`, re-run session 2 / fresh session):** added `POST members/update` and `POST documents/update` handlers (standard `validate_session` + error mapping); the `GET documents` handler now also emits `docTypeCode` + `notes` so the drawer can pre-fill. Deployed + verified 2026-06-15: 0 invalid TM objects, both procedures present, view column present, both templates registered.
  - **Frontend (TM):** `tmService` `updateMember`/`updateDocument`; `teamDetail.js` `openEditMember`/`openEditDoc` + `memberEditId`/`docEditId` (saveMember/saveDoc branch add↔update) + reactive `memberDrawerTitle`/`memberSaveLabel`/`docDrawerTitle`/`docSaveLabel` computeds; `teamDetail.html` Edit button in the Members actions cell + a new Documents actions column, member drawer shows the person read-only when editing, document drawer hides the file picker when editing. New i18n `tm.detail.edit{Member,Document}` (EN+AR). `functions_list.md` updated (`openEditMember`/`openEditDoc` + the two endpoints).
  - **Deploy gotchas (this session):** SQLcl `@script` left on the command line **hangs at the prompt** (scripts have no trailing `EXIT`) → drive it with a small `c:\tmp` runner `.sql` that `@`s the absolute path(s) then `EXIT;`. Do **not** feed `@file` via PowerShell stdin pipe — PS prepends a UTF-8 **BOM** → SQLcl `Unknown Command` on line 1. Run session-1 (views+pkg) and session-2 (ORDS, must be fresh for the synonyms) as separate `sql.exe` invocations.
  - **Pending live smoke test** (open a team → Members → Edit → change role/title → save; Documents → Edit → change name/type/notes → save).

- **2026-06-15 — Fix: New-Team create form → `<edit-drawer>` (was a broken modal). APP_VERSION → 4.5.8.**
  - **Bug:** on the Teams list, **+ New Team** rendered the form at the **bottom of the page** and it **vanished when a field was focused**. Root cause in `views/teams.html`: the create modal was authored with `.modal-overlay` and `.modal-box` as **siblings**, but `platform.css` defines `.modal-overlay` as the `position:fixed` flex-centering container and expects `.modal-box` as its **child**. As siblings the box fell into normal flow (below the table) while the fixed full-screen overlay painted on top — so clicking a field hit the overlay's `click: cancel` → `showForm(false)` → form disappeared.
  - **Fix (redesign, not a patch):** replaced the bespoke `.modal-overlay`/`.modal-box` block with the shared `<edit-drawer params="open: showForm, vm: $vm, title: t('tm.teams.new'), saveLabel: t('tm.action.create'), cancelLabel: t('tm.action.cancel'), onSave: save, width:'620px'">` wrapping the **unchanged** `.form-grid` — same pattern as Edit-Team and the teamDetail add forms. Drawer is fixed-position with its own `.ed-scrim`, so the sibling/stacking bug can't recur. **No `teams.js` change** (`showForm`/`newTeam`/`cancel`/`save` already work as the drawer's `open`/handlers); `functions_list.md` unaffected; all `t()` keys already existed in both locales. `'shared/editDrawer'` was already in TM `main.js` → no shared-layer change, only TM's `APP_VERSION` bumped.
  - **Verified** in a KO render harness (real view+VM+`shared/editDrawer`, stubbed services): drawer slides in at the inline-end edge (top y=0, not bottom-of-page); focusing/typing in each field keeps it open and accepts input; Create with a name calls `createTeam` + closes + reloads; Cancel / scrim / **Esc** close it; no page errors.

- **2026-06-15 — Team-detail add forms → `<edit-drawer>` + per-row EDIT on read-only tabs. APP_VERSION → 4.5.7.**
  - **What:** (a) every data-entry form on the **Team detail** page (`teamDetail`) now uses the shared `<edit-drawer>` slide-in, matching the Edit-Team drawer — the inline `<div class="card">` "+ Add" panels are gone. (b) The four previously **read-only** tabs — **Deliverables, RAID, Milestones, Meetings** — gained a per-row **Edit** action (last column) that opens the same drawer pre-filled to **update** the record.
  - **Add forms (TM):** the six generic add cards (objective/task/deliverable/raid/milestone/meeting), which share one `adding()` state + `fTitle/fDesc/…` fields + `saveAdd`, were consolidated into **one** `<edit-drawer params="open: addOpen, title: addTitle, onSave: saveAdd">` whose body switches field-set by `<!-- ko if: adding() === '…' -->`. The **member** and **document** add cards became their own drawers (`memberOpen`/`docOpen` → `saveMember`/`saveDoc`). Writable computeds `addOpen`/`memberOpen`/`docOpen` let the drawer's internal `close()` clear the open state.
  - **Per-row edit (TM):** `teamDetail.js` gained `self.editId` (record id; null = create) + `self.openEdit(kind,row)` which pre-fills the shared add drawer from the row and sets `editId`. `saveAdd` now passes the id (`deliverableId`/`logId`/`milestoneId`/`meetingId`, also `objectiveId`/`taskId`) so the **existing upsert POST handlers update by id** — *no DB/ORDS change* (every TM save endpoint already read its `*_id` from the body and `DCT_TM_PKG.upsert_*` switches insert↔update; UPDATE clauses are NVL-guarded so fields the form doesn't carry are preserved — except link cols `objective_id`/`milestone_id`/`task_id`, which the UI never exposes anyway). `addTitle` switches Add↔Edit label via new i18n keys `tm.detail.edit{Deliverable,Raid,Milestone,Meeting}` (EN+AR). HTML: each of the four tables got a trailing `<th>` + a `cell-actions` Edit button (`$vm.openEdit('…', $data)`).
  - **Scope note:** **Objectives** already edits via its "Manage" drawer and **Tasks** via the task drawer (kanban card) — left as-is (richer `.modal-box .tm-drawer` panels with sub-lists/KR/activity). **Members** role is editable inline (`set_member_role`); **Documents** are insert-only — neither has a record-update endpoint, so no per-row Edit was added there (would need new ORDS/package handlers — see open question).
  - No shared-layer change (`'shared/editDrawer'` already in TM `main.js`), so only TM's `APP_VERSION` bumped. `functions_list.md` updated (added `openEdit`).
  - **Pending live smoke test** (Playwright quick-login → open a team → Deliverables/RAID/Milestones/Meetings → "+ Add" and per-row "Edit" → verify slide-in pre-fill, save/upsert, Esc/scrim close).

- **2026-06-15 — Teams list redesign: search toolbar + platform data table. APP_VERSION → 4.5.5.**
  - **What:** the **Teams** list (`views/teams.html` + `viewModels/teams.js`) was redesigned from a bespoke `.team-grid`/`.team-card` card grid (with a `.filter-bar` + `.input` filter row) into the shared platform pattern — `.view-toolbar` + `.search-box` for search, the three lookup filters as `.form-control--sm`, and a `.data-table-wrap` > `table.data-table` result (Code · Team[+Arabic] · Type·Class · Members chip · Tasks[+overdue] · Health RAG pill · Status badge). Row click still opens `teamDetail`. Frontend-only — no DB/ORDS change (`tm.listTeams()` already returns every field). Built/approved against a static mockup first (frontend-design + ui-design-system skills); verified end-to-end in a KO harness (real view+VM, stubbed services) → 4 rows, no binding errors.
  - **Frontend:** `teams.html` toolbar + table rewrite; `teams.js` enriches each row with display labels (`typeClass`/`statusName`/`healthLabel` via the boot lookups — now loaded **before** the first `load()` so codes map to names) + new `self.statusClass(code)` for the status badge; `app.css` dropped the dead `.team-card*`/`.team-grid` rules, added `.tm-team-*` cell helpers (structure still comes from platform.css). New i18n keys `tm.teams.col.*` + `tm.teams.over`/`tm.teams.results` added to **both** `app.en.json` and `app.ar.json`. `functions_list.md` updated (added `statusClass`).
  - **Note:** the create-team modal was left untouched (already platform-correct). No shared-layer change, so only TM's `APP_VERSION` bumped.

- **2026-06-15 — Shared `<edit-drawer>` component + Edit-Team conversion. APP_VERSION → 4.5.3.**
  - **What:** introduced a reusable APEX-style right-edge drawer as the first promoted entry from the new `final apps/Shared UI Components/` reference gallery. TM is the reference adopter — the **Edit Team** modal in `teamDetail` is now a drawer.
  - **Shared (new):** `shared/css/platform.css` gained the `.ed-*` block (scrim, drawer, themed header reusing `--region-hd-*`, pulsing unsaved-dot, staggered field reveal, RTL-aware slide); `shared/js/editDrawer.js` registers the `edit-drawer` Knockout component. Body markup is supplied by the host view and rebound to its own ViewModel via `template:{ nodes: $componentTemplateNodes, data: vm }` — so fields keep binding to the view's observables while the chrome binds to the component VM. **This is a `final apps/shared/` change** → per the shared-change rule it eventually needs an `APP_VERSION` bump in every app, but the `.ed-*` CSS + component are inert until an app adds `'shared/editDrawer'` to its `main.js`, so the practical minimum is to bump each app as it adopts the drawer (only TM here).
  - **Frontend (TM):** `main.js` require array gained `'shared/editDrawer'`; `teamDetail.html` Edit-Team `.modal-overlay`/`.modal-box` block replaced by `<edit-drawer params="open: showEdit, vm: $vm, title:…, onSave: saveTeam, width:'760px'">` wrapping the **unchanged** form-grid. (The `width` param binds the drawer's real `width`, bounded by `min/max` in platform.css; default 560px — an earlier draft bound `max-width` only, which pinned every drawer at the 480px base.) **No ViewModel change** — `showEdit`/`eName…`/`saveTeam` already existed (so `functions_list.md` is unaffected). The task drawer + Teams create modal still use the legacy `.modal-box` pattern (unchanged).
  - **Note:** body markup is now always in the DOM (drawer hidden via transform) instead of gated behind `<!-- ko if: showEdit -->`; fields render empty until `editTeam()` populates them, which is fine. **Pending live smoke test** (Playwright quick-login → open a team → Edit → verify slide-in, save, Esc/scrim close).

- **2026-06-14 — Measurable objectives / Key Results (round 3). APP_VERSION → 4.5.2.**
  - **What:** team objectives are now measurable (OKR-style). Each objective owns 1..N **Key Results** (unit, baseline → target → current, direction, weight, target date); the objective's `progress_pct` is a **weighted roll-up** of its KRs when `progress_mode='AUTO'` (a `MANUAL` mode pins a hand-entered %). Objectives also gained full edit/remove (were add-only).
  - **DB (new):** `@08_tm_key_results.sql` — `DCT_TM_KEY_RESULTS` (ON DELETE CASCADE from objectives) + `DCT_TM_OBJECTIVES.progress_mode VARCHAR2(10) DEFAULT 'AUTO'` + lookups `TM_KR_DIRECTION` (INCREASE/DECREASE/MAINTAIN) and `TM_KR_UNIT` (suggestions only — the `unit` column is free text, NOT validated). Script is **re-runnable** (CREATE/ALTER guarded by ORA-00955/-01430; lookup seed is MERGE). Slots into `install.sql` as step **[3/7], before `02_tm_views.sql`** (the views reference the KR table + new column).
  - **DB (re-run):** `@02_tm_views.sql` (adds `dct_tm_key_result_v`, extends `dct_tm_objective_v`), `@04_tm_pkg.sql` (adds `upsert_key_result`/`record_kr_value`/`delete_key_result`/`delete_objective` + private `recompute_objective_progress`; `upsert_objective` gains `p_progress_mode`). Then **fresh session** `@06_tm_ords.sql` (new routes `GET/POST key-results`, `POST key-results/value`, `DELETE key-results/:id`, `DELETE objectives/:id`; +2 synonyms `dct_tm_key_results`, `dct_tm_key_result_v`). Smoke test **26/26**.
  - **KR CRUD is governed by the existing `OBJECTIVE` artifact permission** — no new `DCT_TM_ROLE_PERMS` rows / artifact type. KR status **reuses** `TM_OBJECTIVE_STATUS`.
  - **Per-KR progress is computed in `dct_tm_key_result_v`** (clamped 0–100): INCREASE `(cur−base)/(target−base)`, DECREASE `(base−cur)/(base−target)`, MAINTAIN `100 if cur≥target else cur/target`. `recompute_objective_progress` writes the weighted Σ back to `dct_tm_objectives.progress_pct` only when AUTO — so existing objective-progress dashboards/charts improve for free.
  - **Frontend:** objectives tab rows are clickable → **objective drawer** (mirrors the task drawer `.tm-drawer`): edit objective fields + Auto/Manual toggle, Key Results list with a baseline→target gauge, inline **Update value**, and add/edit/remove KR. New `tmService` methods `deleteObjective`/`listKeyResults`/`saveKeyResult`/`recordKrValue`/`deleteKeyResult`. ~22 EN+AR i18n keys; new `.tm-kr*` CSS in `css/app.css`.
  - **Gotcha (new):** the inline KR "current value" input must bind `value: $data.current, valueUpdate: 'input'` — plain `value:` only writes back to the (non-observable) row property on **blur**, so a click-to-Update that doesn't blur first posts a stale value. `valueUpdate:'input'` writes on every keystroke. (Auto-progress roll-up otherwise verified live: 50%→37.5%→62.5%→100%.)
  - **Verified live (Playwright quick-login):** drawer add INCREASE+DECREASE KR → objective auto 37.5%, update value → 62.5%, delete → 100%; 0 real console errors (only the universal pre-login branding 401).

- **2026-06-13 — UI/UX rework + functional enhancements (round 2). APP_VERSION → 4.5.1.**
  - **DB (redeployed):** `@04_tm_pkg.sql` (assign_task/unassign_task now log assignment + primary-reassignment history into `DCT_TM_TASK_UPDATES`; new `add_document` → unified `DCT_DOCUMENTS`, source_module='TM', base64 ≤ ~24 KB inline or metadata-only). Then **fresh session** `@06_tm_ords.sql` — new routes `GET /users`, `GET /tasks/assignees`, `POST /documents`; boot returns `docTypes`; +3 synonyms (`dct_users`, `dct_tm_task_assignees`, `dct_document_types`). Both compiled clean; new routes 401-gated.
  - **Frontend:** rebuilt `teamDetail` (overview definition-grid + Edit Team modal, member add/role/remove, task drawer with assignees + activity feed + comments, milestones/meetings add, new Documents tab). Converted `teams` modal + all forms to platform `.form-control`/`.form-group`/`.modal-overlay` classes; `css/app.css` gained an `.input`→form-control shim, helpers, and new components. New EN/AR i18n keys.
  - **Gotchas hit (add to the canonical list):**
    - **APEX_JSON omits NULL keys**, so a row missing an optional column has *no* such property. KO `text: dueDate` then throws *"dueDate is not defined"* (ReferenceError, not undefined). **Always bind optional fields as `$data.field`** in `foreach` rows — never bare. This is the #1 cause of a blank/broken list row.
    - **KO computed + plain (non-observable) vars:** a `ko.computed` only re-runs when an *observable* dependency changes. `canEditTeam` read a plain `myUid` set inside `boot().then`; because `isTmAdmin` stayed `false`, the computed never re-evaluated after `myUid` was set, so the Edit/manage buttons stayed hidden. Fix: make such late-set values `ko.observable`.
    - **TM redirects to the Admin portal for auth.** After quick-login the browser is at `/Admin/Jet/index.html`; to test/use the TM SPA load `/<port>/index.html` (TM root) *after* the session exists, else relative view paths resolve under `/Admin/Jet/` and 404.
    - Milestone status uses the **`TM_OBJECTIVE_STATUS`** lookup set (shared with objectives); meeting status uses **`TM_LOG_STATUS`** (which intentionally includes PLANNED/HELD/CANCELLED). There is no `TM_MILESTONE_STATUS`/`TM_MEETING_STATUS` set.
  - **Verified live (Playwright quick-login):** 15/15 authenticated backend flows incl. assign→reassign trail; UI renders with 0 console errors.

- **2026-06-13 — initial build (layer-by-layer).**
  - DDL: 13 `DCT_TM_*` tables + indexes + triggers.
  - Seed: 14 `TM_*` lookup sets, module `TASK_MGMT`, roles `TM_ADMIN`/`TM_USER`, 7 settings, 6 team-role templates × 10-artifact CRUD matrix (60 rows). Verified CATS=14/VALS=72/TMROLES=6/TMPERMS=60.
  - Package `DCT_TM_PKG` + 12 views + `DCT_TM_REMINDER_PKG` — all VALID; smoke test 16/16.
  - ORDS `tm.rest` — 23 templates, PUBLISHED; live 401 gate verified.
  - Scheduler job created (daily 07:00 Asia/Dubai).
  - JET app built (10 views/VMs), registered App 207 in shared shell + i18n; bumped APP_VERSION → 4.5.0 across all 8 apps. Static-served smoke OK.
  - Live Playwright E2E (quick-login via Admin) — **8/8 flows pass, 0 console errors**; evidence in `UAT/UAT_TM_round1-13-06-2026/evidence_13-Jun-2026/`.
  - Three fixes during the live run: (1) boot lookup query was SQL-Server `LIKE 'TM[_]%' ESCAPE '['` (matched nothing → empty type/class dropdowns → ORA-01400 on create) → Oracle `LIKE 'TM\_%' ESCAPE '\'`; (2) `notificationService.js` hit `/tm/notifications` → now `{base:'auth'}` (platform `/dct`); (3) `SIBLING_APPS` in **all 8 dev-proxies** was missing `'TM'` so the switcher's `/TM/Jet/` URL 404'd in dev — added. **Dev gotcha:** run only ONE app's dev-proxy at a time (port 8080); it serves itself at `/` and siblings at `/<App>/Jet/`. Kanban uses native HTML5 DnD — Playwright `drag_to` can't fire it; the E2E spec dispatches real `DragEvent`s.
  - Pending: authenticated pytest API (`tests/api`, needs `TM_USER`/`TM_PASS` env), UAT round-1 sign-off doc, AI-assist hooks (deferred sub-feature).
- **2026-06-15 — Binary document upload + file download (TM APP_VERSION 4.6.4; shared/ change):** document files now upload as **raw bytes** (no base64, no ~24 KB cap). The `documents` POST still creates the metadata row (now sent WITHOUT `fileB64`); two NEW handlers added to `06_tm_ords.sql`: **`PUT documents/:id/file`** (raw `:body`, deref ONCE; guarded by new `MAX_UPLOAD_MB` setting → HTTP 413; default 10) and **`GET documents/:id/file`** (`def_media` — TM previously had NO file-download endpoint). Added a `def_media` helper to `setup_tm_ords_tmp`. `teamDetail.js` `onFilePick` now stashes the File via shared `docUpload.choose()` (dropped the FileReader/24 KB cap); `saveDoc` does POST-metadata → `tmService.uploadDocumentFile(docId, file)` (`putBinary`). View: file `<input type=file>` replaced with a button calling `onFilePick`. Registered `shared/docUpload` in `main.js`. DB: `db/v2/27_max_upload_mb.sql` (seeds TASK_MGMT) + `06_tm_ords.sql` rerun (fresh session). **Verified live: 1.2 MB round-trip OK.**
- **2026-06-20 — Round 4 UAT (Reporting-Cycle wave) + a real bug caught & fixed:** ran the new hybrid Playwright+ORDS runner `assessment-3/phase4/tests/uat_run_tm_cycle.py` (28 cases: reporting cadence/period lifecycle, member check-in draft→submit, leader roster/sign-off/close-snapshot/lock, executive view + scorecard + badges, cross-team visibility grants + the row-level security retrofit, custom-role CRUD, parent-child hierarchy + cycle guard, AI period summary, Gantt/timeline). Package in `UAT/UAT_TM_round4-20-06-2026/` (workbook + Word results + 28 evidence shots). **Final: 28/28 PASS.**
  - **Product bug found & fixed — granted/non-member teams missing from the Teams list.** In `06_tm_ords.sql` the `teams` GET filter was `(l_mine != 'Y' OR EXISTS(member…))`. The UI sends `mine=''` (→ NULL) when the "My teams" toggle is off, so `NULL != 'Y'` is NULL and the whole predicate went NULL for any team the caller is **not a member of** — silently dropping admin-visible teams AND executive **visibility-grant** teams from the list (detail/exec/periods were unaffected because they use `can_view_team`/`visible_teams` directly). Fixed to `NVL(l_mine,'N') != 'Y' OR EXISTS(…)`. After fix: ADMIN list 13→16 (now all visible), and a VIEWER grant correctly surfaces the team in the grantee's list. **This was a genuine gap in the executive-viewer feature, caught only by the cross-team UAT cases.**
  - Redeploy: re-ran `06_tm_ords.sql` **then** `14_tm_cycle_ords.sql` in one fresh SQLcl session (06 does `ORDS.DELETE_MODULE('tm.rest')` → always run 14 right after). No frontend change → **no APP_VERSION bump**.
  - The AI case needs the gated `AI_FEATURES_ENABLED`; the runner flips it to `Y` via SQLcl before the run and restores `N` after — **verified back to N in PROD** post-run. AI produced a genuine ~750-char board-ready summary.
  - Runner env gotchas: SQLcl 26.1 throws a `SqlclConsole`/"Incorrect function" stack when stdin is a pipe — invoke via Python `subprocess` (works) not the raw shell; use forward-slash absolute paths in the `@` runner (back-slash `\f`/`\0` get mangled). Isolation persona = `SHAIKHA.GALAMERI` (sees 0 teams baseline); denial persona = `NASER.ALKHAJA`.
